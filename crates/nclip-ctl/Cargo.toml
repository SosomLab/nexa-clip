# nclip-ctl — vendor 컨트롤 재수출 + **우리 신규 컨트롤**.
# ★ 규율 U-1(docs/13 §5): crates/vendor/* 는 고치지 않는다. 확장은 전부 여기에 둔다.
# 앱 도메인 의존 0을 vendor와 동일하게 유지한다 — 문자열은 주입으로 받는다.
[package]
name = "nclip-ctl"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Nexa Clip 컨트롤 — nbeep-ctl 재수출 + ClipRow·RichTextView·ViewModeList·세로 Toolbar"

[lints]
workspace = true

[dependencies]
nbeep-ctl.workspace = true
nbeep-gfx.workspace = true
