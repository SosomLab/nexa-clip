[package]
name = "nclip-ctl"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Nexa Clip 컨트롤 — 렌더 기반(draw·raster)·기하·이벤트·위젯 계약·컨트롤 (앱 도메인 의존 0)"

[lints]
workspace = true

# ⚠️ 앱 도메인(nclip-core 등) 의존 금지 — 이 크레이트의 존재 이유다(docs/13 §2-2).
# 문자열(i18n)·크기 배율 같은 앱 정책은 전부 **주입**으로 받는다(controls::set_ctl_labels 등).
[dependencies]
nclip-gfx.workspace = true
