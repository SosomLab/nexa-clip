[package]
name = "nclip-store"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Nexa Clip 영속 — 암호화 세그먼트·내용 주소 blob·인덱스"

[lints]
workspace = true

[dependencies]
nclip-core.workspace = true
# ── 암호화(beep nbeep-store 원장과 동일 판 — 자체 구현 금지 부류 · docs/10 §3)
chacha20poly1305 = "0.10"  # 레코드·blob AEAD(ADR-0005 계열 · Noise와 같은 암호군)
sha2 = "0.10"              # 봉투 키 KDF · blob_id(키 있는 내용 주소)
getrandom = "0.2"          # 마스터·salt·nonce 난수(OS CSPRNG)
