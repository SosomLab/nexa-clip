# STATUS — 지금 상태 한 장

> 시간 역순. 상세는 [journal](journal/), 여기는 요약.

---

## 2026-09-05 (6차 · Linux VM) — ★ 설치본 자리에서 실기(`dev-install-linux.sh`) — 재설치 없이 갱신

배포된 v0.1.1 `.deb`를 기준선으로 삼아 **실제 설치 위치(`/usr/bin`)** 에서 실기하고, 이후 소스 수정은 릴리스 빌드 → 그 자리에 원자 교체 →
설치 `.desktop`을 **경로로 지정해** 재시작(직접 실행·`systemd-run`·`gtk-launch`는 전역 단축키 등록 실패 또는 개발 빌드 오기동 — 09-05 실측).
데이터는 `~/.config/nexa-clip`(개발 인스턴스와 별개) · 로그 `target/installed-nexa-clip.log`. 가짜 프리픽스로 전 과정 실측(교체 md5 일치 ·
단축키 등록 ✓). ⏳ **사용자 1회**: `sudo dpkg -i out/nexa-clip-0.1.1-linux-x64.deb`(대화형 인증 필요) — 그 뒤로는 스크립트 한 줄.

---

## 2026-09-05 (5차 · Linux VM) — ★ 창이 뒤에 뜨고 "준비됨" 알림 → 페이저 소스로 직접 올림(메인창·설정 창 공통)

사용자 제보 "설정 창이 뒤에 뜨고 확인 창(알림)이 먼저" = winit `focus_window()`가 `_NET_ACTIVE_WINDOW`를 소스=1(앱)로 보내 Mutter
포커스 탈취 방지에 막힌 것(붙여넣기 복원은 소스=2라 늘 통했다). `raise_x11_window`(페이저 소스)를 `bring_to_front` 공통 길목에 넣어 메인창·설정
창의 신규/재표시 전부 앞으로 오게 하고, X11 알림을 없앴다(T-41 ① 트레이 열기 동반 해소). ignored 테스트로 Mutter 존중 실증(before=B after=A).
3타깃 clippy ✓ · 테스트 501. ⏳ 사용자 실기(설정·메인 창이 앞으로 오고 알림이 안 뜨는지).

---

## 2026-09-05 (4차 · Linux VM) — ★ Linux 전체 점검 · 붙여넣기 불능 근인(포털 세션 고착) 수정 · 설정 파일 중복/664 · T-15d 완화

사용자 제보 "항목 선택해도 붙여넣기 안 됨" = 셸이 RemoteDesktop 세션을 닫은 뒤(상단 바 "화면 공유" 표시 소멸) 앱이 죽은 핸들로
`Invalid session`을 영영 반복 → **전송 실패 시 저장 토큰으로 재개설·1회 재시도**(재현 테스트 ✓ · 권한 재승인 불요). 부수: `ui.set_*`
등재 누락(설정 창 위치 복원 불능 + 파일 키 중복) · `settings.cfg` 664(패스프레이즈 평문) → 0600 · T-15d 합성/repeat 차단 · T-18g 근인 =
09-03 라벨만 필터(색인으로 해소). 자동 실기: 캡처→mac 전파 ✓ · SNI ✓ · 테마 추종 ✓ · 3타깃 clippy ✓ · 테스트 500.
⏳ 사용자 실기(Linux): ① 팝업 선택 → 붙여넣기 ② 단축키로 팝업 열었을 때 검색창 비어 있는지(T-15d) ③ 다중 행 항목 2행 단어 검색(T-18g)
④ 설정 창 위치가 재시작 뒤 복원되는지. 감사 잔여 결함 = **T-41**(트레이 열기 Wayland · XFIXES 사망 · UTF8_STRING 별칭 · UI 블로킹 …).
열린 결정 **D-80**: 포털 RemoteDesktop 세션 수명(상시 = 현행 · "화면 공유" 표시 상시 vs 붙여넣기 순간만) — 권장 상시 유지.

---

## 2026-09-05 (3차 · Windows) — ★ 최신화 뒤 빌드 실패 · CI 16회 연속 빨강(09-04 10:44~) = 3-OS `cfg` 모듈 경로 사슬 → 3타깃 복구

mac/Linux 21커밋 수신 뒤 디버그 빌드 `E0425`(`win` 모듈 `super::super::hotkeys()`). CI 이력: **`10f18a1`부터 16회 연속 실패** — Windows 세션이
mac(`mac::hotkey` 2단 → `super::` 오류)·Linux(`redundant_clone`)를 깨고, mac 5차가 mac을 고치며 Windows(`win` 1단)를 깨뜨린 사슬.
★ 수정: `win` `super::hotkeys()` 환원 + Linux `.clone()` 제거 · **3타깃 clippy `-D warnings` 로컬 통과**(타깃 std 설치돼 있음 · 링크 없이 26초) ·
fmt ✓ · 8 스위트 ✓ · 디버그 인스턴스 상주. 교훈 = `cfg` 모듈 깊이 + **CI를 본다**(T-40). ⏳ Windows 실기 4건(팝업 논리 px · None 자동 재기동 ·
이름 재소개 · 팔레트 PNG 섬네일) — 두 PC 실기(M2 판정)와 묶어서.

---

## 2026-09-05 (2차 · Linux VM) — ★ 호스트 mac과 LAN 직결 불능 = 게스트 러너 미기동 → 릴레이 None 자동 재기동

Linux 개발 PC = VMware Fusion 게스트(192.168.80.131) · 호스트 mac bridge101 192.168.80.1. 설정 창에서 릴레이 none → 핸들·암호 순으로 채우면
핸들·암호 변경이 재기동 계기가 아니라 **러너가 한 번도 서지 않았다**(비콘 소켓·identity.key 없음). 재시작으로 즉시 mac 접속 ✓(`기기 연결(LAN) — kiros33@mac`).
★ 수정: 핸들·암호 변경 시 none·켜짐·둘 다 있음이면 **800ms 디바운스 뒤 자동 재기동**(Test·재시작 불요 · 서버 릴레이는 종전 계약). 부수 발견: 비콘은
기본 경로 /24만 → 호스트 → 게스트 방향은 안 온다(**T-26 인터페이스 열거** 필요 · 지금은 mac이 다이얼 차례라 성립). 설정 저장은 이미 quiet 1s/최대 10s 스케줄러.
⏳ 사용자: 암호 지웠다 재입력 → 자동 재기동 확인 · 양쪽 승인 → 전파 실기.

---

## 2026-09-05 (mac 3차) — ★ T-40 `scripts/check-3os.sh` — push 전 3타깃 clippy(mac에서 Win·Linux 타깃 실측 ✓)

Windows 3차(CI 16회 빨강 · 사슬 ② = mac 5차 sed가 `win` 모듈까지 바꿈)를 받은 뒤 재발 방지 구현: 호스트 자동 판별 · fmt →
호스트 → 나머지 두 OS `--target` clippy `-D warnings` · 규약 16/18/CLAUDE.md 반영 · **현재 트리 3-OS green**.

---

## 2026-09-05 (2차 · macOS) — ★ 기기 이름 변경 재소개(저장 박자 · 릴레이/LAN 전 세션 · 미승인 포함 · PeerId 갱신)

`PeerCmd { Item, Hello }` + `announce_name()` — 이름 변경 1s 디바운스 뒤 살아 있는 세션 전부에 `NCH1` 재전송 · 상대는
`upsert_online`(PeerId 기준)으로 자동 갱신 + 저장 + UI 갱신(승인 무관 — 최신 이름으로 승인 판단). 와이어·서버 변경 0.
⏳ 두 PC 실기(이름 변경 → 1초 뒤 상대 목록 반영).

**★ 다음 작업 추천(09-05 정리)** — ① **두 PC 실기로 M2 판정 닫기**(21 §6 S3 승인 → S4~S7 전파·병합 → 이름 재소개 · 코드 0 ·
Windows/Linux는 `9080b57` 이후 빌드) → ② **T-26 인터페이스 열거**(비콘 /24 한 방향 결함 · beep `netif.rs` 재사용 · IPv6) →
③ **T-27 잔여**(리치 HTML 전파 + T-29b 헤더 정규화 · 파일 경로 · 덮어쓰기/되돌리기) → ④ **T-24 DeviceList 서명 승인**(ADR-0007 ·
DR-5 ① 자동 연동). 병행 소품: T-38 Linux 단축키 런타임 재등록(mac 방식) · mac 정식 알림 · 하네스 슬라이드 정리.

---

## 2026-09-05 (1차 · macOS) — ★ 수신 이미지 섬네일 불능 = 팔레트 PNG 거부 → 디코더 정규화

원격 수신 이미지(⇄ · `public.png` 한 표현)가 라벨만 — `nclip-imgdec`가 팔레트(colormap) PNG를 거부(`Indexed => None`).
Windows는 CF_DIB가 가려 줬다. `png` EXPAND|STRIP_16로 팔레트·16비트 정규화 + 회귀 테스트 · 진단 예제 `dump_item`(저장 바이트)·
`decode_probe`(격리 워커) 신설 · 실제 바이트 384×33 ✓ · 설치본 교체. 회신: 미리보기 ✓ · 섬네일 ✗ → ★ **섬네일 자가 치유**
(`pump_thumbs` — blob 참조 없는 이미지 항목을 본문에서 만들어 표시·PNG blob 영속) · "수신 표시 사라짐" = 검증 부산물(로컬 재캡처)
+ 중복 제외 로컬 우선 병합 — 결함 아님(덤프 확인). **다음 점검 = 두 PC LAN 직결(21 §6 S2 → S3~S7)**.

---

## 2026-09-04 (6차 · macOS) — ★ 붙여넣기 불능 근인 확정 · 자동 실기 3/3 · 사용자 ✅ "붙여넣기 정상"

근인 = **TCC 항목의 요구사항 고착**(최초 애드홉 cdhash — 토글 ON이어도 재서명 앱과 불일치 · 서명은 유효). 판정 도구 =
번들 신원 `open --args status` 자기 판정 + 같은 바이너리 터미널 권한 3/3 대조. 처방 박제 = `mac-grant-accessibility.sh`
(리셋→재시작→토글→검증) · 기동 시 `AXIsProcessTrustedWithOptions` 대화상자 · PKCS12 OpenSSL 3 호환 · ★ `mac-paste-e2e.sh`
(설정의 실제 단축키 · 팝업 Enter/⇧Enter/전역 평문 → TextEdit 검증). 결과: 설치본 `paste inject: ok` · **3/3**.
mac 실기 사슬 마감: 트레이·단축키·붙여넣기·PPT 서식·전파 전부 설치본에서 동작.

---

## 2026-09-04 (5차 · macOS) — ★ 설치본 실기 체계 · 단축키 부팅 순서(3-OS) · 즉시 재등록 · ★ PPT HTML · ★ PPT 자동 실기 12/12

Windows 5~17차 수신 뒤 brew 설치본에서 연속 실기 — 제보 9건 전부 근인·수정: ★ `dev-install-mac.sh`(빌드→교체→재서명→재시작) +
`mac-dev-cert.sh`(TCC는 서명 식별 → 애드혹은 교체마다 손쉬운 사용 권한 리셋 · 안정 신원 1회) · main 빌드 복구 ·
★ **단축키 부팅 순서 결함**(`tray::spawn`이 목록 전달보다 앞 → mac·Linux 빈 등록 · Windows만 가려짐 → 3-OS 수정) ·
★ **즉시 재등록**(사용자 ✅) · 평문 주입 ⌘V 통일(⇧Enter·⇧⌥X) · 팝업 **논리 px**(저장값 1374×1126 증식 발견) + `cursor_pos` ·
★ PPT 텍스트 평문 = `richtext` HTML 판별에 **`public.html` 누락** + 색 이름 + mac HTML 정제 배선 ·
★ **`mac-ppt-e2e.sh` 12/12**(AppleScript · 물리 키 코드 — 서식 텍스트 색 보존 · 글상자 2개 Object 붙여넣기).
**실측**: clippy 0 · 20 스위트 · 설치본 재기동. ⏳ 사용자: 안정 신원 1회 생성 · PPT 텍스트 재복사(색 표시) · Presentation1 슬라이드 정리.

---

## 2026-09-04 (4차 · macOS) — 승격 확정 · ★ mac 자동 왕복 검사 · ★ set/read 공용 직렬화(SIGSEGV 근절)

16차(Windows): ★ **v0.1.0 첫 릴리스 → v0.1.1 재배포**(brew 포함 · winget/choco 제외 · 5타깃 14자산 · 둘 다 전부 녹색 · brew 탭 0.1.1) · ★ **리치 2단** — Outlook Wingdings 불릿 ■ · 목록·들여쓰기·배율 · 인라인 `data:` 이미지 미리보기 · ★ **터미널 복사 색/글꼴/배경**(블록 pre · 고정폭 Mono 슬롯 `ui.font_mono` = Nerd Font 폴백) · ★ ANSI SGR(원시 로그). ★ 더블클릭/Enter·이미지로 복사 후 **새 자리 따라가기**. ★ 툴바 재배치(토글 그룹 + **감시 정지/재개** 2상 토글 벽돌/초록) · 사용자 지정 아이콘 3종(stop/play/settings). ★ 툴팁·행 hover 페이드 + **hover 의도 코얼레싱**(`HoverIntent` · 70ms · 스크롤 안정 · 세대 가드 · 재hover) → **DR-41 최소 처리 원칙** 승격. ✅ 사용자 실기(이미지 미리보기 · 1번 이동). PLEdit 리치 = 원천이 HTML/RTF를 안 올림(회귀 아님 · 21 §7 R3). ★ **메모리 상주 계층**(DR-42 · 30): 냉 본문 즉시 내림 + 30초 GC + 섬네일 PNG blob(384²) 이관·LRU 캐시 → **기동 RSS 82 → 18.7MB**. ✅ **오늘 실기 전부 통과(Windows)** — 섬네일·검색·신원 점검·설정 창 배치·메인창 기하·터미널/ANSI·메모리(21 §6~§9). ★ **전역 단축키 설정**(단축키 범주 · 팝업 Shift+Alt+C · 평문 붙여넣기 Shift+Alt+X · 보조 없음 · 캡처 오버레이 · Windows 즉시 재등록 · 21 §10 H1~H8 ⏳) · 설치본 실기 스크립트 · 편집 단축키 물리 키 판정. mac/Linux 실기 ☐.

15차(Windows): ★ 배포 파이프라인(brew·winget·choco · 검수 대기 자동 판정 · secrets/vars 준비 · Linux 배포판 비종속 zig/glibc 2.17). 드라이런 전부 녹색 ✅ · ⏳ 첫 태그 `v0.1.0`은 사용자 결정.

14차(Windows): 점 툴팁 · 팝업 = 메인창 규칙(공용 dedup) · 문서 최신화(14·21) · **위키 6페이지**. 한 PC 2인스턴스 시험 완료(사용자 확인) → 기본 인스턴스만 상주.

13차(Windows): 리치 한글 경계 패닉 수정 · 병합 열쇠 = 내용 · 점 배치/색 확정 ✓ · ★ 병합 = 기본 켬(툴바 토글 유지). 한 PC 두 인스턴스 시나리오 마감.

12차(Windows): 표시 3색 · 로컬 2줄 결함 수정(부분집합 승격) · ★ 중복 제외 툴바 토글(기본 켬) + 내용/메타 분리(1행 + 출처 합집합). ⏳ 실기.

11차(Windows): ★ 수신 항목 = 보낸 기기별 항목(합성 표현 무시 · 재수신 ×N) · 녹색 점 · 미리보기/우클릭 출처 · ★ 중복 제외 보기 스위치. ⏳ 실기.

10차(Windows): 실기 회신 — None 마감(잠금·즉시 적용·라벨) · 기기 행 버튼 축소+줄바꿈 · ★ 연결 표시 판정 통일(릴레이 ∨ LAN). ⏳ 실기.

9차(Windows): ★ 재시도 정책 통일(실패 횟수 지수 백오프 · 설정 옵션+설명) · 릴레이 **None** = LAN만 · ★ **기기별 승인**(행 버튼 · 6자리 대조) · 동기화 끔 = 전부 끔 + 잠금. ⏳ 실기.

8차(Windows): ★ **과다 트래픽 예방** — 백오프(기기별 5분·페어링 2분) · 동시 핸드셰이크 4 · 송신 합치기 · 재전송 억제 · 수신 예산 · 비콘 적응/수신 상한 · 실패 IP 냉각. 기기별 승인 버튼은 다음 단계(찬성 의견).

7차(Windows): ★ **LAN 직결**(서버 불요 — 비콘 발견 + TCP 직결 · 릴레이와 같은 세션·승인·전파 · 목록 `(LAN)`) · ★ **네트워크가 UI를 멈추지 않는다**(디코드·인코드·전송 = 워커). 실측: 한 PC 두 인스턴스 LAN 세션 ✓. ⏳ 두 PC 실기(릴레이 끊고).

6차(Windows): ★ **`--profile <이름>`** — 한 PC에서 두 인스턴스(별도 데이터·가드·제목·기기명)로 동기화 시험. **LAN 직결(서버 불요)은 미구현**(T-25/T-26) — 지금은 릴레이 경유만.

더블클릭 승격 **사용자 확정**(3차 수정 유효 — "안 되다 됨" = 에코 지연 체감). ★ **mac 자동 검사 신설**:
`cargo test -p nclip-plat mac_tests`(실 파스텔보드 왕복 3종 — 텍스트 한글 · 다중 표현 · PNG · 에코 부분집합
성질 단언) + `clip_roundtrip` mac 본편(실측 "전부 동일"). ★ 첫 실행이 **병렬 set/read SIGSEGV**를 잡음 —
상주 앱의 잠재 크래시(메인 set × 감시 read)였다 → `clip_serial()` **공용 잠금** 통일. push 판정 로그 1줄(3-OS).
**실측**: mac_tests 3/3 · clippy 0 · 20 스위트. ⏳ 관찰: "미리보기 준비 중/없음" 문자열 항목 등재 건(재현 대기).

---

## 2026-09-04 (3차 · macOS) — ★ mac 실기 회신 4건 — 쓰기 내재화 · settle · ⇄ 재전송 금지 · Carbon 단축키

5차(Windows): ★ **기호·이모지 폴백 체인**(OS별 — ✓·🎉 두부 제거 · Windows 이모지는 흑백 윤곽 · 컬러 이모지 = T-18f). mac 3차(쓰기 내재화·⇄ 재전송 금지 등) 수신 · Windows 검증 ✓.

2대 전파 실기 제보 4건 전부 근인 특정·수정: ① 더블클릭 승격 불능 = **mac 쓰기 스텁** → `NSPasteboard`
직접 게시(beep macclip + 다중 표현·뮤텍스) ② 한 복사 = 두 항목 → `watch_mac` **settle**(30ms×4 · watch_win 동일 문법)
③ 이미지 핑퐁 → ★ **수신 표식(`source_app = "⇄ 기기명"`) = 영속 플래그** — 맨 앞 항목이 ⇄면 재전송 금지
(스키마 변경 0 · 목록 우측 출처 표시 겸용 · 지문 10초 가드는 보조) ④ Ctrl+Shift+V → ★ **Carbon
`RegisterEventHotKey`**(권한 불요 · 의존 추가 0 — 선언 차용).

**실측**: clippy 0 · 테스트 20 스위트 · 재기동. ⏳ 2대 실기 재요청: 승격 · 1복사 1항목 · 핑퐁 소멸 · mac 팝업 단축키 · ⇄ 표시.

---

## 2026-09-04 (1차 · macOS) — ★ T-12e mac: 메뉴바 상주 + 연결 배지 + Dock 정책 — mac 셸 첫 가동

2차(Windows): ★ **클립보드 전파 1단** — 복사·클릭 승격(평문·PNG)이 **승인된** 연결 기기로 가고, 받는 쪽은 이력 맨 위 + 클립보드 게시(바로 Ctrl+V). 에코 지문 차단 · 파일/리치는 후속. 설정 "기기 승인" 버튼(기기당 1회). ⏳ 2대 실기.

mac 실기 "동기화가 안 된다" 진단: 러너 재기동을 소비하는 곳이 **트레이 셸뿐**인데 mac 트레이 미이식 → 구조 공백.
사용자 지시로 근본 해결: ① ★ **메뉴바 상주**(`tray.rs::mac` — beep 이식 + 최근/설정 메뉴 확장 · objc2 winit 동판)
② ★ **연결 시 아이콘 변경**(기존 녹색 점 합성 그대로 — mac 추가 코드 0) ③ ★ **`ui.dock_icon`**(끔 = Accessory —
Dock·⌘Tab 숨김·메뉴바 전용 · 기동 빌더 정책 + 즉시 반영 + 열기 `activate_front` · `dock.rs` 신설).

**실측**: clippy 3타깃 0 · 테스트 전부 통과 · **실기동 ✓** — 트레이 ok · 감시 ok · ★ 동기화 러너 자동 가동 →
릴레이 접속 ✓ · ★ **`kiros33@windows` 첫 만남 ✓**(devices.txt — 09-03 기기 목록의 mac↔Win 실전 첫 동작).
재실행 2차 ✓ — ★ 저장소 1개 복원(캡처→암호화 영속 왕복 · mac 첫 확인) · ★ kiros33@windows **재**연결(devices.txt 영속 판정).
⏳ 실기: 메뉴바 아이콘/메뉴 · 녹색 점 · Dock 토글 · Report 행 상호 표시. 후속: T-15 mac 단축키 · 정식 알림.

---

## 2026-09-03 (9~13차 · Windows) — 추천 3종 · ★ 지연 로드(기동 107ms·RSS 21MB) · ★ M2 동기화 기반 + 연결 수명 계약 + ★ 기기 목록

9차: 단일 인스턴스(T-12e4 ✅)·미리보기 리치화·붙여넣기 스택(Ditto). 10차: 속도 실측 → 병목 = 전장 적재.
11차: ★ **blob 지연 로드**(1.8s→107ms · 287→21MB) + ★ **M2 동기화 기반**(nclip-sync 신설 — beep 릴레이
스택 이식 사본·앱 격리 3종·설정 4키·셸 접속 러너·TOFU 핀·페어링 RID 등록). 서버 변경 0.
콘솔 창 제거(windows 서브시스템)·무인수=트레이·타이틀바 팝업 등 실기 수정 다수.
12차: ★ **연결 수명 계약** — Test 성공 = 자동 접속(재실행 복원) · Disconnect/정보 변경/Enable 끔 = **즉시 해제** ·
Test = 재접속(RUNNING 가드) · ★ **실행 시 자동 Test**(러너 상태 → 노트). 설정 암호 행 마감(버튼 2개 상자 위 ·
★ 생성 2단 확인(빨강·2초) · 편집 배선 Ctrl+C/X/V·우클릭(호스트 누락이었음) · 노트 위치 결함 2건 · 사용자 SVG 아이콘 3종).

13차: ★ **기기 표시 이름 + 종단 세션 + 기기 목록** — `sync.device_name` · 인사 프레임(Noise 안) · 인바운드/알려진 기기/
페어링 첫 만남 3경로 · `devices.txt` · 설정 `Report` 행("이 기기" + 만난 기기 이름·지문8·OS·상태) · 연결 중 Test 잠금.
승인(DeviceList)·전파는 다음 단.

**실측**: 20 테스트 스위트(+8) · clippy 3타깃. ⏳ 실기: ★ 2대 만남(목록에 서로 표시) · 수명 계약 · 지연 로드 회귀 · 스택/리치.

---

## 2026-09-03 (6~8차 · Windows) — 탭 두부 해소 · ★ T-18d 1단(리치 런 + 이미지로 복사)

- 탭 두부: `Font::control_advance` — 탭 = 공백 4칸 폭 · 기타 제어 = 폭 0(측정·그리기 공통 = 캐럿 일관).
- ★ **T-18d 1단**: `nclip-core::richtext` 신설(제한 HTML→줄×런 · 색/굵기/기울임 · pre 원시 개행 · 테스트 5) —
  메인·팝업 Ctrl+1 행이 원본 편집기 색 그대로 + ★ **탭 스톱 열맞춤**(4칸 격자).
  CopyQ 분석: 구문강조가 아니라 편집기가 올린 text/html을 Qt가 그릴 뿐 — 같은 재료를 자체 파서·래스터로(DR-8).
- ★ **"이미지로 복사"**(우클릭 · `render_img` 신설): 리치 런을 흰 바탕 비트맵 렌더 → PNG+CF_DIB 게시 —
  PPT/Word에 그림으로 붙는다(표시·기본 붙여넣기는 텍스트 유지).

**실측**: clippy 3타깃 · 테스트 전부 통과. ⏳ 실기: 리치 행(색·줄·탭 열) · 이미지로 복사 → PPT.
잔여(T-18d 본편): 미리보기 패널 리치화 · 배경색·표 · 다크 색 보정 · 팝업 단축키.

---

## 2026-09-03 (2~5차) — 패키지 SSOT · VTE 판정 · ★ 자동 시작 결함 · ★ 텍스트 타깃 순위 결함 → 병합

`feat/linux-clipboard-native` **main 병합(09-03)**. 사용자 실기로 잡은 결함 둘 해소:
- ★ **로그인 자동 시작 무동작** — beep 이식 가정(무인수 = 상주) ≠ clip(무인수 = 점검) → 등록 명령에 `tray` 인자 누락, **3-OS 공통**(`TRAY_ARG`).
  desktop 실행기 재현 ✓ · 로그인 실기 ⏳. 부수: 단일 인스턴스 가드 없음 → **T-12e4** 등재.
- ★ **Firefox 한글 `\uXXXX` · 터미널 `\E2\9E\9C`** — Mutter XWayland 브리지가 charset 없는 `text/plain`(GTK = ASCII 이스케이프)을
  선두에 두고, 우리는 TARGETS 순서의 첫 텍스트 타깃을 집었다 → **UTF-8 보장 순위**(`text_rank`: charset=utf-8 → UTF8_STRING → text/plain → TEXT → STRING) +
  실패 시 다음 순위. GTK3-X11 실증 ✓ · 브리지 순서는 사용자 재복사 검증 ⏳.
- 2차 [18 §9-1b](18-build-and-test.md)·[21 §1-0](21-manual-test.md) 기능별 시스템 패키지 SSOT(클립보드 = 무설치) · 3차 터미널 붙여넣기 = VTE Ctrl+V 관례(결함 아님 · **T-15c** 등재) ·
  `scripts/dev-restart.sh`. ✅ 사용자 확인: **최상위 고정 실동작**.
- ⏳ **사용자 실기 미완료 등재**: T-14i(내재화 실기 가이드 미수행) · T-15d(단축키 후 `v` 연타) · T-18g(검색 `push` 결과 없음 · 두부 글리프) · T-12e4(단일 인스턴스).

---

## 2026-09-03 (1차) — ★ T-14 본편: Linux 클립보드 내재화 — xclip 의존 소멸

`feat/linux-clipboard-native`(09-03 병합 · 롤백 태그 `rollback/pre-linux-clipboard-native`).
[29 §6](29-linux-clipboard-access.md) 설계 그대로 — `selection_x11.rs`(연결 3개: XFIXES 감시 · INCR 수신 · 서빙+INCR 송신) ·
감시 사다리 1순위 = `x11rb`(폴링·spawn 소멸) · ★ 쓰기 = **표현 전부 게시**(1단 1개 제약 해소 · 텍스트 별칭 광고) ·
에코 = 소유자 창 비교 · 도구 파이프 = 폴백 계단. 새 crate 0(x11rb `xfixes` feature만).

**실측**: 실서버 왕복(1.5MB INCR·상한 도중 집행) ✓ · ★ **xclip 전무한 PC에서 E2E ✓**(외부 게시 → 수집 →
트레이 최근·영속 seg) · `watch: ok (xwayland-x11rb)` · clippy 3타깃 · 테스트 전부 통과. ⏳ 사용자 실기 → 병합.

---

## 2026-09-02 (1·2차) — 검색 완전판 · ★ 동적 언어 · 실기 M 반영

팝업 검색 = 메인과 동일 스택(캐럿 깜밖임 · ×지우기 · 우클릭 편집 메뉴 · IME) · 최상위 고정(툴바 · 영속) ·
TextBox word-wrap(편집 시트 기본 켜짐 · Alt+Z + ★ 우상단 스위치) · 더블=단어/트리플=줄 ·
★ i18n 29키 + **동적 언어**(app.lang 부팅부터 미적용이던 결함 발견 → 즉시 반영 · 재시작 필요 = ui.font_family 하나) ·
★ 메인 ×(지우기) 후 빈 목록 잔류 수정.

**실측**: 테스트 전부 통과 · clippy 3타깃. ✅ 실기 N1·N2 통과(09-02). ★ K4 미리보기 패널 구현(3차) + 실기 P 회신 3건 수정(4차 — 휠 스냅백 · 평문 순위 `plain_of` · Object 이미지 폴백 · ⏳ 재실기 Q1~Q4). 5~15차: ★ 픽셀 스크롤·스크롤바 · ★ Ctrl+1 = CopyQ 화법(가변 행·논리 크기 64%·EMF GDI 렌더·bilinear) ·
섬네일 캐스케이드/승격 영속 · 타이틀 Clipboard Manager. ✅ **사용자 확정: "CTRL+1 보기와 스크롤 완성"**(09-02).
16~18차(팝업 완성판): 보기 모드 설정(`ui.popup_view` 기본 rich) · 크기 기억(`ui.popup_w/h`) · 타이틀바 ·
★ 핀 구획 동기화(메인과 동일 · 선택 = 최신 유지) · 휠 스냅 회귀 수정 · PPT 글상자 평문 붙여넣기 · Ctrl+2 식별 보조. ⏳ M6 i18n 잔여 건별 · K4 미리보기 패널 다음 라운드.
19차: ★ 최상위 고정 Linux — 창만 XWayland(X11) 강제(`with_x11` · 환경변수 불변) · ★ HIDDEN_KEYS 누락으로
창 상태 키 4종(최상위·미리보기·팝업 크기) 재시작 유실 발견·해소 · libxkbcommon-x11 선결 검사(미설치 = 정직 강등 + 안내).
⏳ 실기: `libxkbcommon-x11-0` 설치 후 최상위 실동작 · `wl-clipboard xclip` 설치로 감시 활성.
20차: ★ **Linux 클립보드 접근 조사·설계 완료 → [29 신설](29-linux-clipboard-access.md)** — 경쟁 9종 실증(xclip 파이프 0/9 ·
성숙 GUI 전원 직접 구현) · CopyQ 백엔드 정독(Qt 위임+방어층 · GNOME 셸 확장) · ★ EcoPaste **v1.0에서 Linux 철회** 발견(03 정정) ·
구현 설계 권장안(29 §6 — x11rb 2연결 · XFIXES · INCR 양방향 · 다중 표현). ⏳ **확정 대기 3건**(폴백 유지 · PRIMARY 범위 · 종료 시 소멸) → 답 나오면 P1 착수.

---

## 2026-09-01 (6차) — ★ 단기 개발 일괄 7건 (결정 4건 선확정)

보관 예산(기간 무제한+500MB · 휘발 토큰 제외) · 4모드 키(⇧/Ctrl/Alt+Enter + 종류별 힌트) ·
★ **한글 조합 중 검색**(beep hangul·typeahead 이식 + IME Preedit 실시간 필터) ·
S2 우클릭 메뉴·보기 3모드(Ctrl+1/2/3)·★ 평문화 편집(S4 1단) · CF_HTML 정제 1단(오프셋 재계산) ·
T-21 저장 게이트 = 구조상 충족 판정. 암호화 강화는 **보류**(사용자 확정).

**실측**: **398 테스트** · clippy 3타깃 클린. ⏳ 사용자 실기(아래 J 목록).

---

## 2026-09-01 (4·5차) — ★ 복원 5.7s→43ms · Material 아이콘 20px · 글리프 폴백 체인

- ★ **단형화 착지 발견** — AEAD 제네릭은 호출 크레이트에 컴파일된다. 원어만 올려서는 5MB/s 그대로 →
  **nclip-store를 dev opt 3** → 복원 26MB 5.74s → **43ms**. 트레이 즉시 뜩 + 첫 우클릭부터 최근(D1).
- 메인창: Material 20px 아이콘(시안 아티팩트서 사용자 선택) · hover 상태 레이어 · 툴팁(가림 수정·확대) ·
  창 위치·크기 기억(ui.win_*).
- ★ **글리프 폴백 체인**(nclip-gfx) — 시스템 기본이 항상 자동 폴백: JetBrains Mono 지정에도 한글 두부 0.
  `ui.font_family` 설정 신설(재시작 후 적용) · 트레이 우클릭 "설정" 메뉴.

**실측**: 374 테스트 · clippy 3타겟. ⏳ 사용자: I1~I5(트레이 설정 메뉴 · 폰트 지정/폴백).

---

## 2026-09-01 (2·3차) — ★ S2 메인창 1단 + 실기 후속 일곱

★ **메인창**([28](28-main-window.md) 설계 → 1단): 검색 · 세로 툴바(핀·삭제·복사·⚙) · 핀 구획 상단 ·
삭제/핀 영속 동기 · **트레이 좌클릭 = 메인창**. 후속: 트레이 최근 복원 갱신 · 목록 휠/버튼(MouseUp) ·
작업 영역 클램프 · 창 앞으로 · 목록 키 라우팅 · 빈 목록 i18n. 374 테스트 · 3타겟 클린.
⏳ 사용자 실기: 메인창 전반 + 잔여(목록 휠·버튼 · 트레이 최근 · B5·B6).

---

## 2026-09-01 (1차) — 실기 후속 셋: "기록 사라짐" = 시작 블록 · 팝업 클램프 · ★ 차단 목록 ListBox

| 보고 | 판정 · 조치 |
|---|---|
| 종료 시 기록 소실(옵션 off인데) | ★ **소실 아님** — 진단 덤프(store_dump 신설)로 전수 확인: REMOVE·wipe 0 · 14항목 생존. 실체 = debug 빌드 ChaCha20 미최적화로 복원이 수 초 블록 → dev 프로필 암호 크레이트 opt 3 |
| 우측 하단에서 팝업 잘림 | 커서가 든 모니터 안으로 전체 클램프(clamp_to_monitor) |
| 차단 페이지·제외 앱 편집성 | ★ **ListEditor 컨트롤 신설** — ListBox + ＋/－ + 행 인라인 편집 · 값 모델 `;` 불변 |

전날 잔여 통과: 휠 · 트레이 최근 ①② · 자동 시작 · hover/스플리터 · Word 설정 반영 확인.
**실측**: 374 테스트 · clippy 3타겟 클린. ⏳ 사용자 재실기(영속 1부 재검 + ListBox + 클램프).

---

## 2026-08-31 (4차) — ★ T-16 1단: 암호화 영속 — "저장소: 2개 복원"

DR-37 확정 직후 착수(`feat/store-persistence`). beep `sealed.rs` 이식 + clip 도메인 새 설계:
**append-only 이벤트 로그**(레코드마다 AEAD · 쟘린 꼬리 관용 · 열 때 압축) + **내용 주소 blob**
(키 있는 파생 id · 같은 평문 = 한 파일 · 참조 0 = 삭제) + **키 래핑 한 겹**(device.key · 불일치 = `.locked`
보관) + 이력 **id·★핀 기초**. 셸: 시작 복원 · push→add/touch/remove · `sec.clear_on_quit` wipe.

**스모크 실측**: 복사 2건 → 재시작 `저장소: 2개 복원` → 재복사 = 승격(증식 없음) → 재시작에도 2개.
디스크 평문 부재를 테스트로 박제. **368 테스트** · clippy 클린. ⏳ 사용자 실기(아래 목록).

---

## 2026-08-31 (3차) — ★ 핵심 결정 4묶음 확정 → DR-37~40 · T-16 착수 가능

T-1~T-4b 소화(전부 권장안): **DR-37**(저장 = 자체 직렬화·인덱스/blob·암호화 세그먼트) ·
**DR-38**(at-rest 기본 켜짐 · off=기기 키 유지 · ⚠️ DR-20 로컬 조항 대체) · **DR-39**(신원·페어링 D-24~29) ·
**DR-40**(Maccy 수식 키 · 검색 둘 다 · 보관 1000). T-1은 DR-28로 이미 닫혔 있었다(TODO 낡음 · 정리).
→ **다음 = T-16 영속 저장소**(beep `nbeep-store` 이식).

---

## 2026-08-31 (2차) — Windows 실기 라운드: 결함 둘 수정 + ★ "서식 유실"은 Word 설정

| 보고 | 조치 |
|---|---|
| 창·작업표시줄 아이콘이 일반 창 | `icon.rs` 공유 + 세 창에 `with_window_icon` — ✅ 통과 |
| ⇧Enter가 팝업만 재열림 | ★ 주입 Ctrl+Shift+V = **우리 자신의 단축키** + 물리 Shift 잔류 → Ctrl+V 통일 · 수식 키 중화 — ✅ 통과 |
| 팝업 붙여넣기에서 빨강 유실 | ★ **결함 아님** — 왕복 바이트 동일 실측 · Word COM 실험으로 판정: 이 PC Word "다른 프로그램에서 붙여넣기 = 서식 병합" 설정(굵게 유지·색 유실). Win+V도 동일 조건. 사용자 안내로 종결 |

통과 목록: 테마 추종 · 닫기=트레이 · 에코 승격 · `v` 유출 없음 · ⇧클릭 · 썸네일 · 트레이 재복사 승격·툴팁.
계약 재정의: "팝업 연 채 재복사"는 구조상 불가(포커스 잃으면 닫힘) → *재복사 후 재열기 = 커서 첫 줄*.

**실측**: 350 테스트 · clippy 클린. ⏳ 휠 스크롤 확인 하나 남음.

---

## 2026-08-31 (1차) — Windows 자리 최신화: ★ main이 red였다(Windows·mac test 빌드 실패)

Linux 자리 38커밋을 fast-forward → **Windows `cargo test`가 컴파일 실패**. `cargo check`(lib)는 3-OS 통과라
안 보였고, 깨지는 것은 **test cfg 빌드**뿐이다(`watch_linux` 순수부를 3-OS test에서 검증하는 구조 때문).

| # | 결함 | 조치 |
|:--:|---|---|
| ① | `wayland_probe::has_data_control()`이 linux 한정 — Windows·mac **test 빌드 E0425** | `not(linux)` 상수 거짓 짝 |
| ② | `launcher_content` dead_code(clippy `-D warnings`) — 박제 테스트 부재 | `.desktop` 계약 테스트(`tray` 인자·`StartupWMClass`·`Icon`) |

★ **CI는 이미 잡고 있었다** — run `33262111549`가 windows·macos failure인 채 방치([16 §3](16-doc-git-conventions.md) *"main은 항상 green"* 위반).
예방: 병합 전 **크로스 `clippy --all-targets` 3타깃**(링커 불요).

**실측(Windows)**: **350 테스트** · clippy 클린 · fmt 클린 · 크로스 `--all-targets` 3타깃 클린.

---

## 2026-08-30 (3차) — 실기 라운드 2: 주입 순서 · 닫기=트레이(확정) · 에코 승격 · ★ 테마 시스템 추종

| 보고 | 조치 |
|---|---|
| 첫 번만 붙음 | winit이 루프로 돌아가야 창 파괴를 flush → 주입을 **다음 바퀴**로(`PasteAfterClose`). ✅ 사용자 "텍스트·이미지 붙는다" |
| 닫기 = 종료 | ★ **사용자 확정: 창 닫아도 트레이 · Quit만 종료** → `ui.close_to_tray` 기본 켜짐으로 정정 |
| 팝업에 같은 항목 둘 | 1단 부분 게시(평문 1개)의 에코 → `History::expect_echo` = 원본 승격 |
| ★ 테마 시스템 추종 | beep `theme.rs` 이식 + `resolve_theme` — 설정 창·팝업·CSD · 포털 `SettingChanged` 실측 ✓ |

**실측**: 352 테스트 · clippy 클린. ⏳ 사용자: 팝업 중복 소멸 · 닫기→트레이 · 테마 전환 눈 확인.

---

## 2026-08-30 (2차) — 사용자 실기 셋: 포털 RemoteDesktop 주입 · ★ wl-paste 포커스 플랩 → XWayland · 톱니바퀴/두부

| 보고 | 원인 | 조치 |
|---|---|---|
| 붙여넣기 안 됨(로그는 ok) | Xwayland `-enable-ei-portal` — XTest가 앱까지 못 간다(1차 가정 **정정**) | ★ **xdg 포털 `RemoteDesktop`**(`remote_input_linux.rs`) — 첫 회 "원격 제어" 승인 → `restore_token` 영구 |
| ★ **포커스 뺏겼다 풀렸다 반복 · 타이핑 불가 · 톱니바퀴 창** | GNOME에 data-control 없음 → `wl-paste`가 **매 폴링 숨은 창으로 포커스** | `wayland_probe`(레지스트리 사실) → data-control 없으면 **XWayland `xclip`**(Mutter 양방향 동기화). `wl-paste` 스폰 0 실측 |
| 타이틀바 한글 두부 · Dock 톱니바퀴 | sctk-adwaita 자체 폰트 · 런처 `.desktop` 부재 | Linux ASCII 제목 · `install_launcher`(.desktop + 아이콘) |

**실측**: 349 테스트 · clippy 클린. ⏳ 사용자 재실기(붙여넣기 · 플랩 소멸 · 아이콘).

---

## 2026-08-30 (1차) — ★ Linux 트레이(SNI) + 전역 단축키(포털) + K-1 Linux(XTest) + 클립보드 쓰기

사용자 요청 *"1(트레이)·2(K-1) 순차 — beep 참고"*. 브랜치 `feat/linux-tray-paste`(코드 3 커밋).

| 항목 | 결과 |
|---|---|
| **트레이 T-12e Linux** | beep `sni` 이식 + **최근 항목·알림** 확장. D-Bus 실측: 워처 등록 `Id="nexa-clip"` · 메뉴 = 헤더/최근 2건/Open/Quit · `Event(101)` → **재적재 `wl-paste`=alpha one** · Quit 정상 종료 |
| **전역 단축키 T-15 Linux** | ★ **xdg 포털 `GlobalShortcuts`**(신설 `hotkey_linux.rs`) — `CreateSession` ✓ · `BindShortcuts`는 **사용자 확인 대화창** 대기(사람 몫) |
| **"열기" = 진짜 포커스** | `wlactivate` 이식 + 새 창 `with_activation_token` · app_id `nexa-clip` |
| **K-1 T-9b Linux** | x11rb `XTest` — X11: 포커스 기억/복원 · **Wayland: 팝업 닫힘 → 컴포지터가 포커스 반환 + XWayland XTest**. ★ **Xvfb 자동 왕복 ✓**(자기 창이 `Ctrl`→`V` 수신) |
| 실기가 잡은 빈 자리 | **Linux 클립보드 쓰기 미이식** → `wl-copy`/`xclip` 1단(표현 1개 · `pick_rep`) |

⏳ **사람 실기**([21 §2-5·§2-8](21-manual-test.md)): 포털 대화창 → 단축키 · 팝업 포커스 · Wayland 앱에 붙는가 · 좌클릭 열기 앞으로.

**실측**: **347 테스트** · clippy 클린(호스트 + win + mac) · 의존 +5(Linux 한정 · [10 §3](10-decision-record.md)).

---

## 2026-08-29 (4차) — Linux 남은 확인 사항 소화(X11 · Weston · 프로토콜 실측)

사용자 요청 *"리눅스에서 확인해야 할 사항 진행 — 기능 개발은 mac에서"*. 3차가 남긴 ⏳ 넷 중
**사람 없이 되는 것**을 전부 **sudo 없이** 했다(`xvfb`·`wayland-utils`·`weston` 로컬 프리픽스).

| 항목 | 결과 |
|---|---|
| **X11(`xclip`) 경로** | ✅ **XWayland 7/7 · 순수 Xvfb 7/7** + 표식 거부 — X11 세션 로그인 없이 검증하는 두 방법을 [21 §2-7](21-manual-test.md)에 |
| **다른 컴포지터** | ✅ **Weston 14.0.2** nested — 텍스트·HTML·잘라내기 정판정(헤드리스는 seat가 없어 wl-clipboard 거부) |
| ★ **프로토콜 실측** | **Mutter 50.1 · Weston 14.0.2 모두 `wlr`·`ext` data-control 없음**(`wayland-info`) — [18 §9-5](18-build-and-test.md) 표가 측정값 |
| 결함 | 빈 클립보드 `peek` 안내가 Linux에서 **거짓**(*"다른 앱이 잡고 있다"* = Windows 사정) → OS별 사실 안내 |

✅ **Nautilus 실측**(사용자 `Ctrl+C`/`Ctrl+X` — 둘 다 `Files` · 포털 전송 키 2종 발견 → 곁다리). ⏳ **남음(다른 PC)** — KWin/Sway(data-control 있는 컴포지터) · Klipper/GPaste 공존.

**실측**: 340 테스트 · clippy 클린 · Linux 실기 **Wayland 7/7 · XWayland 7/7 · Xvfb 7/7 · Weston 3/3**.

---

## 2026-08-29 (3차) — ★ Linux 실기 환경 구축 + 실기가 잡은 결함 여섯

이 자리가 Linux(Ubuntu 26.04 · GNOME/Wayland)라 1차에서 못 한 **Linux 실기**를 했다.
사용자 요청 셋(최신화·분석 / 전체 테스트 자동화·파일 포함 / 환경 구성·배포판별 문서).

- ★ **sudo 없이 환경 구축** — rustup + **`apt-get download` + `dpkg-deb -x`** 로컬 프리픽스.
  ⚠️ **`cc`가 없으면 크로스 `cargo check`도 죽는다**(빌드 스크립트가 호스트에서 링크된다) —
  [docs/18 §1](18-build-and-test.md)의 *"C 툴체인 불요"* 는 **틀렸다 · 정정함**.
- ★ **자동 실기 하네스** [`scripts/linux-watch-e2e.sh`](../scripts/linux-watch-e2e.sh) — 8유형을
  `wl-copy --type`으로 직접 주입. 사람 없이 재현된다([docs/21 §2-7](21-manual-test.md)).

| # | 결함 | 조치 |
|:--:|---|---|
| ① | `watch.rs`가 **git에서 바이너리**(raw NUL 2개 — diff·blame·병합 불가) | `\0` 이스케이프 |
| ② | ★ **빈 클립보드가 영구히 활동 주기**(500ms마다 프로세스 · DR-9 위반) | `next_idle_ticks` — 못 읽음·빈 것·같은 지문 = 전부 "변화 없음" |
| ③ | 64MB 상한이 **읽고 나서** 걸렸다 | `run_bytes` 스트리밍 — `cap+1`까지만 |
| ④ | ★ **Linux 파일 잘라내기 유실**(`x-special/gnome-copied-files` → `Object`) | 파일 표현 3종 + KDE 곁다리 · **경로 중복 제거** |
| ⑤ | `MissingTool`을 *"미구현"* 으로 안내 | `unsupported_hint` — 배포판별 설치 명령 |
| ⑥ | ★ **부분 스냅숏**(실기가 잡음) — `text/html`이 `text/plain`만 보일 때 잡혀 **같은 복사가 두 항목 + `Text` 오분류** | 백엔드 `settle` — **두 번 같게 읽힐 때까지**(120ms×4). `coalesces`는 **늘어나는 방향만** 합쳐 절반만 막혔다 |

✅ **실기 8/8** — 텍스트(한글)·HTML·RTF·PNG(256×256)·파일 1개(한글 이름)·다중 파일·
★ **잘라내기**·민감 표식. 수정 후 **케이스당 정확히 1건**.
📄 [docs/18 §9](18-build-and-test.md) 신설 — Linux 개발/실행/테스트 환경 **배포판별 차이 포함**.

**실측**: **340 테스트**(Linux) · clippy `-D warnings` 클린(호스트 + **4 크로스**) · rustfmt 클린.

---

## 2026-08-29 (2차) — mac 유형별 실기(사용자)가 결함 둘을 잡았다

사용자 훑기 9건 — 판정은 전부 정확(★ **PPT 도형 = `Object` 12표현** — GVML·SVG·PDF·TIFF ·
결함 ⑤ 수정이 mac에서도 유효). 결함 둘 수정:

| # | 결함 | 원인 · 조치 |
|:--:|---|---|
| 1 | ★ 한글 파일명이 자모로 낱낱이 | macOS 파일명 = **NFD**. 래스터라이저는 shaping이 없어 실결함 → [`compose_hangul_nfd`](../crates/nclip-core/src/capture.rs)(한글 한정 NFC · `file_names()` 표시만 · 경로는 원본 · NFC 멱등이라 3-OS 공통) |
| 2 | ★ 출처 앱이 전부 iTerm2 | `NSWorkspace.frontmostApplication`은 **런루프 없인 박제** — 제외 앱 게이트(FR-S-2) 오판 위험 → `CGWindowListCopyWindowInfo`(호출마다 창 서버 조회 · layer-0 첫 창 소유 앱 · 권한 불요) |

✅ 실기: Finder ⌘C → `출처: Finder` · `미리보기: 한글검증_v1.0.pdf`.
관찰(기록만): PPT `ole.source.0x…` 토큰이 복사마다 달라 **이력 승격 판정 저해 소지**(T-13 후속).

**실측**: **337 테스트** · clippy 클린(호스트+linux 크로스) · win-msvc check 클린.

---

## 2026-08-29 (1차) — ★ T-14e macOS 감시 + Linux 1단: 3-OS가 다 잡는다

사용자 요청("윈도우처럼 유형별 복사 테스트 · 파일 포함" + "리눅스도") — 감시 백엔드 둘 추가.
`watch`/`peek`/게이트/디바운스는 OS 중립이라 무수정으로 돈다.

- ★ **macOS**([`watch_mac`](../crates/nclip-plat/src/watch_mac.rs) · `mac-changecount-poll`) —
  `changeCount` 적응형 폴링(200ms→1s) · objc 직접 호출(DR-8) · 결함 ⑫(빈 스냅숏 미처리)
  선반영 · nspasteboard 표식 3종 fail-closed · ★ Finder **참조 URL → 경로 해석**.
  **유형별 실기 ✅**: 한글 텍스트 · RTF · PNG(치수 256×256) · **Finder ⌘C 파일**(표현 4개 ·
  파일명 미리보기) · 다중 파일.
- **Linux 1단**([`watch_linux`](../crates/nclip-plat/src/watch_linux.rs)) — `wl-paste`/`xclip`
  파이프(beep 선례 · 링크 0) + 내용 지문 폴링(500ms→2s) · `x-kde-passwordManagerHint` ·
  환경 사유(`NoDisplayServer`/**`MissingTool` 신설**) 정직 보고. ⚠️ 실기는 Linux 환경에서(⏳).
- ⚠️ 발견: AppleScript 인공 작성기는 changeCount 없이 내용을 늦게 채워 그 세대가 빈
  것으로 캐시될 수 있다(실제 앱 무관 · pb-level 폴백 배선) · `NEXA_CLIP_DIAG=1` mac 배선.

**실측**: **334 테스트** · clippy `-D warnings` 클린(호스트 + linux-gnu 크로스) ·
win-msvc check 클린. ⏳ 재점검: mac `watch` 실사용(Maccy와 병행) · Linux 실기.

---

## 2026-08-28 (10차) — Ctrl+C = 정상 종료 (오류 오인 해소)

사용자가 "자꾸 오류"로 보고한 것의 정체 = **Ctrl+C 종료를 cargo가 오류처럼 표시**
(`STATUS_CONTROL_C_EXIT` — 크래시 아님 · 재적재·붙여넣기는 정상 동작이었다).

- ★ [`nclip-plat::console`](../crates/nclip-plat/src/console.rs) — `SetConsoleCtrlHandler`로
  Ctrl+C·Ctrl+Break·콘솔 닫기·로그오프를 **셸의 정상 종료 경로**(트레이 "종료"와 동일)로
  돌린다 → 설정 flush를 거쳐 **exit 0**. cargo 오류 표시가 사라진다.
- 시작 안내에 종료 방법 명시.

---

## 2026-08-28 (9차) — 실기 피드백 4건: 팝업 다듬기 + 이미지 썸네일

아침 실기(사용자)가 트레이·설정·숨기기·단축키 팝업 **정상**을 확인하며 넷을 요청/발견:

| # | 피드백 | 조치 |
|:--:|---|---|
| 1 | ★ 단축키의 `v`가 검색창에 들어감 | 열림 직후 **150ms 문자 유예** + Ctrl 눌린 동안 문자 무시 |
| 2 | ★ 이미지 미리보기를 설정으로 | **`ui.image_preview`**(모양 · 기본 켜짐) + ★ **팝업에 실제 썸네일**(DIB=순수 변환 · PNG=격리 워커 · 비율 유지 24px) — 끄면 종전처럼 `[이미지] W×H` |
| 3 | ★ 중복 복사 시 커서만 위로 | 복사(승격 포함)가 들어오면 팝업 **선택을 맨 위로**(방금 것이 항상 첫 줄 + 선택) |
| 4 | ★ 마우스 클릭 선택 안 됨 | 팝업에 마우스 배선 — **클릭 = 선택+붙여넣기**(⇧클릭 = 평문 · Maccy 관례) · **휠 스크롤**(3행) |

**실측**: 332 테스트 · clippy `-D warnings` 클린. 설정 26항목. ⏳ 재점검: 팝업(클릭·썸네일·`v` 소멸).

---

## 2026-08-28 (8차) — T-19: 이미지 격리 디코더 (`nclip-imgdec` 이식)

beep `nbeep-imgdec`(권한 강등 격리 워커)를 크레이트째 이식 + 본체 어댑터.

- ★ **본체는 이미지 파서를 링크하지 않는다**(beep R-5) — `png`·`jpeg-decoder`는 워커에만
  (원장 기록). 워커는 입력을 다 읽은 뒤 **파싱 전에 자신을 잠근다**(Windows 완화 정책 ·
  seccomp · Seatbelt) — 파서가 뚫려도 열 수 있는 것이 없다.
- 어댑터([`nclip-plat::imgdec`](../crates/nclip-plat/src/imgdec.rs)) — 시간 상한은 부모가
  kill로 강제(3s/10s) · `NIMG` 응답을 **본체가 재검증**(오염된 자식의 크기 주장 차단).
- 셸에서도 설정 즉시 반영(게이트·상한·메뉴 개수 — 재시작 불요) 함께.
- ✅ E2E: raw 8×4 → `--encode-raw` PNG → 디코드 → **픽셀 비트 동일** ·
  ★ **lockdown 실기 확인**: `windows-mitigation(dynamic-code·image-load·win32k-lockout)`.
- 소비처(썸네일 표시)는 저장소·목록 UI(T-16·T-18 본편)에서 잇는다.

**실측**: **332 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-28 (7차) — ★ S1 퀵 팝업 1단: 단축키 → 팝업 → 골라서 붙는다

**제품의 존재 이유(K-1 왕복)가 실데이터와 처음 이어졌다** — `Ctrl+Shift+V`(전역 단축키 ·
[docs/14 §3-2] 기본값) → **대상 포커스 기억** → 커서 위치 팝업(DR-24) → ↑↓/타이핑 검색 →
`Enter` **원본** / `⇧Enter` **평문**(DR-35) → 재적재 + **포커스 복원 + `Ctrl+V` 주입**.

- 전역 단축키 = 트레이 스레드 `RegisterHotKey`(WM_HOTKEY → 프록시). **충돌은 정직하게**
  알린다(등록 실패 시 안내 · 실기에서는 등록 성공 — CopyQ 비충돌).
- 팝업 = 장식 없는 최상위 창 · demo의 S1 간략 보기 화법을 실데이터로 · 포커스 잃으면 닫기
  (Maccy 관례). ⚠️ **생성 직후 `Focused(false)` 레이스** 발견 — 받아 본 적 있을 때만 닫는다.
- `paste.auto`(기본 켜짐) 꺼짐 = 재적재까지만. 평문 없는 항목의 ⇧Enter는 정직하게 거절(DR-31).
- ✅ 자동 검증: 단축키 등록 · WM_HOTKEY → 팝업 열림(창 실존 확인) → 토글 닫힘.
  ⚠️ 검색·Enter 주입은 **잠금 화면이라 주입 불가** — 사용자 실기로([21](21-manual-test.md)).

**실측**: 326 테스트 · clippy `-D warnings` 클린.

---

## 2026-08-28 (6차) — ★ 제품 루프 1차 완성: 이력 → 트레이 메뉴 → 재적재

**"복사하면 쌓이고, 트레이에서 골라 다시 클립보드로"** — 저장소(T-16) 없이 세션 한정으로
핵심 루프가 처음 이어졌다(T-13 1단 + T-18e 1단).

- ★ **`nclip-core::history`** — 같은 내용 재복사 = **승격**(횟수 +1 · 도배 방지) ·
  연속 장면은 **교체**(D-80 `coalesces` 재사용) · 상한 = `store.max_items`.
  동일성은 정렬된 (표현, 바이트) 전체 — FNV 지문으로 거르고 바이트로 확정.
- ★ **트레이 우클릭 = 최근 N개**(`ui.tray_recent_n` · 최신 위) — 클릭하면
  **표현 전부를 클립보드로 재적재**(P-2 원본 붙여넣기 · 표준/등록 포맷 왕복).
- **`nclip-plat::clipboard`** — 쓰기 어댑터. ⚠️ **실 HWND ClipGuard**(beep 08-21 힙 오염
  실측 이식: `OpenClipboard(NULL)`은 배타가 깨진다) + 프로세스 내 직렬화.
- 게이트 공용화 — `watch` 진단과 셸이 **같은 `Gate`**(민감·제외 앱·브라우저 암호):
  막힌 것은 이력에도 안 들어간다. 에코(재적재의 되돌아옴)는 승격이 곧 처리.
- ✅ 실기: 클립보드 왕복 테스트(게시→읽기 일치 · `--ignored`) · 셸 상주. 메뉴 클릭은 눈 확인(⏳).

**실측**: **326 테스트**(+이력 6 · 왕복 1) · clippy `-D warnings` 클린.

---

## 2026-08-28 (5차) — 자동 시작 + 닫을 때 트레이로 (사용자 요청 · beep 이식)

- ★ **자동 시작**(`app.autostart` · 기본 켜짐) — beep `autostart.rs` **3-OS째 이식**(이름 4곳
  치환): Windows = HKCU `Run` · mac = LaunchAgent plist · Linux = XDG `.desktop`(전부 무권한).
  설정 토글 → **즉시 OS 등록/해제** · `tray` 시작마다 **경로 재동기화**(포터블 이동 대비) ·
  ★ **앱 밖에서 지운 등록은 존중**(재등록 안 하고 설정을 끔으로 — `boot_sync` 순수 판정).
- ★ **닫을 때 트레이로**(`ui.close_to_tray` · **기본 꺼짐** = X는 종료, beep 사용자 확정 동일) —
  `tray`가 **상주 셸**로 승격: winit 한 루프에서 트레이 좌클릭/"열기" → **설정 창**,
  닫기/Esc → 설정에 따라 종료 또는 **트레이로 숨기**(재열기 가능). 시작은 트레이만(창 없음).
  감시 카운트도 폴링에서 **프록시 이벤트**로(200ms 폴링 제거).
- 설정 25항목. 단독 `settings` 명령은 종전대로(닫기 = 종료).
- ✅ E2E: 켜짐 → HKCU `Run` 등록(경로 실측) · 끔 → 해제. 창 열고 닫기는 눈 확인(⏳).

**실측**: **321 테스트**(boot_sync·plist·desktop 계약 포함) · clippy `-D warnings` 클린.

---

## 2026-08-28 (4차) — T-12e 트레이 상주 (Windows) · `nexa-clip tray`

**24시간 상주 제품의 첫 상주 모양.** beep `tray.rs`(1,006줄)에서 **공통 타입 + Windows
모듈**을 이식했다(mac은 objc2 의존 결정 필요 · Linux(zbus)와 함께 실기 환경에서 후속).

- `nexa-clip tray` — 청록 아이콘(코드로 그림 · 애셋 없음) + 우클릭 메뉴(이름/열기/종료) +
  풍선 알림 경로. ★ **감시 통합**: 잡힌 항목 수가 **툴팁에 실시간 반영**(`update()` 실증).
- ★ 이식이 **crate 규약 충돌을 드러냈다** — 같은 Win32 함수를 다른 시그니처로 재선언하면
  `clashing_extern_declarations`(우리 설정 = 오류). `watch_win`의 *"같은 선언이어야 한다"*
  경고가 실제로 터져 **겹치는 선언을 [`win32`](../crates/nclip-plat/src/win32.rs) 한 곳으로
  통합**했다(paste·watch_win·tray가 같은 선언을 쓴다).
- 트레이 창은 **메시지 전용이 아니라 보이지 않는 일반 창** — TaskbarCreated 브로드캐스트
  (explorer 재시작 시 아이콘 재등록)를 받아야 해서다(beep 분석 그대로).
- ✅ 자동 확인: 상주 + 감시 시작. ⏳ 아이콘·메뉴·툴팁 카운트는 눈 확인([21](21-manual-test.md)).

**실측**: 316 테스트 · clippy `-D warnings` 클린.

---

## 2026-08-28 (3차) — T-14c 1단: 이미지 치수·축소 (`nclip-core::img`)

이미지 항목의 *"미리보기 준비 중"* 이 **`[이미지] 1920×1080`** 이 됐다.

- ★ 새 모듈 [`nclip-core::img`](../crates/nclip-core/src/img.rs) — 전부 순수 함수:
  `png_dimensions`(서명+IHDR만) · `dib_dimensions` · `bmp_dimensions` ·
  [`dib_to_rgba`](beep `nbeep-plat/clipboard.rs` 이식 · 테스트째) · `downscale_rgba`(**박스 평균** —
  최근접은 가는 선을 건너뛰어 썸네일이 빈 화면처럼 보인다).
- ★ **경계 원칙** — 압축 스트림 디코드(PNG/JPEG)는 여기 없다. beep 선례대로
  **격리 워커**(`nclip-imgdec` · T-19)로 간다: *"본체는 이미지 파서를 절대 링크하지 않는다"*.
  여기는 고정 오프셋 머리글 + 비압축 DIB + 우리가 만든 RGBA만.
- 상한 = 변 8192 · 16.7M픽셀(beep imgdec와 동일 — 할당 폭탄 방어).
- ✅ E2E: 모의 PNG(1920×1080) → `미리보기: [이미지] 1920×1080`.

**실측**: 테스트 +9(316) · clippy `-D warnings` 클린. 남은 것: 격리 디코드·썸네일 저장(T-19·T-16).

---

## 2026-08-28 (2차) — 차단 목록을 설정에서 직접 편집 (사용자 요청)

- ★ **`sec.conceal_urls`** — 차단할 출처 URL 접두를 `;` 구분으로 **추가/삭제/수정**.
  기본값 = 코어 기본 목록(테스트가 동기화 강제) · **비우면 기본으로 복귀**.
- ★ **`sec.exclude_apps`** — 제외 앱 이름 목록(FR-S-2 표시층 첫 구현). **토글과 무관하게**
  적힌 앱의 복사는 기록 안 함 · 대소문자 무시 **완전 일치**(부분 일치는 사고) · 기본 비어 있음.
- `watch`/`peek`가 두 목록을 읽어 적용. 설정 24항목.
- ✅ E2E: 제외 앱(msedge 소유 클립보드 → 마스킹) · 커스텀 URL(넣으면 차단 · **뺀 기본 항목은 통과**).

**실측**: **314 테스트** · clippy `-D warnings` 클린. ⏳ 설정 창 편집 실기(창 조작).

---

## 2026-08-28 (1차) — DR-36 실기: Chrome ✅ · Edge는 경로가 달랐다

사용자 실기(옵션 켬): **Chrome 암호 복사 3건 전부 마스킹 ✅** · **Edge는 그대로 보임 ❌**.

원인 — 이 Edge 버전의 암호 관리자는 `edge://wallet/`이 아니라
★ **`edge://settings/autofill/passwords/details?domain=…`** 경로였다(스크린샷 주소창으로 확정).
목록에 추가하고 실기 URL 그대로 테스트 박제 + 모의 E2E 마스킹 확인.
⚠️ DR-36이 예고한 대로 **URL 목록은 늙는다** — 실기에서 만나는 대로 채우는 운영(D-75식)이 맞았다.

**실측**: 311 테스트 · clippy 클린.
✅ **재점검 통과**(사용자): Edge도 정상 차단 — **T-21b 완료(Chrome + Edge)** · DR-36 실기 종결.

---

## 2026-08-27 (11차) — T-14g 캡처 디바운스 (D-80 닫힘)

탐색기 복사 1회가 2~4개 항목으로 쌓이던 것을 **하나로**.

- ★ [`coalesces`](../crates/nclip-core/src/capture.rs)(core 순수 함수) — 같은 앱 +
  ① 표현·**바이트까지 동일**(재게시) 또는 ② **이전 ⊂ 나중**(플러시 중간 → 완본)일 때만
  나중 것으로 교체. ⚠️ **같은 표현의 다른 내용은 안 합친다** — 합치면 복사가 사라진다.
- `watch` 수신 루프가 **500ms 창** 디바운스 — 조용해지면 찍고, 안 합쳐지는 새 복사는
  순서대로 먼저 찍는다. 유휴엔 그냥 잔다(CPU 0).
- ✅ **실기 E9**: 탐색기 복사 × 2 각각 **정확히 1항목**(이전: 쌍 + 부분 스냅숏).

**실측**: 테스트 +9(coalesces 포함) · clippy `-D warnings` 클린.

---

## 2026-08-27 (10차) — DR-36: 브라우저 암호 차단 옵션 (기본 꺼짐)

사용자 확정: Edge 암호 복사가 남는 것에 대해 **차단을 옵션으로, 기본은 꺼짐**.

- ★ **`sec.conceal_browser_pw`** 신설(보안·개인정보 · Toggle · **기본 꺼짐**) — 설정 22항목.
- 판정 = [`is_password_manager_url`](../crates/nclip-core/src/capture.rs) — `Chromium internal
  source URL` 표현의 **내용**이 `edge://wallet/` 등 내장 암호 관리자 페이지인가.
  ⚠️ 웹사이트는 흉내 못 낸다(`edge://` 스킴은 내부 페이지 전용) · 목록은 늙을 수 있다(D-75식 운영).
- `watch`/`peek`가 켜짐이면 `(브라우저 암호 관리자 복사 — 기록하지 않습니다)` 마스킹.
- ✅ **모의 E2E**: 꺼짐 → 그대로 보임 · 켜짐 → 마스킹. ~~D-79~~ → **DR-36** 확정.

**실측**: **303 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-27 (9차) — ★ 진짜 원인 확정: 빈 스냅숏의 `LAST_SEQ` 오염

*"diag를 켜면 되고 끄면 안 된다"*(사용자) — 통제 실험으로 상관관계 재현(끄면 1/3·1/4,
켜면 3/3) 후 **링버퍼 추적**(복사 순간엔 메모리 push만)으로 범인 확보:
`deliver seq=4774 **reps=0**`.

★ 탐색기는 비우기/채우기가 **별개 트랜잭션** — 그 틈을 읽으면 표현 0개인데 일련번호는
최종값이고, **채우기는 번호를 안 올린다**(지연 렌더링) → `LAST_SEQ` 오염 → dup 차단·
하트비트 모두 "처리됨" 오판 → 영영 유실. diag는 출력 지연만큼 읽기를 늦춰 우연히 통과.

**조치** — 내용 없는 스냅숏(`has_content` 거짓)은 실패로 돌려 `LAST_SEQ`를 안 건드림 →
재시도(200ms)가 채워진 뒤를 읽는다. ✅ **E8 검증: diag 끄고 탐색기 `^C` 4/4 포착**.

**실측**: 301 테스트 · clippy `-D warnings` 클린.
✅ **사용자 최종 확인**(08-27): *"이제 정확하게 잡히고 있어"* — **T-14b''' Windows 통과**.
Windows 감시(T-14b 계열)는 실기 기준 **안정** — 다음: macOS 감시(T-14e) · 썸네일(T-14c) · 트레이(T-12e) · 디바운스(T-14g).

---

## 2026-08-27 (8차) — ✅ 하트비트 실기 통과 (유실 구제 2건이 로그에)

사용자 실기 8건(`NEXA_CLIP_DIAG=1`) — **복사·잘라내기 전부 포착** ·
★ `하트비트 — 놓친 변화 감지` **2건**([4]·[8]) = 유실이 실재하고 안전망이 일한다.
**T-14b''' Windows ✅**. 이전 "복사 안 됨"은 하트비트 이전 바이너리로 추정.

로그가 보여 준 후속 2건 → **D-80**: ① 탐색기 **연속 재게시**로 항목 쌍(내용 중복 승격
T-13이 합침) ② **플러시 중간 스냅숏**([5] 3개 → [6] 16개) — 캡처 디바운스/합치기.

---

## 2026-08-27 (7차) — ★ 이벤트가 안 오는 일이 있다 · 하트비트 안전망

⑨ 수정 후에도 탐색기 복사가 **간헐적으로** 안 잡혔다(첫 회만 성공). 사용자가 PC를 넘겨줘
**직접 재현** — 실제 탐색기 `Ctrl+C`에서 클립보드는 바뀌었는데(`peek` 확인)
**`WM_CLIPBOARDUPDATE` 자체가 안 왔다**. 같은 절차가 다음엔 잡히기도 했다(간헐).

★ **조치** — **일련번호 하트비트**: 2초마다 `GetClipboardSequenceNumber`(클립보드를
열지 않는 호출 1회)만 비교, 다르면 그때 읽는다. 유휴 비용 무해(DR-9) · 이벤트 경로와
중복 없음. + **`NEXA_CLIP_DIAG=1`** 진단 스위치(수신/전달/중복/실패/하트비트).

✅ **검증** — 실제 탐색기 `^C` × 3 + 텍스트 왕복 6회 **전부 잡힘**. 잠금 1.5s 시나리오로
⑨ 재시도 경로도 실증. **재점검 요청**: `watch` 재시작 후 복사/잘라내기 반복(2초 안에 잡히면 정상).

**실측**: **301 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-27 (6차) — ★ Edge 암호 관리자는 표식을 안 붙인다

사용자 실기: `edge://wallet/passwords` 암호 복사가 **표식 없이 평문 `Text`로** 잡혔다.
우리 결함이 아니라 Edge 동작(표식이 있으면 막는 것은 5차 모의 검증 완료) —
같은 복사가 **Win+V 기록·클라우드 클립보드에도 남는다**.

★ **함의** — FR-S-1(표식 존중)만으로는 부족 → **D-79** 신설([27 §9](27-capture-cases.md)):
① 제외 앱(FR-S-2) ② `Chromium internal source URL` 내용으로 출처(`edge://wallet` 등) 판정.
전용 관리자 표식 실기는 보유 시에만(보류).

---

## 2026-08-27 (5차) — 3차 훑기(Word·탐색기)가 결함 셋을 또 · 데모 두부

사용자가 감시 2차 목록과 창 점검 4건을 실기로 넘겨줬다.

**Word ✅**(`RichText` 2건 · `Embed Source` 유지 — 4차 방향 실기 확인) · Outlook 보류(미사용).

| # | 증상 | 조치 |
|:--:|---|---|
| ⑧ | ★ 탐색기 **잘라내기**가 `Object` — `CF_HDROP`이 열거되지 않는다(지연 렌더링) | `Shell IDList Array`(PIDL 본체)를 **파일 포맷**으로 |
| ⑨ | ★ 탐색기 **복사가 아예 안 잡힘** — 비동기 플러시가 클립보드를 쥐어 275ms 초과 → **실패를 조용히 버렸다**(업데이트는 병합돼 다시 안 옴) | ★ **타이머 재시도**(200ms × 15회 ≈ 3초) |
| ⑩ | `watch`가 **민감 표식을 무시** — `concealed`를 채우고 안 읽었다 | 내용 없이 `(민감 표식 …)` **마스킹 한 줄** |

⑧·⑩은 **모의 클립보드로 자가 검증** 완료. [19] Word·[20] 잘라내기를 회귀 박제.

**창 점검(사용자 실기)**: T-9b' 포커스 복원 재검증 ✅ · T-12c3 설정 쓰기 ✅ · T-12d5 숫자 표시 ✅ ·
T-12b2 데모 ⚠️ **두부(□)** — 의도가 아니라 **이모지가 맑은 고딕에 없어서**(폴백 없음).
KS X 1001 도형 문자로 교체(`▤▦◆¶`). ★ 제품 아이콘은 글리프가 아니라 직접 그린다(T-18b).

⏳ **재점검 요청**: 탐색기 복사/잘라내기(⑨ 타이밍) · Edge 비밀번호 복사(표식 실기) · 데모 두부 소멸([21 §1-2](21-manual-test.md)).

**실측**: **301 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-27 (4차) — 실기 18건이 결함 셋을 더 잡았다 · `ClipKind::Object`

사용자가 실제 업무 자료로 `watch`를 훑었다(PPT·Excel·Edge·VS Code·Greenshot·CopyQ).

| # | 증상 | 조치 |
|:--:|---|---|
| ⑤ | ★ **PPT 도형이 `Image`** 로 분류 | ★ **`ClipKind::Object` 신설** — 그림도 글도 아닌 앱 개체 |
| ⑥ | `JFIF`·`GIF`·SVG가 **벤더로 세어짐** | 그림 포맷 목록에 추가 |
| ⑦ | ★ **표현 0개 항목**이 생김(Excel 지연 렌더링 · rdpclip 표식뿐) | `has_content` — **내용 없으면 항목이 아니다** |

★ **경계가 실제로 갈린다** — PPT 도형 `Object` · PPT 표 `RichText` · PPT 글상자 `RichText` ·
Greenshot `Image`. 같은 앱에서 온 셋을 뭉뚱그리면 **셋 다 틀린다**.

✅ **예측 적중** — 3차에서 미리 고친 Chromium 표식 건이 실기 [16](Edge 평문)으로 확인됐다.

- ★ **실기 18건을 회귀 테스트로 박제**([`tests/real_clipboard_captures.rs`](../crates/nclip-core/tests/real_clipboard_captures.rs)) — 포맷 이름 **그대로**.
- ⚠️ 반대 방향도 박았다 — Excel `Embed Source`·`Native`는 **곁다리가 아니다**(버리면 개체 붙여넣기가 깨진다).
- **`peek`** 신설 — 지금 클립보드만 한 번. `watch`와 함께 써도 된다.

**실측**: **300 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-27 (3차) — ★ 제품이 처음으로 "동작"한다 (T-14b)

```
$ nexa-clip watch
클립보드 감시: ok (win32-listener)
[3] 종류: Text · 표현 6개 · 미리보기: 한글 평문 확인
```

- ★ **Windows 클립보드 감시** — 메시지 전용 창 + 전용 스레드(`GetMessageW`가 블록 → 유휴 CPU 0).
  ⚠️ 클립보드는 한 번에 한 프로세스만 연다 → **재시도** · 핸들 포맷은 `GlobalLock` 금지.
- ★ **`nexa-clip watch`** — 복사할 때마다 무엇이 잡히는지 찍는다. **저장 경로와 같은 함수**를 쓴다.
- ★ **첫 실행에서 버그 넷** — 전부 *조용히 틀리는* 종류였다:
  ① `CF_UNICODETEXT`가 **UTF-16LE**(한글이 통째로 사라졌다) ·
  ② `DataObject` 하나가 텍스트를 리치로 · ③ **`CF_OEMTEXT` 누락** ·
  ④ *"벤더면 무조건 리치"* 가 **그림까지** 리치로.
- 설계 정정: `ClipSnapshot`이 **`RawRep`**(감시는 `blob_id`를 만들 수 없다) ·
  `capture`가 `RepInfo` 제네릭 · `Captured.keep`(인덱스) · `PreviewMissing::ThumbMissing`.

| 실기(Windows) | 결과 |
|---|:--:|
| 평문 · 색 · 파일 · 이미지 분류 | ✅ 자동 확인 |
| ⏳ **Word·Excel·Chrome·PPT 훑기** | 점검 요청([21 §1-2](21-manual-test.md)) |

**실측**: **286 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-27 (2차) — macOS Retina 배율 정정 · K-1 mac 통과

- ⚠️ 설정 창에서 **레이아웃은 2배인데 글자만 1배**였다(사용자 스크린샷).
  `RasterCtx`에 배율을 안 넘겼다 — beep은 `.with_scale(entry.scale)`를 부르고 있었다.
- ★ **테스트가 아니라 구조로** 막았다 — `RasterCtx::new(surface, font, **scale**)` 필수 인자.
  Windows(배율 1.0)에서는 우연히 맞아 보여 **한쪽 OS에서만 조용히 틀리는** 버그였다.
- ✅ **macOS K-1 통과**(사용자 실기) — 손쉬운 사용에 iTerm 추가 후 Sublime 붙여넣기.
  ⚠️ 권한 주체가 터미널이라 **배포본(.app) 통과를 뜻하지 않는다**(→ T-9d).

---

## 2026-08-27 (1차) — macOS 빌드 복구 · 설정 읽기 mac 확인

- ⚠️ **`nclip-plat`이 mac에서 한 번도 컴파일된 적이 없었다** — K-1 스파이크의 `appkit` 헬퍼 5개가
  `forbid(unsafe_op_in_unsafe_fn)` 아래서 `unsafe {}` 없이 FFI를 불렀다(E0133 × 13). 본문을 감싸 복구.
  ★ OS 분기 코드는 **그 OS에서 빌드되기 전까지 "됨"이 아니다.**
- **T-12c 설정 읽기 macOS ✅** — 기본값 `Compact·system·1000·8` / 저장본 `Rich·dark·2500·15` 반영.
  `nexa-clip settings` 창 뜸(Apple SD Gothic Neo · 21개). 창 조작이 필요한 항목은 ⏳.

**실측**: **235 테스트** · clippy `-D warnings` 클린 — **mac에서**.

---

## 2026-08-26 (14차) — ★ 캡처 파이프라인 (T-14a)

[27](27-capture-cases.md)의 규칙이 코드가 됐다 — `nclip-core::capture`.
★ **OS 호출·디코딩·파일 접근이 없는 순수 함수**라 실제 클립보드 없이 전부 테스트된다.

| 하는 일 | |
|---|---|
| `classify` | 표현 이름들 → 종류. ★ **순서가 전부**(파일 → 벤더 → HTML/RTF → 비트맵 → 텍스트) |
| `primary_index` · `thumbnail_source` | 사다리 꼭대기 · 미리보기 원본(PNG > DIBV5 > DIB) |
| `select_reps` | 용량 상한 — **필수는 안 버린다** · 버린 양을 알린다 |
| `capture` | 위를 묶어 `Captured`(종류 · 표현 · `Preview` · 버린 바이트) |

- ★ **벤더 포맷을 목록으로 알아보지 않는다** — *"아는 표준이 아니면 벤더"*.
  앱 이름 표는 영원히 늙는다. **모르는 포맷을 넣은 앱이 그걸 읽을 줄 아는 앱**이다.
- ⚠️ 그 규칙의 함정은 **곁다리**였다 — `CF_LOCALE` 하나로 맨 텍스트가 리치가 된다.
  `is_metadata_format`으로 걸러내고 테스트로 박았다.
- `ClipItem.preview`가 `String` → **`Preview`**(Text/Rich/Thumb/Files/**None(사유)**).
  ★ JPEG는 `미리보기 없음`이 아니라 **`JPEG`** 라고 적는다 — 못 그리는 것과 깨진 것을 구분한다.
- `primary`를 필드가 아니라 **파생값**으로 — `select_reps`가 표현을 버리면 인덱스가 밀린다.
- 문서의 네 케이스가 그대로 테스트 이름이 됐다(`case1_ppt_shapes` …).

**실측**: **266 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-26 (13차) — 개수 설정을 숫자로

> *"Maximum items가 최대 보관 클립보드 수라면 숫자로 직접 지정하도록 숫자로 표현해줘. 200, 1000 등등"*

- ★ **`SettingKind::Number`** 신설 — 라벨을 번역하지 않는다. **후보 문자열이 곧 화면 값**이다.
- `store.max_items` = `200·500·1000·5000·10000` + 직접 입력(**10~100000**) · 기본 **1000**
- `ui.tray_recent_n` = `5·8·10·15·20` + 직접 입력(**3~20**) · 기본 **8**
  (같은 화면에서 하나만 숫자면 읽는 사람이 두 번 생각한다 — 함께 바꿨다)
- ⚠️ 범위를 벗어나면 **경고 + 직전 값으로 원복**(기존 검증 배선 그대로).
- 글꼴 크기는 **안 바꿨다** — 거기선 *작게/보통/크게* 가 사용자가 원하는 표현이다.

- ⚠️ **점검 화면이 거짓말을 하고 있었다** — `default view`가 `ViewMode::default()`를 찍어
  사용자가 뭘 바꾸든 영원히 `Compact`였다. **저장본을 읽도록** 고쳤다(영속을 터미널에서 검증 가능).

**실기 확인**(빌드 후 실제 실행)

```
settings        : ...\target\debug\data\settings.cfg (저장본 사용)
default view    : Rich (rich)
theme           : dark
max items       : 2500        ← 후보에 없는 직접 입력값이 살아 있다
tray recent     : 15
```

| 항목 | Windows | macOS |
|---|:--:|:--:|
| 설정 **읽기**(저장본 반영) | ✅ **자동 확인** | ✅ **자동 확인**(15차) |
| 설정 **쓰기**(창에서 값 변경) | ⏳ **점검 요청**(창 조작 필요) | ⏳ 점검 요청 |
| 개수 설정 숫자 표시 | ⏳ 점검 요청 | ⏳ 점검 요청 |

**실측**: **235 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-26 (12차) — 설정이 저장된다 · hover가 서서히 밝아진다 · 붙여넣기 모드 4개

```
cargo run -p nexa-clip -- settings
설정 파일: …\data\settings.cfg      ← ★ 값을 바꾸고 닫았다 열면 그대로 있다
```

- ★ **설정 영속**(T-12c2) — `nexa-conf`를 **이름 그대로** 가져왔다([DR-32](10-decision-record.md)).
  변경은 플래그만 · 쓰기는 미룬다(조용해진 뒤 1s · 늦어도 10s) · **종료 직전 flush** · **모르는 키 보존**.
- ★ **hover 페이드**(사용자 요청 · [DR-34](10-decision-record.md)) — 1000ms 인 / 220ms 아웃.
  확인해 보니 **설정 화면에 hover가 아예 없었다**. `Fade`·`HoverFade`·`hover_alpha` 공용 부품으로 만들고
  트리 행·버튼·콤보에 적용. 스플리터도 같은 부품으로.
- ★ **[27 케이스별 캡처·표시](27-capture-cases.md)** — PPT 도형·색 텍스트·이미지·파일.
  ⚠️ **PPT 도형에도 `CF_DIB`가 있다** → 종류 판정 순서가 전부. ★ **미리보기는 Office가 준 비트맵을 쓴다**.
- ★ **붙여넣기 모드 4개**([DR-35](10-decision-record.md) · 사용자 확정) — 원본 / 평문 / **객체로** / **경로만**.
  설정이 아니라 **단축키**다. ★ 넷 다 *"고르는 게 아니라 빼는"* 같은 기계.
  `PasteAs::applicable(kind)`가 힌트 줄·메뉴·키의 **단일 원천**.
- ★ **[26 §4-4](26-file-content-sharing.md)** — 원격 파일 상한은 **제안할 때** 검사(전송 중이 아니라).
  **실패할 약속을 하지 않는다**.

**실측**: **226 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-26 (11차) — 디자인 토큰 · 스플리터 글로우 · 빌드/테스트 문서

- ★ **`nclip-ctl::tokens`** — 간격 4그리드 · 타입 스케일 · **상태 레이어**(색을 새로 만들지 않고 알파로 덮는다) · 엘리베이션 2겹 · 모션. **팝업 120ms 상한을 컴파일 타임 assert로** 못 박았다.
- ★ **스플리터 글로우** — 1000ms 페이드인 / 220ms 페이드아웃. 2차 요청으로 **세로 균일**하게 정정(색만 보간, 핸들 제거) → [25 §3-7](25-design-system.md) M-1~M-3.
- ★ **[18 빌드·테스트](18-build-and-test.md)** 신설 — 매번 돌리는 네 줄 · **테스트가 왜 있는가** · 교차 검사 · CI · 흔한 실패.
- ⚠️ **K-1 결과 정정** — 사용자 붙여넣기 성공은 **주입까지만** 증명한다. `[3] 포커스 탈취: 실패` = **복원 경로 미실행**. 임시 최상위 창 탈취를 추가하고 [21](21-manual-test.md)을 T-9b/T-9b'로 분리.

| 항목 | Windows | macOS |
|---|:--:|:--:|
| K-1 **키 주입** | ✅ 실사용 통과 | ⏳ 점검 요청 |
| K-1 **포커스 복원** | ⚠️ **미검증**(재확인 필요) | ⏳ 점검 요청 |
| 설정 창(+스플리터 글로우) | ⏳ 점검 요청 | ⏳ 점검 요청 |
| 렌더 데모(+알파) | ⏳ 점검 요청 | ⏳ 점검 요청 |

**실측**: **195 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-26 (10차) — ★ 설정 창이 실제로 뜬다 (+스플리터)

```
cargo run -p nexa-clip -- settings
폰트: 맑은 고딕 · 설정 항목 21개
```

**이식한 프레임워크 2,000줄이 실제로 돈다.** 창·입력 배선만 했고 화면 코드는 이식본 그대로다.
★ 스플리터는 **드래그 로직이 이미 있었고** hover 하이라이트 + OS 커서 번역만 없었다.

| 항목 | Windows | macOS |
|---|:--:|:--:|
| **설정 창(+스플리터)** | ⏳ 점검 요청 | ⏳ 점검 요청 |
| 렌더 데모(+알파) | ⏳ 점검 요청 | ⏳ 점검 요청 |
| K-1 포커스/주입 | ✅ | ⏳ 점검 요청 |

**실측**: 186 테스트 · clippy `-D warnings` 클린.

---

## 2026-08-26 (9차) — 실패 통보 정책 · 진단 로그

- **DR-31** — 실패는 **모달이 아니라 상태 전이 + 로그**. ★ 상태를 **미리** 갱신해 *붙여넣기를 시도하기 전에* 목록이 먼저 바뀐다.
- ★ **"불가"가 "아무것도 안 줌"이 되지 않게** 했다 — 파일로 못 주면 **경로 텍스트**를 준다(적응형 폴백 재사용).
- `nclip-core::diag` 구현 — ★ **`cause`+`action` 쌍** · 버려진 줄 수 카운트 · **항목 내용 미기록**(비밀번호가 지나가는 통로다).

**실측**: **186 테스트** · clippy `-D warnings` 클린.

---

## 2026-08-26 (8차) — 공유 정책 정밀화 · 파일 내용 공유 설계

- **DR-29** — 범위와 승인을 분리. ★ **신뢰는 도달 허가일 뿐 수신 동의가 아니다** → 신뢰해도 기본 수동 승인, 자동 승인은 **사람 단위**. **재전파는 수신측 결정**(보내는 쪽이 정하면 A가 B의 기기 구성을 좌우한다).
- **DR-30** 📐 — 파일 내용 공유 **설계만**. 붙여넣는 순간 원본에서 **지연 수신**. ★ `nexa-dir2` X-42가 **받는 쪽**을 이미 구현해 **함정 4개가 이미 알려져 있다**(`lindex`·packed 구조체·워커화·이름 위장).
- ⚠️ 급소 = **원본 오프라인이면 붙여넣기가 조용히 실패**한다 → 목록 배지 + 사전 캐시 + 우리 토스트.

---

## 2026-08-26 (7차) — 설정 이식 · 알파 · 디자인 시스템

**한 줄**: ★ **재사용 예측이 그대로 맞았다** — 설정 화면 프레임워크 2,000줄을 **화면 코드 무수정**으로 이식하고 `registry()`만 갈아끼웠다.

| 실측 | 값 |
|---|---|
| 이식된 프레임워크 | **2,000줄**(beep `settings.rs`) |
| 우리 레지스트리 | **21항목**(구현된 기능만 — DR-26) |
| 테스트 | **181** · clippy `-D warnings` 클린 |

**사용자 확정 5건**: DR-24(커서 팝업+평문 붙여넣기 **필수**) · DR-25(알파 L1) · **DR-26**(점증 개발·저빈도 배제) · **DR-27**(Material+macOS) · **DR-28**(내 기기는 승인 없이 즉시 반영 → **D-20 닫힘**)

**★ CopyQ 연구로 내 판단 3건이 뒤집혔다**([24 §4-1](24-reference-settings-study.md)) — 창 투명(포커스별 분리) · 트레이 이미지 미리보기 · 활성화 후 동작 4분해.

**beep 반영 완료**(승인) — `docs/44` + `rid_for` 주석. ★ **서버 재배포 불필요**(서버는 RID를 계산하지 않는다).

---

## 2026-08-26 (6차) — 앱 격리 · beep 전달 원장

- **DR-23** — 릴레이 탐색은 **같은 앱끼리만**. RID 도메인 분리(`nclip-rid-v1`) + Noise prologue(`nexa-clip/1`). ★ **판정 로직이 아니라 구조**라 잊거나 우회할 수 없고 **서버 변경 0**.
- 🔴 **[22 beep 전달 원장](22-upstream-beep-liaison.md)** 신설 — 서버·와이어 변경은 beep의 결정 영역이다. **모든 변경에서 3줄 체크리스트**(CLAUDE.md·메모리 등록).

🔴 **사용자 확인 대기** — beep에 전달할 3건: **I-1**(도메인 분리 규약 · 가장 시급) · I-4(glare 타이브레이크) · I-5(relay 결합 방식). beep 저장소 수정은 승인이 필요합니다.

---

## 2026-08-26 (5차) — 창·렌더 데모 · 클립보드 보내기

**한 줄**: ★ **화면이 실제로 그려진다.** 설계 문서의 S1 레이아웃이 픽셀로 나왔다.

```
cargo run -p nexa-clip -- demo    # 1/2/3 보기 · T 테마 · Esc 종료
폰트: 맑은 고딕
```

| 항목 | Windows | macOS |
|---|:--:|:--:|
| K-1 포커스 복원 + 키 주입 | ✅ | ⏳ 점검 요청 |
| **창·렌더 데모** | ⏳ **점검 요청**(실행 확인 · 눈 확인 필요) | ⏳ 점검 요청 |

- 경로: winit → softbuffer → `nclip_gfx::Surface` → `RasterCtx`. 색이 `0x00RRGGBB`로 그대로 맞는다.
- ★ **일반 보기만 행 높이 가변** — 가변 높이 가상화가 필요한 이유가 눈으로 보인다.
- 외부 의존 3개 추가(`memmap2`·`winit`·`softbuffer`) — DR-8 예외이므로 **각각 근거를 원장에** 적었다.
- **DR-22** 클립보드 보내기 확정 — 제안 → 승인 → 등록 · **단일 요청 = 단일 승인** · 수신자 신뢰 목록이 관문.

**실측**: 173 테스트 · clippy `-D warnings` 클린.

---

## 2026-08-26 (4차) — K-1 스파이크 · OS별 점검 관리

**한 줄**: 제품의 존재 이유가 걸린 **포커스 왕복을 창보다 먼저** 검증했다.

| 항목 | Windows | macOS |
|---|:--:|:--:|
| **K-1** 포커스 복원 + 키 주입 | ✅ `paste inject : ok` | ⏳ **점검 요청**(코드는 있음) |

- `nexa-clip spike-paste` — 창 없이 왕복 전체를 검증하는 스파이크. 절차는 [21 실기 점검표](21-manual-test.md).
- ★ **`PasteCapability` 3갈래** — `Full` / **`NeedsPermission`**(권한만 켜면 됨) / `ClipboardOnly`. *"안 됨"* 과 *"켜면 됨"* 을 구분한다.
- ⚠️ **macOS가 미검증 1순위** — objc 런타임을 직접 호출한 코드가 실기에서 처음 돈다.

**사용자 확정 3건**: **DR-19**(동기화 = 핵심 기능 · FR-Y P2→P1) · **DR-20**(보안 투자 = 네트워크 ≫ 로컬 · **D-9 닫힘**) · **DR-21**(i18n 영한중일 4개).

**실측**: 167 테스트 · clippy `-D warnings` 클린 · fmt 클린.

---

## 2026-08-26 (3차) — `nclip-*` 명칭 통일(포크) · 라이브러리화 지침

**한 줄**: 가져온 코드를 **우리 이름으로 흡수**했다(DR-17). 기능은 그대로, **162 테스트 그대로 통과**.

- ⛔ **DR-15 폐기** — vendor 원본명 유지는 *"U-1을 지키려고 이름 일관성을 내준"* 뒤집힌 판단이었다.
- ✅ **DR-17** — `nclip-gfx`·`nclip-ctl`로 포크 흡수. `crates/vendor/` 층위 · `VENDOR.lock` · `check-vendor.py` · CI U-1 게이트 · gitattributes 규칙 **함께 폐지**.
- ✅ **DR-18** — ★ **다음 유사 프로젝트는 공통을 먼저 라이브러리로 분리한 뒤 시작한다**(사용자 지침).

⚠️ **대가**: upstream 분기 — beep 개선을 자동으로 못 받는다. 근본 해법이 DR-18이다.

---

## 2026-08-26 (2차) — ★ 코드 착수 · 아이콘

**한 줄**: 설계 단계를 끝내고 **코드가 돌기 시작했다**. 슬라이스 1~2단계 완료.

| 실측 | 값 |
|---|---|
| 워크스페이스 | **8 크레이트**(vendor 2 + 우리 6) |
| 테스트 | ★ **162 통과**(vendor 136 + 우리 26) |
| clippy `-D warnings` | 클린 |
| `cargo fmt --check` | 클린 |
| 외부 의존 | `ab_glyph` 계열 4개(vendor가 이미 쓰던 것 · 퍼미시브) |

**핵심 결정 2건이 착수하며 확정됐다**

- **DR-15** — ★ **vendor는 원본 크레이트 이름을 유지한다**(`nbeep-gfx`·`nbeep-ctl`). 이름을 바꾸면 vendor 내부 `use`를 고쳐야 해 **U-1이 성립하지 않는다**. 워크스페이스가 경로만 잡아 주면 **한 글자도 안 고치고** 컴파일된다.
- **DR-16** — i18n을 `nclip-core`에 beep와 **동형으로** 배치(`Lang`/`Msg`/`tr`/`current_lang`) → 이식 코드는 `use` 경로만 바뀐다.

**U-1을 문서에서 검증으로 옮겼다** — `crates/vendor/VENDOR.lock`(sha256 32개 + 출처 리비전) + `tools/check-vendor.py` + CI 게이트. 규칙을 문서에만 두면 언젠가 깨진다.

**아이콘** — 계열 골격(풀블리드 라운드 스퀘어·그라디언트·흰 전경·accent 모티프)은 유지하고 **클립보드 + 히스토리 스택**으로 특징을 냈다. 색은 **청록**(beep 파랑 / dir2 초록과 구분).

**다음** — ★ **T-9b(K-1 스파이크: 포커스 복원 + 키 주입)** 와 **T-12d(설정 화면 이식)**. 전자는 제품의 존재 이유가 걸려 있고, 후자는 3단계에서 "설정이 열리는 앱"이 된다.

---

## 2026-08-26 (1차) — 레이아웃 재확정 · 참조 제품 해부 · **구현 설계서**

**한 줄**: 사용자 제공 실물(Maccy·CopyQ)로 설계를 검증·정정하고, **코드 직전에 읽을 한 장**([20 구현 설계서](20-implementation-spec.md))을 만들었다.

- ✅ [20 구현 설계서](20-implementation-spec.md) — ★ **각 기능의 구현 방법**(OS별 API·알고리즘·재사용 표기) · 크레이트 배치 · **수직 슬라이스 11단계** · 기능별 검증
- ✅ [17 참조 제품 UI 해부](17-reference-ui-teardown.md) — Maccy 팝업 · CopyQ 메인창/트레이
- ✅ [14 설정 레지스트리](14-settings-registry.md) — Maccy 설정 전수 → 우리 `registry()` 명세(카테고리 11)
- ✅ **DR-13**(화면 캡처 범위 밖) · **DR-14**(세로 밀도 우선 레이아웃) 확정
- ✅ 확정/대기 결정 번호 계열 분리 — **DR-n**(확정) / **DP-n**(권장 대기)

**정정 3건**

| 무엇 | 초판 | 정정 |
|---|---|---|
| ★ **번호 단축키** | `1`~`9` 맨숫자 | **`Ctrl/⌘+1`~`9`** — 검색 필드가 포커스라 맨숫자는 **동작하지 않는다** |
| ★ **Maccy 파일 캡처** | *"없음"* | ★ **있다**(Storage 탭 `Save: Files`) — 실제 우위는 **원본 포맷 보존** |
| **메인창 툴바** | 상단 가로 | ★ **좌측 세로**(DR-14) — 크롬 4줄 → 1~2줄, 항목 2~3개 더 |

**현재 결정 현황**: 확정 **DR 14건** · 권장 대기 **DP 4건** · 열린 결정 **46건**

---

## 2026-08-25 (3차) — beep UI 재사용 계획 확정

**한 줄**: 사용자 방침(*"beep의 골격과 설정화면을 되도록 그대로 써서 빠르게"*)을 **실코드로 검증**했다 — ★ **재사용 가능 ≈ 21,600 LOC**.

| 실측 근거 | 내용 |
|---|---|
| `nbeep-ctl/Cargo.toml` | ★ **의존이 `nbeep-gfx` 하나뿐** — *"앱 도메인 의존 금지가 이 크레이트의 존재 이유"* 라고 주석에 명시. **11,214 LOC 무수정 이식 가능** |
| `settings.rs` 헤더 | ★ **`Entry` 레지스트리 단일 원천** — 렌더·검색이 같은 원천을 읽는다. **`registry()`만 교체하면 3,510 LOC가 그대로 산다** |
| `controls/pulldown.rs:56` · `toolbar.rs:129` · `tree.rs:502` | ★ **`MenuBar`·`Toolbar`·`TreeGrid`가 이미 있다** → [DR-12](10-decision-record.md) 메인창이 기존 컨트롤로 조립된다 |
| `nbeep-plat/clipboard.rs`(550) · `tray.rs`(1,076) | 클립보드 어댑터·3-OS 트레이 이식 대상 |

**바뀐 판단**: `nexa-dir2` `grid`/`columns` 이식이 **불필요**해졌다(beep `TreeGrid`가 덮는다) → D-36.

**새 규율(U-1)**: ★ **복사한 파일은 고치지 않는다.** 확장은 우리 크레이트에서만 — *"복사해 놓고 조금씩 고치기"* 가 가장 나쁜 결말이라 **파일 단위로 경계를 강제**한다.

**착수 순서가 바뀌었다** — UI를 먼저 세워 눈에 보이게 하고 `plat`을 나중에: 복사 → i18n → ★ **설정 화면**(3단계에서 이미 "설정이 열리는 앱") → 트레이 → 도메인 → **클립보드 감시**(최대 난관) → 메인창 → 퀵 팝업 → **전역 단축키·키 주입**.

---

## 2026-08-25 (2차) — 비전·로드맵 + Rich Text·화면 구성 확정

**한 줄**: **무엇을 만드는가**(FR)에 더해 **왜 좋아야 하는가**(편의성 8)와 **어디까지 언제**(기능별 목표 매트릭스)가 붙었다. 사용자 확정 3건으로 DR이 12개가 됐다.

- ✅ [00 비전](00-vision.md) — ★ **핵심 편의성 8**(전부 측정 가능) · 2026 흐름 11건과 우리 입장 · 성공 판정("Maccy·CopyQ를 지우는가")
- ✅ [02 로드맵](02-roadmap.md) — ★ **기능별 진행 목표 매트릭스**(10영역 × M1~M4) · 단계별 수용 기준 · 의존 순서 · 리스크 K-1~6
- ✅ [12 클립보드 포맷](12-clipboard-formats.md) — **Word/PPT Rich Text + 평문 붙여넣기**. FR-C-5·C-6 **P0 승격**, FR-P-9~12 신설
- ✅ [04](04-feature-scope-and-screens.md) 화면 재설계 — 트레이 최근 8개 · 메인창(메뉴바+툴바+검색바+목록) · **보기 3모드**
- ✅ **DR-11**(Rich Text) · **DR-12**(화면 구성) 확정 추가 → DR 12건 · 열린 결정 **34건**

**이번에 바뀐 판단 2건**

| 항목 | 이전 | 지금 |
|---|---|---|
| **OCR(D-7)** | 범위 밖 | 🔴 **채택 쪽으로 기움** — PowerToys가 **로컬 OCR**로 한다 = 외부 전송 없음 = 로컬 전용 원칙과 충돌 안 함 |
| **Rich Text 범위** | HTML/RTF 보존(P1) | ★ **원본 포맷 이름째 보존(P0)** — 안 하면 PPT 도형이 그림으로 떨어진다 |

**새로 생긴 리스크**: K-3(Office 원본 포맷이 크고 버전마다 다름) · V-4(리치텍스트 렌더러가 공격면이 될 수 있음 → 제한 렌더러 FR-U-16)

---

## 2026-08-25 (1차) — 설계·조사 완료 · **코드 미착수**

**한 줄**: 경쟁 조사부터 신원·페어링 설계까지 **지식 문서 7건**을 세웠고, 이제 **병목은 코드가 아니라 결정**이다.

### 지금 걸려 있는 것

★ **열린 결정 29건 중 11건이 "권장안 제출 · 사용자 확정만 남음"** 상태다([10 §2](10-decision-record.md#2-열린-결정-d--색인)).
그중 **넷이 나머지를 좌우한다**:

| # | 결정 | 권장 | 왜 먼저인가 |
|:--:|---|---|---|
| **D-20** | 자동 덮어쓰기 기본값 | ①기록만 기본 + ②·③ 설정 | 사용자 요구는 ②에 가까운데 **상대 PC 작업 파괴 위험**이 실재. W-1(원본 보존)+W-3(되돌리기)를 붙이면 ②도 안전 |
| **D-9** | 디스크 at-rest 암호화 | **기본 켜짐** | 평문이면 **현재 쓰는 CopyQ보다 후퇴**하고 차별점 ②가 사라진다 |
| **D-1** | 저장 구조 | **자체 파일 직렬화** | 크레이트 구조·검색 방식이 여기서 갈린다 |
| **D-24** | 랑데부 파생 재료 | **핸들 + 패스프레이즈** | 신원·페어링 UX 전체가 여기에 달림 |

### 완료 (08-25)

- ✅ 라이선스 구성 — `nexa-beep`와 동일(PolyForm NC 1.0.0 + 한글본 + `.gitattributes`). 프로젝트명·URL 4곳만 치환
- ✅ [03 경쟁 조사](03-competitive-landscape.md) — OS별 24종 + 크로스플랫폼 6종. **사실 2건 정정**(CopyQ 암호화 존재 · Maccy 2.x 이미지 지원)
- ✅ [04 기능·화면](04-feature-scope-and-screens.md) — FR 8분류 · 화면 S1~S8 · 컨트롤 매핑 · 크레이트 초안
- ✅ [05 다중 기기](05-multi-device-sharing.md) · [07 랑데부](07-device-rendezvous.md) · [08 자동 전파](08-clipboard-propagation.md) · [09 신원·페어링](09-identity-and-pairing.md)
- ✅ [06 저장 설계](06-storage-design.md) — D-1 답(파일 직렬화 + 3규칙)
- ✅ 문서 골격 — CLAUDE.md · README · STATUS/DEVLOG/journal/MILESTONES/TODO/BRANCHES · [10 DR](10-decision-record.md) · [16 규약](16-doc-git-conventions.md)

### 이번에 확인된 사실 (재조사 금지 — 실코드 근거)

| 사실 | 근거 |
|---|---|
| ★ `nexa-beepd`의 RID 맵이 **`HashMap<Rid, ConnId>` = 1 RID 1 연결** → **공유 URID 안이 깨진다** | `crates/nexa-beepd/src/lib.rs:127,459` |
| ★ 릴레이는 **"버퍼가 아니라 파이프"** — 양쪽 동시 접속일 때만 흐른다 → **오프라인 큐 없음** | `crates/nexa-beepd/src/lib.rs:12` |
| `nbeep-relay` 1,769 LOC · `nexa-beepd` 951 LOC — **릴레이 MVP 구현 완료** | 실측 `wc -l` |
| beep roster(`Announce`/`PeerUp`)는 **PeerId 공개키 원본을 노출** → 다중 기기용으로 쓰면 안 됨 | `crates/nbeep-relay/src/lib.rs` |
| beep에서 **컨텐츠 서버(모드 ②)·다중 기기(ADR-0007) 모두 미구현** | `docs/32`(Proposed) · `docs/20`(v2) |

### 다음

1. 위 4건 결정 확정 → [10 §1](10-decision-record.md#1-확정-결정-dr) DR 표로 이동
2. ADR 승격(D-1 · D-24 · D-20)
3. 요구사항 문서 + 스택 ADR(예산 수치)
4. 코드 착수 — 워크스페이스 + `nclip-core` + `nclip-plat` 수직 슬라이스
