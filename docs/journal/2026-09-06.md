# 2026-09-06

> 시간 역순 — 최신이 위.

## 2차(mac) — 재점검(변동 0) · beep User 검증 결함 보고 = beep 몫으로 분리 (사용자 요청 09-06)

**요청**: "저장소 최신화 및 변경사항 분석" → "진행사항 최신화 · commit · push"(2회차).

| 축 | 내용 |
|---|---|
| 재점검 | clip `cd56634` = origin(0/0 · 트리 clean) · `nexa-beep` `0141ac5` = origin · `nexa-dir2` `3a68f90` = origin → **1차 이후 세 저장소 모두 변동 0** |
| beep 결함 보고(스크린숏 3장) | ① mac: User 페이지 `super` / `abcd-abcd-abcd` Verify → **ID 62e252eb** ② Windows: 같은 값으로 Verify → **ID 89ddf617**(다름) ③ Windows Server 페이지: Verify 뒤 연결 테스트가 *"Not verified — press [Test]"* 로 돌아감. 릴레이(`beepd.sosomlab.com:47300` Managed)로 연결된 상태에서 **두 기기가 같은 사용자로 인증되지 않음** |
| 판정 | **beep(ADR-0015 S1~S2 · 형제 발견·UserKey 동기·Succession) 결함 — clip 코드·문서 변경 없음.** 사용자 결정: 이 세션(clip)에서는 다루지 않는다 → beep 세션으로 |
| clip에 남기는 이유 | **T-45 판단 근거** — beep의 "같은 handle·passphrase = 같은 사용자"가 아직 기기 간 수렴하지 않는 것을 실기로 관측했다(ADR-0015 개정안은 UserId = H(무작위 UserKey pub)라 형제가 만나 병합되기 전엔 기기마다 다른 ID가 정상일 수 있음 — 그런데 릴레이 연결 중에도 수렴 안 함). clip이 XXpsk3/UserKey 방식을 흡수할지는 beep 쪽 수렴이 실증된 뒤 판단 |

**실측**: 변경 0 · 기록만(journal · DEVLOG · STATUS 한 줄 · T-45 메모).

## 1차(mac) — 저장소 최신화 · 변경 분석 · ★ beep 전달문 32 처리 결과를 원장 22에 반영 (사용자 요청 09-06)

**요청**: "저장소 최신화 및 변경사항 분석" → "내용 정리 후 진행사항 최신화 · commit · main 병합 · push".

| 축 | 내용 |
|---|---|
| clip 본체 | `origin/main` = 로컬 `0bcaccf`(behind/ahead 0/0) · 태그 v0.1.2 최신 · CI 최근 5 run 전부 success(마지막 = 09-06 14:22 마감 커밋) · 작업 트리 clean → **들어올 변경 없음** |
| 참조 `nexa-dir2` | **80커밋 뒤처짐 → ff-pull**(`88cec03` 08-23 → `3a68f90` 09-04 · 127파일 +11529/-544). clip 컨트롤 원천(`nexa-gui`) 변경은 `widgets/rows.rs` **오버레이 스크롤바**(세로+가로 · 반투명·페이드·호버·드래그 · 축별 독립 표시 `0d111f0`·`b47fca4` · +327) 1건 — 나머지는 dir2 앱 몫(정규 점검 X-51~57 · 아카이브 · winget 패키징) → T-46 |
| 참조 `nexa-beep` | 로컬 = 원격(0/0). **09-06 `feat/userid-handle` 28커밋 병합(`1cfb9ff`) — ADR-0015 UserId 사용자 관리 S0~S2**(Handle+Passphrase · Noise **XXpsk3** · UserHint · 릴레이 페어 RID · 세션 내 증명 · Succession 사슬). 09-05 이후 36커밋 |
| ★ 와이어 판정 | `nbeep-relay/src/lib.rs` +104/-21: `connect_via_rids_first` · `hs_initiate(psk: Option<&[u8;32]>)` · `accept_any` → `via_psk` — **선택적 PSK 경로 추가**뿐. `C2s`/`S2c` 메시지 · kind · 상수 · 핸드셰이크 순서 · `"nbeep-rid-v1"` · prologue **무변경**(diff에 enum/const 변경 0 · PSK는 Noise 패턴 내부라 서버 통과) · beepd 태그 beepd-v0.2.5 그대로 → **`nclip-sync` 사본 동기 불요 · 서버 재배포 불요** |
| ★ 전달문 32 처리됨 | beep `docs/44 §7`(09-05)이 **전달문을 수신·처리**해 두었다: **A-1** 0600 · **A-2** known 우선 = **이식 `92c92ab`**(beep 자체 `trust.seg`·`keys.seg` 등 7경로로 확대) · **B-1** 도메인/prologue = **이미 반영 `78a4035`**(08-26 · prologue는 beep 미사용 · 채택은 X-11과 묶어 major) · **B-2** glare = **X-11 사용자 결정 대기**(beep 앱 계층 가드 `conversations.contains_key`는 "둘 다 죽고 다시" 비결정 · 44 §7-2) · **B-3** 사본 고지 = **수용 `92c92ab`**(nbeep-relay 머리말 + 44 §7-4 절차: 와이어 변경 커밋에 `[clip-sync]` 표식 · §7-5 이력) · C-1~3 정보 공유. → 44 §7-3 회신표 그대로 [22 §3](../22-upstream-beep-liaison.md) 갱신: **I-1·I-5·I-7·I-8 [반영] · I-4 [전달]** · §5 "🔴 사용자 확인 필요" 해소(STATUS 09-05 ⏳ 결정 ②도 닫힘) |
| beep → clip 회신 예정 | beep journal 09-06: *"회신 예정(clip): 패스프레이즈 평문 저장 · 도메인 등재(`nbeep-user-*`) · XXpsk3 채택"* — 44에는 아직 미기재 → 원장 **I-10** 대기 등재 · 판단은 T-45 |
| 3-OS 검사 | `scripts/check-3os.sh` fmt ✓ · 호스트 ✓ · win ✓ · linux ✓(문서만 변경 · 캐시 적중 1초 내) |

**실측**: clip 들어온 변경 0 · dir2 ff 80 · beep 병합 28(relay +104/-21 · 와이어 0) · 원장 상태 갱신 5 + 신규 2(I-9 · I-10) · TODO +2(T-45 · T-46).

**판단**: beep의 XXpsk3는 *"승인 0회"* 를 위한 것이라 **clip DR-39(기기별 사람 승인 유지 · [09 §6-3](../09-identity-and-pairing.md) 타협 불가)와 방향이 다르다** — clip은 만남(RID 파생 · PBKDF2 · 핸들 솔트 · 일 에폭)만 beep과 같은 식으로 공유하고 신뢰 계층은 그대로 둔다. `nclip-sync`가 psk 경로를 흡수할 의무는 없고(와이어 무변경), 흡수하면 "패스프레이즈 = 신뢰의 전부"가 되어 09 §6-3과 충돌한다 → beep 회신 3건을 받은 뒤 사용자 결정(T-45).
