# 2026-09-02

> 시간 역순 — 최신이 위.

## 8차 — 메인 레이아웃: ★ 전고 툴바 + 검색은 우측 상단

사용자 요청 — 세로 툴바가 먼저(창 전고), 검색창은 툴바 제외 우측 영역 상단만 차지.
부분 행(픽셀 스크롤)의 핀 구분선·라벨/존 clip이 검색바 밑으로 침범하던 누수도 함께 수정(clip 정합).

## 7차 — 실기 S 회신: PPT 글상자 미리보기 회귀 · Rich 본문 존

| 항목 | 내용 |
|---|---|
| PPT 글상자 라벨만 보임(회귀) | 4차 순위 선택이 SVG 마크업 폴백까지 걸러냄 → `svg_text()` — `<text>` 요소의 글만 추출(태그 제거·엔티티 해제)해 평문 폴백 체인에 추가. 목록 Rich 서브줄·미리보기 모두 내용이 보인다 |
| Ctrl+1 행 내 미리보기 | 5차 64px 사각 박스는 왕가로 이미지가 슬리버가 됨 → 좌측 섬네일은 24px 지표로 되돌리고, 라벨 아래 **본문 존**에 이미지를 **높이 기준**(≈행 높이)으로 그린다(폭은 비율 · clip). 텍스트 행은 기존 2줄 그대로 |
| 섬네일 해상도 | THUMB_SIDE 48→160 — 존 크기(≈46px)로 그려도 선명(기존 항목은 재복사 때 갱신) |

**게이트**: clippy 3타깃 · 테스트 전부 통과 · debug exe 갱신.

## 6차 — 최상위 고정 아이콘 = Material `layers`

사용자 시안 반영 — 꺼짐 = 윗장 윤곽 + 밴드 1 · 켜짐 = accent 적층 3장(마름모 = `fill_triangle` 2장 합성).

## 5차 — ★ 픽셀 스크롤(메인·팝업) · 타이틀 · Rich 인라인 미리보기

실기 회신 + 요청 2건:

| 항목 | 내용 |
|---|---|
| ★ 픽셀 스크롤 | 목록 스크롤을 행 단위 → **픽셀 단위**(`scroll: i32` + 부분 행 그리기). ★ 터치패드 소수 델타가 `as i32` 절단으로 0이 되던 결함은 f32 누적기(`wheel_frac`)로 해소 — '천천히 굴리면 안 움직임'의 원인. 메인·팝업 동일 규약 |
| 타이틀 | "Nexa Clip — Clipboard Manager"(4언어) |
| Rich 보기(Ctrl+1) | 섬네일 40→64px + 텍스트 시작 72px — 행 안에서 내용 미리보기. Ctrl+2 섬네일·Ctrl+3 텍스트 전용은 현행 유지 |
| 진단 | `preview_probe` 예제(nclip-plat) — 현재 클립보드를 K4 체인으로 디코드(48/1600). 실측: DIB 430×113 → 두 상한 모두 성공 = 디코드 체인 정상 |

⚠️ **PPT 글상자 한계** — PPT는 래스터 표현(PNG/DIB) 없이 EMF(핸들·바이트 없음)·SVG만 준다.
EMF/SVG 렌더러는 없으므로(DR-8) 라벨 폴백이 현재 상한. 후속 후보: Windows GDI로 EMF 래스터화(T-18d 연계).

**게이트**: clippy 3타깃 · 테스트 전부 통과 · debug exe 갱신.

## 4차 — 실기 P 회신: 휠 스냅백 · ★ 평문 순위 선택 · Object 미리보기

| 버그(실기 P1~P3) | 원인 → 처치 |
|---|---|
| 미리보기 켜면 목록 휠 미동작 | `paint()`가 매 프레임 `ensure_visible`로 휠이 옮긴 top을 선택 위치로 되돌림(잠재 결함 — 패널로 목록이 줄어 표면화) → paint에서 제거. 키 이동은 자체 호출 유지 |
| 텍스트인데 깨진 미리보기(CF_HTML 헤더 `Version:0.9`·`□□1`) | `Row.plain`이 **첫 디코드 성공** 표현을 물었다 → 스냅샷 `plain_text` 계약대로 `plain_rank` 순위 선택(`plain_of`) — 목록 Rich 서브줄·편집 시드도 함께 수정 |
| PPT 글상자 미리보기에 SVG 마크업 원문 | Object 종류는 텍스트 경로로만 흘렀다 → Image·Object 모두 이미지 디코드 시도, 실패만 텍스트 폴백(`preview_img` 페이로드 `Option` · `set_preview_failed`) |

**게이트**: clippy 3타깃 · 테스트 전부 통과 · debug exe 갱신(18:02).

## 3차 — ★ K4 미리보기 패널(CopyQ식 하단)

사용자 K4 요청(09-01) 이행 — 메인창 하단 미리보기:

| 구성 | 내용 |
|---|---|
| 토글 | 툴바 눈 모양(Material `visibility` 근사 · 켜짐=accent) · 빈 목록에서도 동작 · `ui.preview_open` 영속(**기본 접힘**) |
| 패널 | 상태줄 위 · 툴바 오른쪽 · 목록 영역의 35%(최소 80px) — 목록·`ensure_visible` 자동 축소 |
| 텍스트 | 전문을 읽기용 멀티라인 TextBox로(wrap 켜짐 · 패널 위 휠 = 전문 스크롤 · 키는 안 보내 편집 불가) |
| 이미지 | ★ 원본 해상도(긴 변 1600 상한) — 셀이 선택 항목만 **지연 디코드**(`take_preview_request` 펌프 · 섬네일 48px와 별도) · 비율 유지 · 확대 금지 |
| 공유 | `make_thumb` → `decode_image(reps, side)` 공용화 · clippy 8/7 → `OpenOpts` 구조체 |

**게이트**: clippy 3타깃 · 테스트 전부 통과 · debug exe 갱신.

## 2차 — 실기 M 회신 반영: × 즉시 재필터 · 편집 시트 줄 바꿈 스위치

실기 통과: 최상위 고정 · M1(우클릭 편집 메뉴) · M2(단축키) · M3(더블/트리플 클릭) · M5(동적 언어). 수정 2건:

| 버그/요청 | 원인 → 처치 |
|---|---|
| ★ 메인 ×(지우기) 후 목록이 빈 채로 남음 | ×는 **MouseDown**에서 값을 바꾸는데 검색창 MouseDown 분기가 무조건 `None` 반환(변화 감지는 MouseUp에만 있었음) → down에서도 `display_text` 전/후 비교해 `QueryChanged`. 팝업은 `feed_search`가 이미 감지해 정상이던 것과 대칭 |
| M4 줄 바꿈 토글 UI | 편집 시트 우상단에 `Switch`(`LabelSide::None` + 왼쪽 라벨 직접 페인트 · i18n `WrapLabel` 4언어) — Alt+Z와 양방향 동기 · 시트 상단에 26px 스위치 줄 확보 |

**게이트**: clippy 3타깃 · 테스트 전부 통과 · debug exe 갱신. **실기 N1·N2 통과**(사용자 확인).

## 1차 — 검색 완전판 · 최상위 고정 · wrap · 더블클릭 · ★ 동적 언어 (실기 L 일괄)

실기 통과: L1(Sublime 연타)·L2(PPT ×)·L3(A-B-A 승격)·K5·K6. 요청 일괄(`72496ac`):

| 주제 | 내용 |
|---|---|
| 팝업 검색 = 메인과 동일 | 정식 TextBox — 캐럿·드래그/⇧선택·Ctrl+A/C/X/V·IME·★ ×지우기(beep `clearable`)·★ 우클릭 편집 메뉴(beep `EditMenu` · `set_ctl_labels`로 i18n 주입) |
| ★ 캐럿 깜빡임 | 셸 500ms 위상 → `RasterCtx::with_caret_on`(인프라 기존재 — 배선만) |
| ★ 최상위 고정 | 툴바 ⚙ 위 · 켜짐=accent · `ui.always_on_top` 영속 · `WindowLevel` 즉시 |
| ★ TextBox 확장 | word-wrap(소프트 행 · 편집 시트 기본 켬 · Alt+Z) — 히트테스트는 `line_lay`로 자동, 세로 이동은 논리 줄(1단) · 멀티라인 더블=단어/트리플=줄 |
| ★ i18n 스윕 | 메인/팝업 노출 문자열 29키 신설(4언어) — "기본 en인데 한글 보임" 해소 |
| ★ 동적 언어 | **`app.lang`이 부팅에서조차 미적용이던 결함 발견** → 부팅 적용 + 변경 즉시 전 UI 반영. 재시작 필요 설정 = `ui.font_family` 하나 |

**게이트**: 398 테스트 · clippy 3타깃 · exe 10:21.
