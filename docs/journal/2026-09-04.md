# 2026-09-04

> 시간 역순 — 최신이 위.

## 16차(Windows) — ★ 리치 2단(Outlook 불릿·목록·들여쓰기·배율·인라인 이미지·터미널 색/글꼴·ANSI) + ★ 활성 대상 따라가기 + 툴바 재배치·감시 토글 + hover 의도 코얼레싱(DR-41) (사용자 실기 다수)

| 축 | 내용 |
|---|---|
| 실기 | 새 Outlook 메일 복사 → 목록·미리보기에 `n  S&OP …`. 클립보드 실측: `HTML Format` + `UnicodeText`(+Chromium 토큰)만 — **비트맵 없음**. HTML = `<span style="font-family: Wingdings">n</span>`(Wingdings 0x6E = ■) · **평문 표현도 `n`**(U+006E). CopyQ는 Qt(QTextDocument)가 HTML을 통째로 그려 ■이 보이지만 제목줄(평문)은 CopyQ도 `n` |
| 판단 | "PPT처럼 이미지로"는 불가 — PPT는 Office가 미리보기 DIB를 같이 올려 준 것(08-26)이고 Outlook(WebView)은 안 준다. 우리가 HTML을 그림으로 만드는 것 = 레이아웃 문제는 그대로 + 메모리(장당 수백 KB~1MB · DR-9) · 테마/DPI 고정 · 검색 불가 → **런 파서 확장**(T-18d 2단). 래스터라이저 차이 = 픽셀 층은 동급, Qt의 **레이아웃 층**(HTML/CSS 박스 · HarfBuzz shaping)이 없는 것 |
| ★ 2단 | `richtext`: **심볼 글꼴 치환**(Wingdings/Symbol 표 18자 + PUA `U+F0xx`) · `<ul>/<ol>` **불릿 합성**(• ◦ ▪ / `1.`) · 블록 `margin-left`·`padding-left`·`text-indent`(pt/px/cm/in/em · `margin` 단축형) → `Run.indent`(em · 줄 첫 런) · `font-size` → `Run.scale`(본문 ±15%는 1.0 · 0.8~1.3 — 줄 간격 고정) · `<img>` = `[image]`. 구조가 있으면 색이 없어도 리치 경로 |
| 그리기 | 메인 행 · 미리보기 · 팝업 행 · "이미지로 복사" 4곳: `em = text_width("한")` · `select_font_sized(delta = em×(scale−1))` · `x += em×indent`. 이미지 렌더는 줄 높이 = 줄 최대 배율 |
| 실측 | 실기 HTML(92KB)을 `dump_fixture`(ignored 테스트 · `NCLIP_HTML_FIXTURE=<path>`)로 → `■  S&OP…` 2.0em · `• 일시…` 2.2em(Outlook `<ul>`은 margin-left 0 명시) · 참고문 5.8em · 표 자리 `[image]` — 원본 화면과 같은 구조. 표는 Outlook이 `data:image/png`로 넣는다(인라인 이미지 = 3단). 실기 덤프는 남의 데이터라 저장소 밖(테스트는 구조만 옮긴 축약본) |
| ★ 따라가기 | 사용자: "더블클릭은 선택이 아니라 **활성 대상** 변경 — 5번을 활성화하면 1번으로 가는데 선택은 5번에 남아 미리보기가 바뀐다". `follow`(id · 내용 열쇠 · 5초 시효) — `refresh`가 옮겨간 자리를 찾아 `sel` 이동 · 미리보기는 id 키라 **그대로** · `paint`에서 새 자리가 첫 화면 안이면 맨 위(1·2번이 보임) 아니면 그 행을 위에. 클릭·화살표·우클릭 = 해제. Enter·우클릭 복사 4종도 동일. 내용 열쇠 = 수신본을 복사해 새 로컬 항목이 된 경우 대비. ★ **이미지로 복사** = 새 항목(1번)이 생기므로 `follow_new`(arm 시점 최대 id보다 큰 첫 비고정 행) — 선택·미리보기·스크롤이 새 항목으로(사용자 요청) |
| ★ 인라인 이미지 | 사용자: "붙여넣기는 이미지가 나오니 미리보기도". `<img src="data:image/…;base64,…">` → base64 자체 해제(DR-8 · 8MiB 상한) → `Run.image`(원본 바이트 `Arc`). 미리보기 항목이 바뀔 때 격리 워커(`imgdec` · PNG/JPEG)로 8장·변 1600까지 디코드 → 줄 오프셋 누적합(가변 줄 높이)으로 패널 폭에 맞춰 비율 축소해 그림. "이미지로 복사"도 합성(원본 크기 · 폭 1200). 행은 `[image]` 텍스트 유지 |
| ★ 회귀 항목 | 사용자 제보: PLEdit7 SQL 복사가 평문처럼 — "기존 기능(색·굵기)이 없어지지 않도록 회귀 항목". [21 §7](../21-manual-test.md#7--리치-표시-회귀-t-18d--09-04) 신설(R1~R6 · 판정 순서 = `peek`로 HTML 유무 → `dump_fixture`). ★ **PLEdit 판정(`watch` 실측)**: PLEdit7 = 평문 6표현뿐(HTML/RTF 없음) → 원천이 서식을 안 올린 것, 앱 회귀 아님. Golden8 = 같은 SQL에 `HTML Format` 1.6KB → 색 표시. R3 = "평문 행이 정답"으로 기록 |
| ✅ 실기 | 사용자 확인(09-04): 미리보기 `[image]` 자리에 실제 표 이미지 · 이미지로 복사 후 1번 이동 · 더블클릭 승격 후 1번 이동(선택·미리보기 따라감) |
| ★ 툴바 재배치 | 사용자: "토글을 모아서 중복제외/미리보기/최상위 순으로 묶고 그 아래 감시 끄기 토글". `TOOLS_TOP` 11칸(구분선 뒤 Dedup·Preview·AlwaysTop · 구분선 · WatchOff) · 바닥 = 연결 표시 + ⚙(최상위 바닥 고정 제거). **감시 끄기** = `Tool::WatchOff`(사용자 지정 Material `stop_circle` SVG → 96² 알파 `icon-watch-96.alpha` + `play_circle` `icon-watch-play-96.alpha` — ★ **2상 토글**(사용자 3차 지시): 감시 중 = stop 벽돌색 `#B24C34`(누르면 중지) · 중지됨 = play 초록 `#43A047`(Material Green 600 추천 — 연결 녹색보다 한 톤 낮춰 "진행" 신호 · 누르면 재개) · 툴팁 = 현재 상태 + 다음 동작(`TipCaptureStop`/`TipCaptureResume` 4개 언어)) → `MainAction::ToggleWatch` → 셸 `watch_off`가 `on_captured`에서 로컬 캡처를 버린다(수신 항목은 그대로). 세션 한정(영속 없음 — 재시작하면 감시 켜짐) · 툴팁 = 기존 `ActionPauseCapture` |
| 아이콘 | 설정 ⚙ = 사용자 지정 Material `settings` SVG → `icon-settings-96.alpha`(톱니 벡터 근사 대체 · 툴바 알파 아이콘 6종째) |
| ★ 툴팁 페이드 | 사용자: "설정 창처럼 서서히 진해지게" → 정정 "툴바가 아니라 **툴팁**". 공용 `HoverFade`/`Fade::hover` — 툴바 툴팁·상태줄 점 툴팁이 바탕 알파 + 테두리/글자 색 보간(글자 알파 API 없음)으로 진해진다 · hover 상태 레이어도 같은 진행도(0.10×g) · `tick_ui`가 애니메이션 중이면 셸 박동 500ms → **16ms** |
| ★ 행 hover 페이드 | 사용자: "선택은 하늘색, 마우스 오버는 기본색보다 조금 진하게 서서히". `row_fade: HoverFade`(행 인덱스) — 본문색 6% 상태 레이어 × 진행도(선택 행 제외 · 다크는 밝아짐) · 커서 이동·이탈에서 목표 갱신 · 같은 16ms 박동 |
| ★ 설계 | 사용자: "빠른 스크롤 — 마지막 항목만 서서히, dir2 파일 처리처럼 큐에 넣고 필요한 것만 처리". [28 §hover 의도 코얼레싱](../28-main-window.md#-hover-의도intent-코얼레싱--설계-2026-09-04--사용자-요청): 큐 없는 코얼레싱(의도 1개 덮어쓰기) · 70ms 의도 · 120ms 스크롤 안정 · refresh 세대 가드 · `HoverIntent` 공용 구조체(tokens) → 같은 날 **구현**(사용자 "준비되면 진행") · 단위 테스트(5행 통과 = 0회 · 마지막 70ms 뒤 1회 · settle 보류 · clear). ★ 사용자 원칙 발언("메타 먼저·내용은 필요할 때 — 프로세스 방해 없이 필요한 일만 최소로") → **DR-41 최소 처리 원칙**으로 승격(10 · CLAUDE.md) |
| 재hover | 사용자: 더블클릭 승격으로 순서가 바뀐 뒤 커서를 안 움직이면 진해지지 않는다. `refresh`의 세대 가드가 의도를 버린 뒤 **재hover 예약**(`rehover_pending`, 커서가 창 안일 때) → 다음 `paint`가 새 오프셋으로 커서 아래 행을 의도로 등록(70ms 뒤 페이드). `cursor_in`은 CursorMoved/CursorLeft로 추적 |
| ★ 터미널 리치 | 사용자: "터미널 복사를 색·글꼴 그대로 미리보기에?" 실측 ① 기본 복사 = 평문뿐(ESC 0개 — 터미널이 렌더된 글자만 복사) ② **Windows Terminal 복사 서식(HTML) 켜면** `DIV white-space:pre; background-color:#0C0C0C; font-family:'D2Coding, JetBrainsMono Nerd Font'` + 색·기울임 span. 구현: 블록 `white-space:pre` 존중(열 보존 — 전엔 span만 봤다) · `font-family` 고정폭 판정 → `Run.mono`(Mono 슬롯) · `background-color` → `Run.bg`(글자 폭 배경 · 행/미리보기/팝업/이미지로 복사) · ★ **고정폭 글꼴 슬롯** `ui.font_mono`(비면 Nerd Font → D2Coding → … 후보 · 주 글꼴 폴백) + 주 글꼴 폴백 체인에도 붙여 평문 행의 Nerd 아이콘(PUA) 두부 방지 |
| ★ ANSI SGR | 사용자 "ANSI 이스케이프 인식은 불가능해?" → `ansi_runs_of`: HTML 없고 평문에 `ESC[…m`이면(원시 로그) 16색(Campbell)·256·트루컬러·굵게·기울임·배경을 런으로(OSC·기타 CSI 스킵). 터미널 복사엔 ESC가 없어 해당 없음 — CI 로그·`script` 기록용 |
| ★ 기록 모두 삭제 | 사용자: "고급에 일괄 삭제 · 2단계 확인". `hist.clear` Action(고급) — 설정 창이 첫 클릭에 Warn 노트로 무장(2초 · `expire_clear_arm` 폴 만료) · 둘째 클릭에 `clear_request` → 셸이 `History::remove_unpinned`(고정 제외) + 저장소 `remove` × n + 메인/팝업/트레이 갱신 · Ok 노트 "삭제했습니다 · 고정 항목은 남겼습니다" · ★ 무장 2초 동안 **버튼 자체가 Danger 톤**(사용자 — 암호 재생성과 같은 패턴 · `Button::set_tone` + `SettingsWidget::set_action_tone`). 고정 항목을 남긴 것은 설계 선택(큐레이션 보호) — 전부 지우려면 `sec.clear_on_quit`/`wipe` 경로 |
| ★ 툴바 복사 3종 | 사용자: "좌측에 Copy · Copy as plain text · Copy as image 순, Copy는 항상 · 나머지 둘은 항목에 따라 활성(우클릭 메뉴와 동일)". `Tool::CopyImage`(Material image 근사 벡터) 추가 · `tool_enabled` 단일 규칙 — 평문 = `PasteAs::applicable`에 Plain(서식 있는 글) · 이미지 = Text/RichText · `act`가 비활성 클릭을 무시 |
| ★ 메모리 설계 | 사용자: "메모리가 많다 — 순위 밀린 항목은 메타만 남기고 해제, 쓸 때 다시, 주기 스케줄". 실측 WS 82MB(291개) · 원인 = 인덱스의 **원시 RGBA 섬네일 64MB 기동 적재** + 본문 미해제 + Row 섬네일 복제·전행 리치 파싱 → [30 메모리 상주 계층](../30-memory-residency.md) 신설(M/T/B/V 계층 · GC 틱 · 비동기 재적재 · 섬네일 PNG blob 이관 A+C 권장 · 결정 ①~⑤ 질문) |
| ★ DR-42 + 2단계 | 사용자 결정 ① 고정 항상 상주 ② 본문 즉시 내림 ③ 30초 ④ 40/96MB ⑤ A+C(384²). 구현(2단계): `unload_cold`(이미지 blob 표현만) · 참조 보존형 `ensure_loaded` · `release_body`(복사·이미지로 복사·미리보기 뒤) · `gc_memory`(30초 · 상한 초과 즉시 · 고정·미리보기 제외 · 로그 1줄) · `HistoryStore::add → Vec<blob 참조>`(캡처 항목도 내릴 수 있게) · 창 닫힘 뷰 캐시 비움. 섬네일(3단계)은 다음 |
| ★ 3단계 섬네일 | `EV_ADD3` + `thumb_ref`(PNG blob) · `AddRefs` · `store_add` PNG 인코드→blob→캐시 · 기동 마이그레이션(백그라운드 · 오래된 것부터) · `thumbs.rs` LRU(32 · 고정 제외) · 메인/팝업 Row = 치수만 · `pump_thumbs`(동시 4 워커 디코드) · 384². 인덱스 64MB → 참조만 · ★ **실측: 기동 WS 82MB → 18.7MB(Private 5.3MB)** · 107개 이관 완료(12초) · 인덱스 파일 축소는 다음 압축 때 |
| 4단계 | 자리표시(흐린 상자) · `rich_cache`(id+내용 열쇠 · refresh 재파싱 0) · 마이그레이션 완료 → `compact_now`(인덱스 64MB 즉시 축소) · DR-9 수치 확정 · 21 §8 M1~M7 |
| 팝업 hover | 사용자: "팝업도 마우스 아래 항목이 서서히". 메인과 같은 `HoverIntent`+`HoverFade`(70ms · 휠 settle · CursorLeft · refresh 세대 가드) · `draw`에 페이드 전달 · 셸 박동 OR |
| ★ 검색 | 사용자: "`nexa-app` 검색에 4·5번(터미널 복사)이 빠진다 — 기준? · 정규식 선택". 기준 = **라벨(첫 줄 44자) 부분 문자열**뿐이었고 설정 `find.mode`는 코드가 무시. → `nclip_core::search::Matcher`(라벨 + 본문 전체 · 대소문자 무시 · 정확히/유사/정규식) · ★ **자체 정규식 엔진**(DR-8 · 백트래킹 · . * + ? {n,m} [] () \| ^ $ \d \w \s \b · 단계 상한 20만 · 오류 → 정확히 폴백 · 테스트 3) · 셸이 박동마다 `find.mode` 동기(바뀌면 창 재필터) · 설정 설명 4개 언어 · 21 §9 F1~F5 |
| ★ 검색 색인 | 사용자: "본문이 blob 파일에 흩어져 있으니 I/O 병목 조치". 사실: 16KB 이상 본문은 blob(미적재)이라 종전엔 검색은커녕 **보이지도 않았다**. → `search_index`(id → 소문자 검색문 · 라벨+본문 · 256KB 상한 · Rc 공유): 캡처·수신·편집 때 즉시 · 기동 때 미적재 텍스트 blob은 **배경 스레드**(`BlobReader` — 경로+키만 든 Send 손잡이)가 한 번 읽어 `SearchText`로 채움(그동안 라벨 검색) · 검색은 색인만 `contains`(소문자 변환·파일 I/O 0) · 삭제/일괄 삭제 동기. 향후: 1만 건↑이면 삼중자(trigram) 색인·증분 좁히기·키 입력 코얼레싱(28 §hover 방식) — 실측 뒤 |
| 검색 Space | 사용자: "검색어에 space 입력이 안 됨". 메인창 키 분기가 `Key::Character`만 받아 winit의 `Named(Space)`가 버려졌다(팝업은 `event.text` 경로라 정상) → Space 분기 추가 |
| ★ 검색 방식 드롭다운 | 사용자: "검색바 앞에 방식 3개 · 아이콘 드롭다운(beep 정렬 드롭다운처럼) · 검색창 높이 정사각 · 메인+팝업". beep `IconDropdown`(nclip-ctl 이식본) 재사용 · 아이콘은 UI 글꼴 글리프를 96² 알파로 구움(`mode_drop.rs` — Aa · ≈ · .*) · 열림/그 자리 클릭은 메뉴와 같은 z순서로 먼저 먹음 · 고르면 `find.mode` 설정 → 박동 동기가 두 창의 방식·드롭다운 값·필터를 맞춤(설정 창 변경도 드롭다운에 반영) |
| 드롭다운 정정 | 사용자: "설정에서 바꾼 Fuzzy가 다른 항목을 골라도 안 바뀐다". 원인 = 드롭다운이 열린 동안 우선 분기가 이벤트를 삼켜 `self.cursor` 갱신(원래 CursorMoved 분기)이 막혔고, 클릭은 좌표가 없어 옛 좌표로 행 판정 → 커서 갱신을 분기 앞으로. 함께: 정규식 아이콘 = 사용자 지정 Material `regular_expression` 알파 · 설정 창 라디오가 드롭다운 변경을 되비춤(폴 동기) |
| 아이콘 | 검색 방식 셋 다 사용자 지정 Material 알파 — 정확히 `equal`(두 줄) · 유사 `robot` · 정규식 `regular_expression` (글리프 굽기는 폴백으로만) |
| 기본값 | 사용자: "Fuzzy를 기본값으로". `find.mode` 기본 · `Mode::default` · 창 초기값 · 미지 코드 폴백 전부 유사로 |
| ★ **v0.1.0 릴리스** | 사용자: "winget·choco 제외, brew 포함 릴리스". 변수 `WINGET_PUBLISH`·`CHOCO_PUSH`=false → 태그 `v0.1.0` push → run 33855847193 **전부 녹색**: 5타깃 package · publish(정식 릴리스 · 14자산 · draft=false) · brew 탭 커밋 "nexa-clip 0.1.0"(kiros33/homebrew-tap) · windows-packages는 매니페스트 산출만(choco push · winget PR 단계 **skipped** 확인). 18 §10-1 명령 모음 추가 |
| ★ 신원 점검 | 사용자: "Handle·passphrase 하나라도 비면 서버 접속 불가". 09-03의 "빈 값이어도 접속만 유지"를 뒤집음 — `spawn_if_enabled`가 `SyncStatus::Unconfigured`로 두고 릴레이·LAN 모두 안 띄움 · 설정 창은 Test·Disconnect 잠금 + 노트 "핸들과 페어링 암호가 모두 있어야 합니다"(Warn · 자동 Test도 같은 노트) · 상태줄 점 = 흐림 |
| ★ v0.1.1 재배포 | 사용자: "수정 버전 재배포". 0.1.0 이후 검색(방식 드롭다운·아이콘·유사 기본)·신원 점검·Space 입력·hover 코얼레싱·메모리 4단계를 담아 `0.1.1` 태그(brew만 · winget/choco 변수 false 유지) → run 33856839395 **전부 녹색** · 릴리스 14자산 · brew 탭 "nexa-clip 0.1.1" |
| ✅ 실기 M2 | 사용자 "모두 통과": 섬네일 자리표시 → 도착 · 행 높이 불변 · 빠른 휠 뒤 멈춘 화면만 채움 · 캐시 상한 밖 재채움 · 팝업 동일 · 새 캡처 즉시 표시 |
| ✅ 실기 F1~F3 | 사용자 확인: 본문 전체 검색 · 유사(순서 무관) · 정규식 3패턴 + 틀린 패턴 폴백 |
| S11 정정 | 사용자: "비우고 Enter를 눌러야 효과". 설정 `Text` 행이 글꼴 행과 같은 Enter 확정 규칙을 쓰고 있었다 → `Text` 종류만 글자마다 보고(글꼴은 Enter 유지 · 08-09 사유) — 핸들·암호를 지우는 즉시 잠금·노트·해제 |
| ★ 설치본 실기 스크립트 | 사용자: "빌드 → setup 설치 폴더에 실행 파일 복사 → 기존 프로세스 종료 → 재시작을 스크립트로 · 문서화". `scripts/dev-install-win.ps1`(릴리스 기본 · `-Debug`) — 첫 실행에서 설치본 `--version` 파이프 캡처가 stdout 패닉(os error 232 · windows 서브시스템)이라 버전은 Cargo.toml에서 읽도록 수정. 18 §10-1에 기록. 설치본 data는 개발 인스턴스와 별개 |
| ★ 설정 창 배치 | 사용자: "메인창이 최상위면 설정 창은 그 위 · 메인창 모니터·옆에서 열림 · 마지막 위치 기억 · 메인창이 모니터를 옮기면 초기화". 셸이 열기 직전 `set_anchor(메인 기하, 최상위)` → `initial_placement`(메인 중심이 든 모니터 · `ui.set_mon` 키가 같으면 `ui.set_x/y/w/h` 복원, 아니면 오른쪽→왼쪽→겹침 · 모니터 안으로 클램프) · 창 레벨 = 메인 최상위 따라감(토글 즉시) · 닫기/종료 때 `save_geom`. 21 §6 W1~W4 |
| 자동 시작 인자 | 사용자: "Run 등록에 `tray` 인자 — 기본이니 제외 · 다른 OS도 검토". 무인수 = 상주(windows 서브시스템 전환 뒤)라 군더더기 → Windows Run 값 · mac LaunchAgent `ProgramArguments` · Linux 자동 시작/런처 `.desktop` 모두 실행 파일만. 부팅 재동기화가 옛 등록을 덮어쓴다 · 테스트 4건 갱신 |
| 붙여넣기 즉시 반영 | 사용자: "빈 칸에 붙여넣으면 Test가 안 열린다". 위젯의 변경 수거가 `on_event` 끝에서만 돌아 `clipboard_paste/cut`(Ctrl+V·우클릭) 경로가 빠졌다 → 두 경로 끝에도 수거 · 설정 변경 로그에서 암호 값 마스킹 |
| 메인창 기하 | 사용자: "완전 종료 뒤에도 같은 위치·크기 · 2→1 모니터면 보이게 보정". 이동·크기 변경 이벤트에서도 저장(최소화 -32000 제외) · 열 때 `fit_geom_to_monitors`: 창 중심이 든 모니터 안으로 클램프, 없으면 주 모니터 좌상단+40px(크기도 모니터에 맞춤) · 로그 1줄 |
| ✅ 실기 | 사용자 "동작 확인": 텍스트 행 붙여넣기 즉시 반영(Test 활성) · 메인창 기하 저장/복원(W5) |
| ✅ 실기 W1~W4 | 사용자 확인: 설정 창 최상위 따라감 · 메인창 옆 첫 위치 · 마지막 위치 복원 · 모니터 이동 시 초기화 |
| 남김 | 평문 표현의 `n`은 그대로(메모장 붙여넣기) — HTML로 평문 재생성은 캡처 데이터를 바꾸는 일이라 옵션 결정 필요 · 표 구조 · 색 이름 · 원격 `src`(http) 이미지는 자리표시 · PLEdit은 원천 한계(서식 미제공) |

## 15차(Windows) — ★ 배포 파이프라인(brew · winget · choco) + 검수 대기 자동 판정 (사용자 요청 · secrets 3종 추가)

| 축 | 내용 |
|---|---|
| 이식 | `nexa-beep` `release.yml`·`homebrew.yml`·`publish-windows-packages.yml` + `packaging/`(winget·choco·homebrew 틀 · NSIS · plist · deb · `render-manifests.sh`) → 이름·설명·태그·권한 문구·실행 인자(무인수 = 트레이)·워커 이름(`nclip-imgdec`)으로 치환. 브랜딩은 이미 있던 `packaging/branding`(08-26) |
| 세 채널 모두 | brew Cask `nexa-clip` / Formula `nexa-clip-portable`(kiros33/tap) · winget `SosomLab.NexaClip[.Portable]` · choco `nexa-clip[-portable]` — 릴리스 태그 하나로 전부. 저장소 변수 `WINGET_PUBLISH`·`CHOCO_PUSH`=true 설정(`gh variable set`) · 시크릿 3종은 사용자 추가 |
| ★ 검수 대기 판정 | beep는 사람이 `docs/18 §5` 절차(`gh pr view` · choco 피드 curl)로 점검해 스위치를 켜고 껐다 → 여기서는 `publish-windows-packages.yml` **guard 잡**이 릴리스마다 판정: winget = 토큰 주인이 낸 열린 PR에 `SosomLab.NexaClip`이 있으면 대기 · choco = 직전 정식 태그 버전이 공개 피드에 없으면 대기(모더레이션 중 패키지는 피드에 숨는다). 대기면 그 채널만 건너뛰고 경고 · `force=true`로 사람이 강제 가능 · 첫 제출은 판정 없음 |
| 보강 | `nexa-clip --version`(brew formula test) · 루트 `README.md` 신설(설치 명령·위키 링크) · meta 잡이 태그 ≠ Cargo 버전을 막음 · Linux 러너 시스템 패키지 · 크기 게이트 ≤10MB(DR-9) |
| ★ Linux 비종속 | 사용자: "배포판 가리지 않고 바로 실행(gcc 버전 미종속) — beep와 동일하게". beep의 musl 정적은 **서버**용이고 GUI 앱(winit·xkbcommon·Wayland dlopen)엔 불가 → **zig 링커 glibc 2.17 기준 링크**(`cargo zigbuild … gnu.2.17`) + `wayland-dlopen` features(빌드 시 시스템 lib 링크 0) + **심볼 게이트**(NEEDED가 glibc 계열 외면 실패 · GLIBC_ 최고 > 2.17이면 실패) · deb Depends 2.17 |
| 검증 | `render-manifests.sh`를 가짜 자산으로 드라이런 — 자리표시자 0 · 9자산 해시 치환 확인. 워크플로는 수동 실행(초안) 3회차에서 **전부 녹색**(run 33838509703) — meta · 5타깃 package · publish(초안 릴리스 14자산 · SHA256SUMS · 매니페스트 zip). 1회차 실패 = `cargo zigbuild --version` 미지원 플래그(→ `cargo-zigbuild --version`) · 2회차 실패 = `render-manifests.sh` 실행 비트 누락(Windows 커밋 → `git update-index --chmod=+x`). Linux 심볼 게이트 실측: NEEDED = libc·libm·libpthread·libdl만 · 요구 glibc 최고 **2.17** ✅. 드라이런 초안 `v0.0.0-dev.8f0f146`은 지워도 된다 |

**미결**: 서명·공증(인증서) · 위키 Release-Notes 페이지 관례(지금은 DEVLOG 링크) · 첫 태그 `v0.1.0` 발행 시점은 사용자 결정.

## 14차(Windows) — 상태줄 점 툴팁 · ★ 팝업 = 메인창 규칙(병합·녹색 점) · 공용 dedup 모듈 · 문서/위키 최신화

| 축 | 내용 |
|---|---|
| 툴팁 | 상태줄 우측 점 hover → 색 뜻(녹 릴레이 · 파랑 None · 회색 끔 · 흐림 미연결) 4개 언어 · hover 영역은 점보다 넉넉히 · 창 안쪽(점 위·우측 정렬) |
| 팝업 동등화 | 사용자 지적 "메인창과 팝업 내용이 다르다" → `crate::dedup` 신설(내용 열쇠 `content_key_of` · `merge` — 로컬 대표 · 출처 합집합 · 복사 수 합) · 메인창 `dedup_rows`·팝업 `refresh`가 같은 함수 사용 · 팝업 `set_dedup`(열 때 + 토글 왕복) · 팝업 번호 아래 **녹색 점**(핀 옆 · 5px+2px) · 테스트 +1 |
| 정정 | 13차 ⑤ "병합 = 기본 기능"을 토글 제거로 읽었던 것 → 사용자 정정 "토글이 사라졌어" → revert · **토글 유지 · 기본 켬** |
| 문서 | 14 §3-9 실구현 반영(10키 + 미구현 표기) · 21 점검표 행 + **§6 동기화 실기 절차 S1~S10** · ★ **GitHub 위키 6페이지**(Home · 시작하기 · 동기화 설정 · 같은 네트워크 직결 · 중복 제외와 출처 표시 · 여러 인스턴스로 시험하기 · 문제 해결) |

**실측**: clippy 0 · 테스트 477 · 기본 인스턴스 1개 상주(사용자 요청 "정식 프로세스만"). 사용자 확인 ✓: 두 인스턴스 연결·복사.

## 13차(Windows) — 실기 회신: ★ 리치 행 패닉(한글 경계) · 병합 열쇠 = 내용 · 점 배치/색 · ★ 병합 = 기본 기능 (사용자 확정)

| # | 관찰/지시 | 반영 |
|---|---|---|
| ① | 트레이 좌클릭 → 종료 | **크래시**: `richtext::decode_entities`가 `&` 뒤 12바이트 창을 잘라 `;`를 찾는데 12번째 바이트가 한글 안 → 슬라이스 패닉(메인창 리치 행 생성 중 프로세스 종료). 창 끝을 **문자 경계**로 내림 · 회귀 테스트 3 |
| ② | 회색 행? · 병합 안 됨 | 회색 = Rich 보기가 원본 HTML 글자색(터미널·에디터의 회색)을 그대로 렌더한 **로컬** 항목(수신은 평문 = 검정) · 병합 실패 원인 = 열쇠가 표현 집합 지문(로컬 평문+HTML ≠ 수신 평문) → **내용 열쇠**: 텍스트 = 평문(CR 제거·끝 공백 제거) · 이미지/개체 = PNG 바이트(없으면 DIB) · 그 외 지문. 한계(사용자 안내): DIB만 vs PNG · 인코딩 다른 PNG는 다른 열쇠(픽셀 해시는 선택지) |
| ③ | 점: 핀 옆에 · 상태줄과 같은 녹색 · 2px 붙여서 | Rich 보기 수신 점을 핀 점 옆(점 6 + 2) · Plain/상세도 핀 뒤 8px · 색 = 릴레이 녹색(46,204,64) |
| ④ | 수신본 뒤에 로컬 복사 = 로컬 우선 | 이미 그렇다(로컬이 대표 · 최신이라 맨 위) — 확인 답변 |
| ⑤ | ★ **병합 행 숨기기는 기본 기능으로** | 처음엔 토글을 없애고 항상 병합으로 읽었으나 사용자 정정("토글이 사라졌어") → **토글 유지 · 기본값 켬**(`ui.dedup_view` 미설정 = 켬). 제거 커밋은 revert |

**실측**: clippy 0 · 테스트 통과 · 두 인스턴스 재기동(LAN 세션 유지). 사용자 확인 ✓: 점 위치 · 병합.

## 12차(Windows) — 실기 회신: 로컬 2줄 결함 · 표시 3색 · ★ 중복 제외 툴바 토글 + 내용/메타 분리 (사용자 지시 4건)

| # | 지시/관찰 | 반영 |
|---|---|---|
| ① | None(로컬)은 네트워크 미연결 — 트레이·툴바는 미연결로, 상태줄 점은 3색 | `SyncMode{Off,Local,RelayDown,Relay}`: 트레이 배지·툴바 아이콘 = **릴레이 연결** 기준 · 상태줄 점 = 녹(릴레이) · **파랑(None)** · **진회색(미사용)** · 흐림(릴레이 미연결) |
| ② | 기본 프로필만 로컬 행이 2줄 | **근인**: 같은 PC 두 인스턴스는 상대가 게시한 클립보드도 자기 감시가 다시 읽는다. 그 스냅숏은 평문 하나인데 원본은 HTML 등 표현이 더 많아 "표현 집합 완전 일치" 판정에 실패 → 새 로컬 행. b는 자기 게시 에코가 먼저 흡수돼 타이밍상 안 보였을 뿐. → 승격 판정 = 출처 동일 && (집합 동일 **‖ 스냅숏 ⊆ 기존 항목**). 기존 테스트 기대("에코 소진 후 평문 = 새 항목") 갱신 · 회귀 테스트 |
| ③ | Hide duplicates → 툴바 토글(Material `compress` · 미리보기 위) | 검색줄 스위치 제거 · `Tool::Dedup`(사용자 SVG 96² 알파 · 켜짐 accent · 툴팁 "중복 제외") · 토글은 셸 왕복(`DedupView` → 영속 + `apply_dedup` + refresh) · **기본 켬** |
| ④ | 내용과 메타 분리 — 내용이 같으면 1개만 | 이력은 출처별 항목 유지(기기별 승격·×N 보존) · **보기**가 내용 열쇠로 묶어 대표 행 1개(로컬 우선 · 없으면 최근 수신) + **메타 = 출처 합집합**(`Row.origins` — 로컬 앱 · ⇄ 기기들 · 복사 수 합) · 미리보기 첫 줄/우클릭 맨 위 "출처 A · ⇄ b" · 로컬 출처가 있으면 내 것(녹색 점 없음) · 토글 끄면 출처별 행 |

**실측**: clippy 0 · 테스트 475 · 두 인스턴스 재기동 · LAN 세션 유지. ⏳ 실기: 같은 텍스트 동시 복사 → 토글 켬 = 1행(출처 둘) / 끔 = 로컬+수신 ·
툴바 아이콘 accent 토글 · 상태줄 3색 · 로컬 행 증식 소멸.

## 11차(Windows) — ★ 수신 항목 표시(보낸 기기별 항목 · 녹색 점 · 출처) + 중복 제외 보기 (사용자 지시 3건)

**관찰**(같은 PC 두 인스턴스 · 사용자): 복사 한 번에 두 프로그램이 동시 캡처 → 서로 전송 → 같은 텍스트가 여러 행. 어느 것이
내 것이고 어느 것이 받은 것인지 표시가 없었다. **사용자 결정**: 해시가 같아도 **보낸 기기별로 별도 항목**(수신 횟수만큼 ×N),
목록엔 수신 표시 점, 미리보기/우클릭에 출처 · 그리고 **중복 제외 보기** 스위치(로컬 1건 우선 · 수신끼리는 최근 1건).

| 축 | 내용 |
|---|---|
| 이력 동일성 | ★ **합성 표현 무시**(`CF_TEXT`·`CF_OEMTEXT`·`CF_LOCALE`·`CF_DIBV5`·`CF_BITMAP`·`CF_METAFILEPICT`) — 지문·동일성·부분집합 모두. 원격에서 받아 게시한 `CF_UNICODETEXT` 하나와 감시가 다시 읽은 "+합성 3종"이 같은 내용으로 판정된다(종전엔 어긋나 에코가 새 로컬 항목을 만들었다) |
| 기기별 항목 | 승격 판정에 `remote_origin`(출처 `⇄ ` 접두) 일치 조건 — 같은 내용이라도 **로컬 / mac 수신 / linux 수신은 별도 행**, 같은 기기 재수신은 그 행 승격(×N). 테스트 `remote_items_separate_per_sender` |
| 표시 | 목록: 수신 항목 앞 **녹색 점**(상태줄 연결 점과 같은 색 · 핀 점 옆/아래) · 미리보기: 첫 줄 "출처 ⇄ 기기명" · 우클릭: 맨 위 비활성 항목 "출처 ⇄ 기기명" · 상세 보기는 종전 우측 출처 텍스트 |
| ★ 중복 제외 보기 | 검색줄 오른쪽 **스위치**(`ui.dedup_view` 영속 · 라벨 "중복 제외") — 내용 열쇠(`History::content_key` = 합성 제외 지문)가 같은 행들 중 **로컬 1건**(가장 최근), 로컬이 없으면 **가장 최근 수신 1건**만. 순서는 최신순 유지. 셸이 `MainAction::DedupView` → 영속 + refresh |

**실측**: clippy 0 · 테스트 474(+1) · 두 인스턴스 재기동. ⏳ 실기: 같은 내용 동시 복사 → 로컬 1 + 수신 1(녹색 점) · 재복사 시 ×N ·
미리보기 첫 줄 출처 · 우클릭 출처 · 중복 제외 켜면 로컬 1건만.

## 10차(Windows) — 실기 회신: 릴레이 None 마감 · 기기 행 버튼 축소+줄바꿈 · ★ 연결 표시 판정 통일 (사용자 지시 4건)

| # | 지시 | 반영 |
|---|---|---|
| ① | None이면 Test·Disconnect·포트 불필요 | None = 세 항목 잠금 · None 선택/동기화 켬은 **Test 없이 즉시 LAN 전용** 적용(`sync_respawn`) · None→서버는 종전대로 해제 후 Test |
| ② | 값은 "None"만, 부연은 설명란 | 옵션 라벨 4개 언어 "None" · 설명에 "None = 같은 네트워크만(서버 없음 · 포트·Test·Disconnect 사용 안 함)" |
| ③ | 승인 버튼이 너무 큼 · 설명이 가림 | 버튼 = Status 폰트 · 높이 = 컨트롤 3/4 · **폭 = 라벨 길이 산정(i18n · ASCII 7/그 외 13 + 여백)** · 행 텍스트는 버튼 앞에서 **줄바꿈(최대 3줄)** — 높이 예약·배치·페인트 모두 같은 추정식 |
| ④ | 프로필 인스턴스에 연결 아이콘 없음 — 설계인가? | **빈틈이었다**: 아이콘은 릴레이 러너의 `SyncState` 이벤트로만 켜져 릴레이 None(프로필 b)은 영영 "꺼짐"으로 보였고, 기본 인스턴스는 None 전환 전 끊김 이벤트가 남아 보였다. → `refresh_sync_indicator`: **동기화 Off = 숨김 · 켜짐 = 릴레이 연결 ∨ LAN 피어 연결** — `SyncTick`(LAN 세션 변화)·`SyncState` 둘 다 같은 판정 |

**실측**: clippy 0 · 테스트 통과 · 두 인스턴스 재기동. ⏳ 실기: b 인스턴스 툴바 연결 아이콘(LAN 연결 시 케이블·아니면 플러그) · 기기 행 버튼 크기·줄바꿈 · None 잠금.

## 9차(Windows) — ★ 재시도 정책 통일 · 릴레이 None · 기기별 승인 + 대조 코드 · 동기화 끔 = 전부 끔/잠금 (사용자 지시 4건)

**재시도 정책(사용자: "실패 횟수에 비례해 지수적으로 · 기준을 정리해 설명/옵션으로")** — `sync_cmd::Backoff{base,cap}`:

| 규칙 | 값 |
|---|---|
| 실패 n회째 대기 | `base × 2^(n−1)`, 상한 `cap`, **±20% 지터**(여러 기기 동시 몰림 방지), 성공하면 0으로 |
| 적용 | 릴레이 재접속(끊김도 1회 실패로 셈) · 알려진 기기 재다이얼(기기별) · 페어링 탐색 · LAN 다이얼(기기별) — **한 정책** |
| 설정 `sync.retry` | 표준 5s→5분(기본) · 느긋 15s→15분 · 적극 2s→1분 — 설명에 규칙 전문 · 실패 노트에 "N초 뒤 재시도(실패 k회)" |

종전 릴레이 고정표 [5,15,60,300]s는 폐기(같은 값이 표준 정책의 1·2·4·7회째에 해당). 테스트 `backoff_tests`.

**릴레이 `None`**(사용자 실기 절차 질문에 대한 답 겸): Relay server 옵션 **None = 같은 네트워크만**(러너 없이 LAN 직결 · 상태 `LanOnly` 노트) ·
서버 선택 = LAN + 릴레이. Test는 None이면 서버 시험 없이 LAN 전용으로 켠다. ⇒ "두 인스턴스 Disconnect 후 재시작"으로는
릴레이가 다시 붙어 LAN 검증이 안 된다 — **None으로 두고 Test**가 맞는 절차.

**기기별 승인**(사용자 찬성 → 착수): `SettingKind::DeviceList` 신설 — 행마다 [승인|해제][삭제](Safe/Default/Danger 톤) ·
온라인 행에 **6자리 대조 코드**(`sas::safety_number(me, peer)` 앞 6자리 — 키 쌍의 함수라 양쪽 동일 · docs/09 §6-3 "입력이 아니라 보기") ·
값 = `hex\tstate\ttext` · 클릭 = `sync.devices = approve:hex | revoke:hex | delete:hex` · 전체 승인 버튼 제거.

**동기화 사용 끔 = 동기화 자체 끔**(사용자): `stop_all()` — 릴레이·LAN·세션 전부, 상태 Off(노트 없음). **꺼져 있으면 아래 설정 9키 잠금**
(기기 이름·핸들·암호·서버·포트·재시도·Test·Disconnect·기기 목록 — 비밀 행 눈/생성 버튼도 잠금 존중). 잠금은 매 폴마다 계산(상태 조기 반환 앞).

**실측**: clippy 0 · 테스트 473 · debug 빌드 · 두 인스턴스 LAN 세션 유지. ⏳ 실기: 기기 행 버튼(승인→해제→삭제) · 양쪽 대조 코드 일치 ·
None 선택 시 LAN만 · 동기화 끔 = 목록 오프라인 + 아래 설정 흐림 · 재시도 옵션 변경 후 실패 노트의 대기 시간.

## 8차(Windows) — ★ 과다 트래픽 예방(릴레이·LAN 공통 — 사용자 요청 "패킷 과다로 기능·성능 문제 없게")

| 지점 | 전 | 후 |
|---|---|---|
| 알려진 기기 재다이얼(릴레이) | 오프라인 기기마다 **매 10초** `Open` | 기기별 지수 백오프 10s → 20 → … → **5분**(붙으면 초기화) |
| 페어링 탐색(릴레이) | 상대 없으면 매 10초 `Open` | 10s → … → **2분**(누군가 붙으면 기본 주기로) |
| 인바운드 랑데부(릴레이) | 들어오는 대로 스레드 | 동시 핸드셰이크 **4** 초과분은 버림(상대가 다시 연다) |
| 송신 큐(피어별) | 밀린 항목 전부 순서대로 | **가장 최신 하나만**(클립보드 의미: 마지막이 현재) · 건너뛴 수 로그 |
| 재전송 | 같은 바이트도 다시 | 같은 페이로드 지문 **2초 안 억제**(승격 에코·앱 반복 게시) |
| 수신(피어별) | 조립 상한 32MB만 | **10초 창 24MB 예산** — 초과 조각은 조립 안 함(창마다 1회 경고) |
| LAN 비콘 송신 | 5초 고정 | LAN 피어가 붙어 있으면 **30초**(발견은 끝났다) |
| LAN 비콘 수신 | 무제한 디코드 | **초당 20개** 상한 · `seen`/백오프 표 256 상한 |
| LAN 수락 | 연결마다 스레드 | 동시 핸드셰이크 4 · **실패 IP 30초 냉각**(닫고 로그 없이) |
| LAN 다이얼 | 기기별 8초 고정 | 8s → … → 5분 지수 백오프(붙으면 초기화) |

기존 유지: 릴레이 재접속 백오프 [5, 15, 60, 300]s · 핑 15초/무응답 45초 · 조각 60KB · 항목 32MB · 조립 대기 8개 ·
`DIALING` 동시 다이얼 4. **실측**: clippy 0 · 테스트 통과 · 두 인스턴스 LAN 세션 성립 유지.

**사용자 질문(기기 승인을 각 피어 옆 버튼으로)에 대한 의견** — 찬성. 근거·설계는 STATUS/이 대화 요약 참조: 승인은 키에
붙는 행위라(docs/09 §1) **기기 단위**가 맞고, "지금 연결된 전부 승인"은 우연히 붙어 있던 낯선 기기를 함께 승인할 위험이 있다.
구현 = 설정 `Report` 행을 **행별 버튼이 있는 기기 목록 컨트롤**(`SettingKind::DeviceList`)로 교체 — 행마다 [승인]/[해제]/[삭제],
클릭 = `sync.approve:<hex>` 가짜 키. 다음 단계로 제안.

## 7차(Windows) — ★ 같은 네트워크 직결(릴레이 불요 · T-25/T-26 1단) + "네트워크가 UI를 멈추지 않게" (사용자 요청 2건)

| 축 | 내용 |
|---|---|
| 발견 | `lan.rs` — UDP 비콘 `47301`(5초): `NCLB` ‖ ver ‖ **LAN 태그 16B** ‖ PeerId hex ‖ TCP 포트. 태그 = `sha256("nclip-lan-v1" ‖ 페어링 RID)`(`rid::lan_tags` · 어제·오늘·내일 수용 · **RID 자체는 싣지 않는다** — LAN 도청자가 릴레이 만남 지점을 못 얻게). 송신 = 255.255.255.255 + **기본 경로 /24 지향 브로드캐스트**(가상 어댑터가 여럿이면 제한 브로드캐스트가 엉뚱한 곳으로만 나간다 — 이 PC 실측). 소켓 = 주소 재사용 + 브로드캐스트(`tcp::udp_beacon_socket` — 같은 PC 두 인스턴스가 함께 듣는다) |
| 연결 | 임의 포트 TCP 수락(비콘이 알림) · **내 키 hex가 작은 쪽이 건다**(릴레이 경로와 같은 타이브레이크) · 같은 Noise 핸드셰이크(`E2E_PROLOGUE` pub) · 세션 키 = 비콘 PeerId 검증(비콘은 힌트) → **릴레이와 같은 `run_peer`**(인사·목록·승인·전파 그대로) · 목록 표기 `연결됨 (LAN)` |
| ★ 결함 1건 | 첫 실기: 여는 쪽 성립·받는 쪽만 실패 → 루프백 단위 테스트는 통과 → 차이 = **논블로킹 리스너**: Windows는 `accept`한 스트림이 **논블로킹을 상속**(Linux는 아님) → 핸드셰이크 읽기가 WouldBlock 즉시 실패. 수락 직후 `set_nonblocking(false)` |
| 신원 공용 | `spawn_if_enabled`가 신원을 **한 번** 로드해 릴레이·LAN에 넘긴다(각자 `load_or_generate`면 첫 실행에 키가 둘 생길 수 있다) · 릴레이 끊김은 `all_offline_via("relay")`(LAN 세션은 산다) · `request_disconnect`가 LAN 세대도 올림 |
| ★ UI 비차단(사용자) | 원칙: **UI 스레드는 채널 송수신·이력 등재·게시만**. 수신 = 세션 스레드가 디코드·OS 표현 변환(PNG→DIB)·에코 지문까지 끝내고 `SyncItem{reps,summary,skip_hash}` 전달 · 송신 = `has_peers()`(값싼 검사) 뒤 워커 스레드가 페이로드(DIB→PNG 인코드 포함) 생성·전송 · 릴레이/LAN/세션/다이얼은 전부 별도 스레드(타임아웃 · 세대 폴링) |

**실측**(한 PC 두 인스턴스 `--profile b`): 비콘 상호 수신 → `기기 연결(LAN)` **양방향** → 뒤이은 릴레이 세션은
"세션 중복 — 새 것을 버림"(LAN이 먼저 서면 그쪽이 쓰인다). clippy 0 · 테스트 473(+2). 방화벽: 이 PC는 nexa-clip 인바운드
허용 규칙(Private)이 이미 있었다 — 다른 PC는 첫 실행 때 Windows 방화벽 허용이 필요할 수 있다(비콘 47301/udp · 수락 tcp).
⏳ 실기: **릴레이를 끊고**(서버 없이) 같은 LAN의 두 PC가 목록에 `(LAN)`으로 뜨고 전파되는지 · mac↔Windows LAN.

## 6차(Windows) — ★ `--profile <이름>`(한 PC에서 동기화 2대 시험) + "중계 서버 없이 가능한가" 답

**옵션**: `nexa-clip --profile b`(어느 명령 앞뒤든 · `--profile=b`도) → 데이터 폴더 `data/profiles/b`(설정·저장소·신원·
기기 목록·서버 핀 전부 분리 = **다른 기기**) · 단일 인스턴스 가드 = 뮤텍스/이벤트 이름에 프로필 접미(Unix는 잠금 파일이
폴더 안이라 저절로) · 창 제목 `[b]` · 트레이 툴팁 `Nexa Clip [b]` · 기본 기기 이름 `호스트명-b` · 자동 시작 등록은
프로필 실행이 건드리지 않음. 이름 규칙 영문·숫자·`-`·`_` 1~32자.
**실측**: 기본 + `--profile b` 동시 상주(PID 2개) · b는 0개 복원·별도 폴더 · 전역 단축키는 둘째가 등록 실패(첫째 소유 —
안내 메시지 · 트레이 좌클릭으로) — 시험엔 무해.

**사용자 질문 "같은 네트워크·같은 아이디·암호의 두 PC가 **중계 서버 없이** 동기화되는가"** — **아니오(현재 설계)**.
만남(페어링 RID 등록·Open)·종단 세션·항목 전파가 전부 릴레이(`beepd`) 경유다. `connect_via`의 UDP 홀펀칭도 릴레이가
관측 주소를 실어 줘야 시작된다. **LAN 직결은 T-25(LAN 발견)·T-26(직결)** — docs/09 §7 경로 A("내 기기 찾기") 설계만
있고 미구현. 필요한 것: LAN 비콘(mDNS 또는 UDP 브로드캐스트 · 핸들·암호 파생 RID로 응답 필터) → 직결 TCP(`tcp.rs` 링크 +
같은 Noise·prologue) → 기존 인사·기기 목록·승인·전파 재사용. 릴레이 접속은 그대로 두고 **LAN 경로를 우선 시도**하는
사다리(beep `path.rs` 등급) 형태가 맞다.

## 5차(Windows) — ★ OS별 기호·이모지 폴백 체인(두부 제거) + mac 3차 수신

**저장소 최신화**: mac 3차 2커밋 fast-forward(쓰기 내재화·settle·⇄ 재전송 금지·Carbon 단축키). Windows clippy 0 ·
테스트 470. ⇄ 재전송 금지는 셸 공통이라 Windows에도 산다(재빌드 후).

**두부 진단**(사용자 스크린숏 — Sublime엔 ✓·🎉가 보이는데 목록·미리보기는 □): 글자 단위 폴백 체인(09-01)은 있었지만
**주 글꼴 + 시스템 본**뿐이라 U+2713·이모지를 받을 본이 없었다. Sublime은 OS 텍스트 엔진이 Segoe UI Symbol/Emoji로 넘긴다.

| OS | 체인(주 글꼴 → 시스템 본 → 아래 순) | 효과 |
|---|---|---|
| Windows | Segoe UI Symbol → Segoe UI Emoji | ✓·화살표·수학 기호 · 이모지는 **흑백 윤곽** |
| macOS | Apple Symbols(·Supplemental) → Arial Unicode(있으면) | 기호 · 이모지는 두부 유지 |
| Linux | DejaVu Sans → Noto Sans Symbols2 → Noto Sans Symbols | 기호 · 이모지는 두부 유지 |

구현: `nclip-plat/font.rs` `SYMBOL_CANDIDATES` + `symbol_fallback_fonts()`(존재하는 것만 mmap) · `conf::load_ui_font`이 **모든
경로**(시스템 기본·사용자 지정·못 찾음)에서 붙인다 + 기동 로그 `글꼴 폴백: … · 커버 ✓= 🎉=`(실기에서 어느 본이 빠졌는지 즉시 보임).
기준선·줄 높이는 주 글꼴이 계속 정한다(글자 단위 폴백).

⚠️ **정직한 한계**: ab_glyph는 TrueType 윤곽만 그린다 — Apple Color Emoji(sbix)·Noto Color Emoji(CBDT)는 비트맵뿐이라 넣지
않는다(넣으면 빈칸). **컬러 이모지 = 별도 과제**(COLR/CPAL 또는 비트맵 테이블 디코드 — TODO T-18f 신설).
**실측**: Windows 기동 로그 `글꼴 폴백: Segoe UI Symbol → Segoe UI Emoji · 커버 ✓=true 🎉=true`. ⏳ 실기: 목록·미리보기에서 ✓·🎉 표시(흑백).
## 4차(macOS) — 승격 실기 확정 · ★ mac 자동 왕복 검사 신설 · ★ set/read 공용 직렬화(SIGSEGV 근절)

**사용자 확정**: 3차 수정 후 더블클릭 승격 동작("안 되더니 방금 됨" = 에코 지연 — 감시 폴링 200ms
+ settle 30ms×N이 겹친 체감 지연 · 결함 아님). 이어서 "Windows 기준 동작의 mac 자동 검사" 요청.

| 축 | 내용 |
|---|---|
| push 판정 로그 | `on_captured`에 `이력: New/Promoted/Replaced — "라벨"` 1줄(3-OS) — 승격/증식/에코를 콘솔로 판정(이번 진단이 어려웠던 이유가 이 불가시성) |
| ★ 자동 검사 | `clipboard.rs::mac_tests` 3종 — **실 파스텔보드 왕복**(beep macclip 테스트 선례): 텍스트(한글·이모지) · **다중 표현**(표준 UTI + 앱 사설 타입) · PNG. 단언 = **에코 부분집합 성질**(게시본 전부가 같은 이름·같은 바이트로 감시 읽기에 돌아온다 — `history::is_subset` 승격과 P-2 원본 붙여넣기의 토대). `cargo test -p nclip-plat mac_tests` |
| 진단 예제 | `clip_roundtrip` mac 본편(그간 Windows 전용) — A 스냅숏 → 재게시 → B 비교 표 + 판정. 실측: 한글 텍스트 **"✓ 전부 동일"** |
| ★ SIGSEGV | 첫 실행에서 테스트 3개 병렬 → **SIGSEGV** — 쓰기(`set_reps`)와 감시 읽기(`read_snapshot`)가 **다른 잠금**(beep "동시 clearContents/set = SIGABRT" 실측의 확장). 상주 앱도 메인 스레드 set × 감시 스레드 read가 같은 창이라 **잠재 크래시**였다 → `clip_serial()` 공용 잠금으로 통일(프로세스 밖은 파스텔보드 서버가 중재) + 테스트는 별도 잠금으로 직렬화(같은 잠금이면 재진입 교착) |

**실측**: mac_tests 3/3 ✓ · clippy 0 · 전체 20 스위트 ✓ · 재기동.
**관찰(⏳ 원인 미상)**: 이력에 "미리보기 준비 중"/"미리보기 없음" 문자열 항목 2건 등재(사용자 스크린샷) —
앱 자신의 i18n 문자열이 캡처된 것. 재현 경로 관찰 대기(다음 발생 시 콘솔 `이력:` 로그로 출처 판정).

## 3차(macOS) — ★ mac 실기 회신 4건: 쓰기 내재화 · settle · 수신 표식 재전송 금지 · Carbon 단축키

**사용자 실기 제보**(2대 전파 실기 중): ① 메인창 더블클릭이 최상위 승격 안 됨 ② 한 번 복사가
두 항목으로 등재(7zz 라벨이 `…2026` / `…2026-07-`로 미묘히 다름 = 부분 스냅숏) ③ 이미지 전파가
기기 사이 **핑퐁** ④ Ctrl+Shift+V 팝업 무동작. + 지시 2건: **수신 항목 플래그 + 재전송 금지** ·
**출처 기기 표시**.

| # | 근인 | 수정 |
|---|---|---|
| ① | ★ **mac 클립보드 쓰기가 스텁**(`clipboard.rs` — "미이식" Err) → 재적재 실패 = 에코 없음 = 승격 없음. 수신 항목 게시도 동일 실패 | `NSPasteboard` 직접 게시 구현 — 이식 원본 beep `macclip.rs`(set 경로) + **다중 표현 확장**(clearContents 후 표현마다 `setData:forType:` · UTI 이름 그대로 = 감시 읽기와 맞물려 에코 판정 성립 · 프로세스 안 뮤텍스 직렬화 — beep SIGABRT 실측) |
| ② | mac 감시가 changeCount 변화 즉시 읽음 — 앱이 표현을 **여러 단계로 게시**하면 부분 스냅숏 + 최종본 = 두 항목(Windows Sublime 증식 09-01과 동일 계열 · watch_linux 주석 "mac은 안 겪는다"는 오판) | `watch_mac::read_settled` — 두 번 연속 같게 읽힐 때까지 30ms × 최대 4회(`watch_win`과 동일 문법) |
| ③ | 에코 가드가 **페이로드 지문 10초**뿐 — 창 밖 에코·재복사가 되돌아 나감 | ★ **수신 표식 = 영속 플래그**(사용자 지시): `source_app = "⇄ 기기명"` 접두가 곧 플래그 — 저장·복원·**목록 우측 출처 표시**(main_win 기존 렌더)까지 스키마 변경 0. `on_captured`에서 맨 앞 항목이 ⇄면 **어떤 경로든 재전송 금지**(지문 가드는 보조 유지) |
| ④ | T-15 mac 미이식 | ★ **Carbon `RegisterEventHotKey`**(tray.rs mac `hotkey` — 시스템 전역 · **손쉬운 사용 권한 불요** · 대체 공개 API 없음 · `global-hotkey` crate와 같은 선언 차용 — **의존 추가 0**). Ctrl+Shift+V → `TrayEvent::Hotkey`(앱 이벤트 타깃 = winit 루프 배달) · 결과 1회 통지(HotkeyStatus) |

**실측**: clippy 3타깃 0 · 테스트 20 스위트 전부 통과 · debug 빌드. Cargo: app-kit `NSPasteboard`·foundation `NSData` feature만 추가(새 crate 0).
⏳ **2대 실기 재요청**: 더블클릭 승격 · 복사 1회 = 1항목 · 이미지 전파 핑퐁 소멸(⇄ 항목 재전송 금지) ·
Ctrl+Shift+V 팝업(mac) · 수신 항목 목록 우측 `⇄ 기기명` 표시.

## 2차(Windows) — ★ 클립보드 전파 1단(DR-6): 복사·클릭 승격 → 승인된 기기로, 수신 = 이력 + 게시 (사용자 요청)

**저장소 최신화**: mac 1차 3커밋(T-12e mac 트레이·Dock 정책 · 재실행 확인) fast-forward · Windows 빌드/테스트 이상 없음
(`ui.dock_icon`은 CatGeneral에 전 OS 노출 · 다른 OS no-op — 설명에 명시돼 있어 그대로 둠).

**설계**(2대가 릴레이에 붙어 목록에 서로 보이는 상태에서):

| 축 | 내용 |
|---|---|
| 휴대 페이로드 | `syncitem.rs` — `NCLI` v1 · `image/png` → `text/plain` **고정 순서**(같은 내용 = 같은 바이트) · OS 표현 ↔ 휴대형(Windows `PNG`+`CF_DIB` · mac `public.png` · Linux `image/png` · 평문 = `plain_text_reps`) · DIB만 있으면 디코드→PNG(긴 변 4096 상한) · **파일은 보내지 않음**(경로 목록은 후속) · 32MB 상한 |
| 종단 와이어 | `NCI1` 조각(60KB — Noise 65535 한계) + `Assembler`(seq별 조립 · 32MB) — Noise 안 논리 메시지라 서버·beep 무관 |
| 송신 | 피어별 `mpsc` 채널(`PEER_TX`) · `broadcast` = **승인 + 온라인**만 · 세션 루프 250ms 폴로 조각 송신 |
| 트리거 | `Captured` 본문을 `on_captured(snap, remote)`로 분리 — 우리 캡처(원격 아님)만 `maybe_broadcast`. ★ **클릭 승격은 재적재 에코**(expect_echo → Promoted)가 같은 경로라 자동 포함(팝업 선택·트레이 최근·메인 복사·스택) |
| 수신 | 완성 페이로드 → `ShellEvent::SyncItem` → `apply_remote`: 이력 등재(출처 `⇄ 기기명`) + **클립보드 게시**(그 PC에서 바로 Ctrl+V) + `expect_echo(0)` |
| 에코 차단 | 적용 직전 **우리가 게시할 표현**으로 지문(`from_reps → hash`) 기록 → 감시가 다시 잡은 승격의 지문이 같으면 10초 안엔 되돌려 보내지 않음(핑퐁 방지). 지문을 원격 페이로드가 아니라 게시 표현으로 잡는 이유 = OS가 개행/널을 손봐도 감시 재독과 같은 바이트가 되게 |
| ★ 승인 | docs/09 §6-3 "여기는 타협하지 않는다" — `devices.txt` **v2**(승인 필드 · v1 호환 읽기) · 설정 **"기기 승인"** 버튼(지금 연결된 기기 전부 · 기기당 1회) · 목록에 승인됨/승인 필요 · 승인 전엔 **보내지도 받지도 않음**(수신은 로그만) |

**실측**: clippy 0 · 테스트 470(+4) · debug 빌드. beep 서버·와이어 변경 0(docs/22 대상 아님).
⏳ 2대 실기: 양쪽 "기기 승인" → 텍스트/이미지 복사 → 상대 이력 맨 위 + 바로 붙여넣기 · 클릭 승격 전파 · 핑퐁 없음.
**후속**: 리치(HTML) 전파 · 파일 경로 목록 · 덮어쓰기 정책/되돌리기(T-27 나머지) · DeviceList 서명 승인(T-24).

## 1차(macOS) — ★ T-12e mac: 메뉴바 상주(NSStatusItem) + 연결 배지 + Dock 정책 (사용자 요청 3건)

**배경**: mac 실기에서 "동기화 연결이 안 된다" 진단 → 릴레이·설정·Test는 전부 정상인데,
러너 재기동(`take_sync_respawn` → `spawn_if_enabled`)을 소비하는 곳이 **트레이 셸뿐**이고
mac은 트레이 미이식이라 상주 러너가 영영 못 뜨는 구조 공백이었다.
사용자 지시: ① **윈도우와 동일하게 메뉴바 상주** ② **서버 연결 시 아이콘 변경**
③ **Dock 표시 여부 선택**(미표시 = 메뉴바에서만). ①이 근본 해결이라 우회(설정 단독 배선) 대신 이식으로 갔다.

| 축 | 내용 |
|---|---|
| 트레이 | `nclip-plat/tray.rs::mac` 신설 — 이식 원본 `nexa-beep` `nbeep-plat/src/tray.rs::mac`(M3-2b) + **최근 항목(tag=인덱스)·설정 메뉴 확장**(Windows/Linux 모듈과 동일 구성: 헤더·최근·열기·설정·종료). AppKit 메인 스레드 강제 → 상태는 스레드 로컬 · `spawn`/`update`는 winit 루프에서만 · 메뉴는 update마다 재구성(최근 항목 수가 매번 다름) |
| 아이콘 | 호스트 합성 RGBA → `NSBitmapImageRep` → `NSImage`(18×18pt 표시 = 레티나 2x). ★ **연결 배지 = 기존 `overlay_sync_dot`(녹색 점) 그대로** — `SyncState` → `refresh_tray` → `update` 한 번이 곧 "연결 시 아이콘 변경"(mac 추가 코드 0) |
| Dock 정책 | `nclip-plat/dock.rs` 신설(이식 원본 beep `dock.rs`) — `ui.dock_icon` 토글(기본 켬 · 레지스트리 CatGeneral). 끔 = `NSApplicationActivationPolicy::Accessory`(Dock·⌘Tab 소멸 · 메뉴바만): **기동 = winit 빌더 `with_activation_policy`**(Dock 깜빡임 없음) · **실행 중 = 설정 창 즉시 반영** · ★ 열기 = `activate_front()`(Accessory에서 창을 진짜 앞으로 — `bring_to_front` mac 분기) |
| 의존(DR-8) | `objc2` 0.5 · `objc2-app-kit` 0.2(클래스 feature 명시) · `objc2-foundation` 0.2 — **winit과 같은 판**(Cargo.lock 동일 항목 · 트리 중복 0 · 산출물 증가 미미). 원장 [10 §3](../10-decision-record.md) |
| i18n | `SetDockIcon`/`SetDockIconDesc` 4개 언어 · 완전성 목록 187→189 |

**실측**: 빌드 ✓ · clippy 3타깃 0 경고 · 테스트 전부 통과(개수 불변) · **실기동**: `tray` 상주 ✓ ·
감시 ok(mac-changecount-poll) · 키 주입 ok · ★ **동기화 러너 자동 가동** — 릴레이 접속 ✓(핀 5c5e…) ·
★ **`kiros33@windows` 첫 만남 ✓**(`devices.txt` 1행 — 09-03 13차 기기 목록이 mac↔Win 실전에서 처음 동작).

**재실행 확인**(같은 날 2차 기동): ★ **저장소 1개 복원** — 상주 중 캡처 항목이 암호화 영속을 왕복(mac 첫 확인) ·
★ **kiros33@windows 재연결**("새 기기" 아님 — `devices.txt` 영속 판정 동작) · 감시/키 주입/릴레이 전부 ok.

⏳ **사용자 실기**: 메뉴바 아이콘·메뉴(최근/열기/설정/종료) · 연결 녹색 점 · `ui.dock_icon` 끔/켬(Accessory 전환 ·
메뉴바 "열기"가 창을 앞으로) · 설정 Report 행에 kiros33@windows 표시.
**후속**: T-15 mac 전역 단축키(팝업 호출 경로 없음 — `cursor_pos`도 그때 배선) · 정식 알림(UNUserNotificationCenter · .app 번들 필요).
