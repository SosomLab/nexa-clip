# 2026-09-03

> 시간 역순 — 최신이 위.

## 1차 — ★ T-14 본편: Linux 클립보드 내재화(x11rb+XFIXES) — xclip 의존 소멸

사용자 요청(09-02 밤) — "별도 브랜치에서 리눅스 클립보드 전체를 Windows 동등 수준까지 일괄 개발".
브랜치 `feat/linux-clipboard-native` · 롤백 태그 `rollback/pre-linux-clipboard-native`(main 9994096).
설계 = [29 §6](../29-linux-clipboard-access.md) 그대로.

| 축 | 내용 |
|---|---|
| 신설 | `nclip-plat/src/selection_x11.rs`(~700줄) — **연결 3개**: 감시(XFIXES 이벤트 · 유휴 블록) · 읽기(ConvertSelection + **INCR 수신** · Mutex 트랜잭션) · 서빙(SetSelectionOwner + SelectionRequest 응대 + **INCR 송신** · TARGETS/TIMESTAMP/MULTIPLE) |
| 감시 | `watch_linux` 사다리에 `X11Native` 1순위 삽입 — **폴링·spawn 소멸**(XFIXES 이벤트 푸시 · DR-9). settle·지문 중복 제거·민감 표식 fail-closed 등 기존 규칙 전부 승계. 에코 = **소유자 창 비교**로 원천 차단 |
| 쓰기 | `clipboard.rs` — X11/XWayland면 **표현 전부 게시**(1단 "표현 1개" 제약 해소). text/plain은 UTF8_STRING·STRING·TEXT·charset 별칭까지 광고(GTK·Qt 호환) |
| 상한 | INCR **청크 도중** 집행(수신) · 게시 ack 1s · 정체 송신 10s GC — CopyQ 방어층 수용 |
| 폴백 | 도구 파이프는 계단으로 잔존(확정 ①) — x11rb 연결 실패 시 xclip/wl-paste. data-control Wayland(KWin·Sway)는 wl 도구 유지(P3) |
| 의존 | 새 crate 0 — x11rb `xfixes` feature만 추가([10 §3](../10-decision-record.md#3-외부-의존-원장-dr-8) 원장 갱신) |

**실측(이 PC — xclip·wl-clipboard 전무)**:
- 실서버(:0) 왕복 테스트 ✓ — 소형 + **1.5MB INCR** + 상한 도중 집행(0.05s)
- 환경 점검 `clipboard watch : ok (xwayland-x11rb)` — 어제까지 `MissingTool("xclip")`이던 자리
- ★ **E2E**: 외부 프로세스 게시 → 앱 감시 수집 → 트레이 최근 메뉴 + 영속(seg-000001.idx) ✓
- clippy 3타깃 · 테스트 전부 통과

⏳ 사용자 실기(안내 별도) · 관찰: status 명령이 `| head` 파이프에 Broken pipe 패닉(SIGPIPE) — 경미·후속.
