# 2026-09-08

> 시간 역순 — 최신이 위.

## 2차(win) — 마감: 병합 `eb0dcb4` · 설치본 재교체(#8648 · 13:00) · MILESTONES 등재 · push (사용자 요청 09-08)

- 1차 브랜치 `feat/stack-paste-keymap-09-08`(2커밋) → main `--no-ff` 병합 · 브랜치 삭제 · BRANCHES 표 등재.
- 사용자 "빌드 및 설치된 버전 재시작" → `dev-install-win.ps1` 한 번 더(릴리스 21s · #49512 종료 → #8648). 빌드 대상 = 병합된 main.
- MILESTONES M1에 **창 안 키맵 설정 승격 + 스택 4변형** ✅ 행(실기 ⏳). push 뒤 `gh run watch`.

## 1차(win) — ★ 스택 순차 붙여넣기 4변형(줄바꿈 `Alt+Enter` · 평문 `Shift+Enter` · 평문+줄바꿈 `Shift+Alt+Enter`) + ★ 창 안 단축키 14종 설정 승격 (사용자 요청 09-08)

**요청**: "연속 붙여넣기 방법 다시 알려줘" → "Release 빌드 후 실행 파일 교체(실행 중 프로세스 중단·재실행). Ctrl로 항목 담고 Enter = 순차 붙여넣기가 됨. **`Alt+Enter`는 항목 간에 `\n` 추가** · `Shift+Enter`는 서식 없이니 **서식 없이 연속 + `\n` = `Shift+Alt+Enter`** · **각 단축키는 설정 가능하게 · 기본 내장 단축키도 단축키 항목에 모두 추가**".

| 축 | 내용 |
|---|---|
| 줄바꿈 = **Enter 키 주입** | 클립보드에 `"\n"`을 게시해 Ctrl+V 하면 감시가 **공백 항목**으로 잡는다(승격으로 흡수되긴 하나 첫 회는 새 항목). 키 한 번은 이력에 흔적이 없고 서식 항목(HTML/RTF) 뒤에도 같은 뜻(줄 바꿈·새 문단·다음 셀)이다. `PasteInjector::send_newline` 신설 — Win `send_chord(false, VK_RETURN)`(쥔 Ctrl도 잠시 뗀다 · Ctrl+Enter는 앱마다 다른 뜻) · mac `tap(KVK_RETURN, 0)` · Linux XTest `Return`/포털 `KEY_ENTER=28`. **항목 사이에만**(마지막 뒤 없음) · 게시→주입 60ms · 주입→Enter 60ms · 항목 사이 160ms |
| 평문 순차 | `reps_for_mode(item, Plain)`이 비면(이미지·개체) **원본으로** — 건너뛰면 "3개 담았는데 2개 붙음"이 된다 |
| ★ 창 안 단축키 → 설정 | `nclip_core::hotkey::WINDOW_ACTIONS` 14종(설정 키 · Win/Linux 기본 · mac 기본): `pick`/`pick_plain`/`pick_n`(숫자 자리 패턴)/`stack_toggle`/`stack_paste`×4/`pin`/`delete`/`settings`/`view_rich|compact|plain`. 전역(`ACTIONS`)과 같은 문자열 규약 · **OS 등록 없음** · 레지스트리 하위 그룹 `SubKeysGlobal`/`SubKeysWindow`(첫 `sub` 사용 — 사이드바 하위·그룹 헤더 경로) · `hotkey_rows_match_core` 테스트가 레지스트리 기본값·전역 여부·그룹을 core 원천과 대조 |
| 판정 = `nexa-clip::keys::Keymap` | `Chord::of(물리 키, ctrl, shift, alt, meta)` → `Hotkey::matches`(**수식 키 넷 정확 일치** — `Ctrl+Alt` = AltGr는 `Ctrl+1`과 달라 자연 배제 · 09-07 `!alt` 특례 제거) · `pick_n` = 수식 키 같고 주 키 숫자 1~9. 키맵 순서: 설정 → 번호 → (팝업) 담기 → 스택 4변형 / 단일 선택 · (메인) 보기 → 고정 → 삭제 → 선택. 검색창 편집(Ctrl+A/C/X/V·캐럿)·글자 입력은 그 뒤 논리 키 분기 |
| 캡처 규칙 분리 | 전역 = 수식 키 필수(F키 단독 가능 · 종전) · **창 안 = 글자·숫자·`,`만 Ctrl/Alt/Win 필수**(`Hotkey::is_window_safe` — Shift+글자는 대문자 검색어 · docs/17 M-1) · Enter/Delete/Space 단독 허용 · 안내 문구 `HotkeyNeedModWin`. `KeyCode` +`Backspace`(VK 0x08 · mac 0x33 · `BackSpace`) +`Comma`(VK_OEM_COMMA 0xBC · mac 0x2B · `comma`) · `keycode_token` +Enter/NumpadEnter/Backspace/`,`/Numpad 숫자 → `keys.rs`로 이동(settings_win과 공용) |
| 표시 = 설정값 | `number_badge(n)`(pick_n 조합의 숫자 자리 치환 · 없음 = 배지 없음) · `view_label()`(셋이 수식 공유 시 `Alt+1/2/3`로 접기 · 아니면 ` · ` 나열 · 없음 = 상태줄 이름만) · 툴팁 `with_key`(없음 = ` ({})` 괄호째 제거) · 팝업 푸터: 단일 = `HintFiles/Rich/Image/Default` `{}` 치환 · **스택 = 2줄**(`FOOTER_STACK_H` 40 · `HintStack` 개수·순차·+줄바꿈 / `HintStack2` 평문·평문+줄바꿈·담기) — 24px 한 줄엔 4변형이 안 들어간다 |
| 셸 배선 | `Shell::apply_keymap`(박동마다 `Keymap::sig` 비교 · 바뀌면 `main/popup.set_keymap`) · 기동 1회 · `ShellEvent::PasteStack { ids, as_, newline }` · `PopupAction::PickStack { .. }` · `paste_stack(ids, as_, newline)` |
| 제거 | `popup_win::number_badge`/`SETTINGS_KEY_LABEL`/`VIEW_KEY_LABEL`/`digit_of`/`settings_primary` · 메인 `Enter`/`Delete`/`Ctrl+P`/`Ctrl+,` 하드코딩 분기 · 팝업 `Ctrl+Space`/`Ctrl+,` 분기. 남긴 것: 팝업 `Ctrl/Alt+Enter` = D-38 수식 변형(개체/경로 · 단일 항목 · 키맵에 없을 때만) |
| i18n | +20(4언어 · `ALL_MSG` 255): 하위 그룹 2 · 창 안 설명 · 동작 라벨 14 · `HotkeyNeedModWin` · `HintStack2`. 템플릿화: `HintStack`·`HintFiles/Rich/Image/Default`·`TipPin/Delete/Copy/CopyPlain` |
| 테스트 | core `window_actions_defaults`(14 · 파싱·창 안 규칙·중복·전역 판별) · `window_safe_and_matches` · ui `hotkey_rows_match_core` · nexa-clip `keys` 3건(정확 일치·pick_n·AltGr·지운 키·표기·토큰) — 워크스페이스 전부 ✓ |
| 고정(설정 불가) | `Esc` · `↑↓` · `Ctrl+A/C/X/V` · `Ctrl+클릭` 담기 · 편집 시트 `Ctrl+Enter`/`Alt+Z` — 편집 관례 키라 항목에 넣지 않았다(docs/04 키맵 각주) |
| 문서 | docs/04 키맵 표(설정 키 열 · 스택 4행 · 고정 키 각주) · 14 §3-2(전역/창 안 표) · **21 §12-1 K12~K22** · TODO **T-48** |
| 배포 | `scripts/dev-install-win.ps1`(릴리스 빌드 → 설치본 교체 → 재시작) |

**⏳ 실기**: [21 §12-1 K12~K22](../21-manual-test.md) — 특히 K13(줄바꿈이 **사이에만**) · K14(이미지 섞인 평문 스택) · K17(하위 그룹 첫 사용 — 사이드바 계층 렌더) · K19(키 충돌 시 순서).
