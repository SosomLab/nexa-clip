# 2026-09-07

> 시간 역순 — 최신이 위.

## 2차(win) — ★ 창 안 키맵 정비: 항목 선택 `Ctrl/⌘+1~9` · 보기 `Alt/⌥+1/2/3` · 단축키 표시 (사용자 확정 09-07)

**요청**: "유사 제품은 항목 선택에 어떤 단축키를 쓰나 · 보기를 Alt+1/2/3으로 옮기고 선택을 Ctrl+1~9로 vs 보기 유지+Shift 조합 검토" → **권장안(전자) 확정** → "적용된 단축키가 메인/팝업에서 잘 보이게".

| 축 | 내용 |
|---|---|
| 조사(웹) | **Maccy** `⌘1~9` 선택 · `⌥n` 즉시 붙여넣기 · `⌥⇧n` 평문 · **CopyQ** 메인창 `Ctrl+1~0`(기본) · 트레이 메뉴 맨숫자 · **Ditto** `Ctrl+1~0` 퀵 페이스트(옵션) · Alfred/Raycast 계열 `⌘숫자` = N번째. → **주 수식 키 + 숫자 = N번째**가 표준 · 사용자 기준선 둘(Maccy·CopyQ)이 정확히 이 조합 |
| 기각한 대안 | `Shift+숫자` = 검색창 포커스라 `!@#`가 검색어(맨숫자와 같은 문제 · docs/17 M-1) · `Ctrl+Shift+숫자` = mac `⇧⌘3/4/5` 시스템 스크린숏 충돌 + 두 손가락 · 선택에 `Alt` = 팝업 Alt(경로만) 의미와 겹침 |
| 판정 | 선택 = `Ctrl+1~9`/mac `⌘1~9`(팝업·메인 공통) · 보기 = `Alt+1/2/3`/mac `⌥1/2/3`(메인) · 규칙 3: **보이는 순서**(필터 후·핀 구획 포함) · **붙여넣기 모드 원본 고정**(Ctrl 누른 채 치므로 "Ctrl=개체" 미적용) · **물리 키 자리로 매칭**(AZERTY 숫자=Shift · mac `⌥1`=`¡` · `Ctrl+Alt`=AltGr 배제) |
| 코드 | `popup_win.rs`: `digit_of`(물리 Digit1~9) · `number_badge`/`SETTINGS_KEY_LABEL`/`VIEW_KEY_LABEL`(OS별 표기) · 번호 선택 → `Pick{Original}` · 1~9행 우측 배지(스택 순번 행은 스택 우선). `main_win.rs`: 번호 선택 = Enter와 같은 길(선택 이동·따라가기·원본 복사) · `Alt+1/2/3` 보기(종전 `Ctrl+1/2/3` 논리 키 arm 제거) · Rich/Compact/Plain 우측 배지 · 상태줄 우측 "보기 이름 · Alt+1/2/3" · ⚙ 툴팁 "설정 (Ctrl+,)". `i18n.rs`: `TipSettings`·`StatusView`(4언어 · ALL_MSG 236) |
| 문서 | **[21 §12 K1~K11](../21-manual-test.md) 실기 점검표 신설**(설정 바로가기 · 번호 선택 · 보기 전환 · 표시 · AltGr/한글 자판) · docs/04 FR-P-8 ✅ · 키맵 표에 보기 행 · §2-2 보기 메뉴 `Alt+1·2·3` · V-2 · TODO **T-47** 등재(잔여 = `Ctrl+Enter`·`Ctrl+Alt+Delete`·팝업 `Ctrl+P`/`Delete` · 위키) |

**실측**: `check-3os.sh` fmt ✓ · win ✓ · mac ✓ · linux ✓ · `nclip-core` i18n 4/4 ✓. ⏳ **실기**: 팝업 `Ctrl+3` → 3번 항목 붙여넣기(검색 필터 후 번호 재배정 확인) · 메인 `Ctrl+2` 선택+복사 · `Alt+1/2/3` 보기 · 상태줄/배지/툴팁 표시 · mac `⌘`/`⌥` 동판 · AltGr 자판(있으면).

## 1차(win) — 저장소 최신화(29 커밋 ff · v0.1.2) · ★ 설정 바로가기 `Ctrl+,`/`⌘,` 창 안 단축키 (사용자 요청 09-07)

**요청**: "저장소 최신화 및 변경사항 분석" → "CTRL(CMD)+, 로 설정 바로가기 단축키 설정을 적용해줘".

| 축 | 내용 |
|---|---|
| clip 최신화 | `fff555a` → `917309d` **29 커밋 ff**(충돌 0 · 51파일 +1494/-75) · 태그 **v0.1.2** 수신 · CI 최근 6 run 전부 success · 참조 `nexa-beep`·`nexa-dir2`는 원격 일치(0/0) |
| 들어온 코드(Windows 호스트에 실제 적용) | ① `nexa-conf` 설정 파일 **0600**(`cfg(unix)` — Windows 무영향) + **미지 키 중복 차단**(known 우선 · 3-OS) ② 설정 창 위치 키 `ui.set_*` 등재(재시작 복원 · 3-OS) ③ 팝업 단축키 잔향(T-15d) — `is_synthetic` 버림 · 첫 새 누름 전 repeat 버림 · 유예를 `Focused(true)`부터(**Windows도 같은 구조**라 그대로 듣는다 · ⏳ 실기) ④ `warm_up` 토큰 인자 Linux 전용(Windows에 `%APPDATA%` 폴더 안 만듦) ⑤ `keys.rs` 비밀 파일 생성 시점 0600 |
| 들어온 코드(Linux 전용) | 포털 RemoteDesktop 세션 자가 복구 · X11 페이저 소스 창 올리기(`raise_x11_window`) · 승인 토큰 고정 자리 · 런처 하이재킹 근절(`packaged_install`) · 포털 오류 로그 · `dev-install-linux.sh` |
| 들어온 절차 | **T-40 `scripts/check-3os.sh`**(push 전 3타깃 clippy) · 위키 캡처 20장(`assets/screenshots/`) · beep 전달문 32 처리 결과(원장 22) · T-41~T-46 등재 |
| 호스트 검증 | `cargo clippy --workspace --all-targets -D warnings` Windows ✓(5.8s) · `check-3os.sh` fmt ✓ · win ✓ · mac ✓ · linux ✓ |

**★ 설정 바로가기 단축키** — [docs/04 §2-1 키맵](../04-feature-scope-and-screens.md)에 이미 `설정 = Ctrl+, / ⌘,`가 명세돼 있었고 구현만 없었다.

| 축 | 내용 |
|---|---|
| 판단 | **창 안 단축키**(메인창·팝업)로 구현 · **전역 아님** — 전역으로 잡으면 VS Code 등 다른 앱의 `Ctrl+,`(설정 열기)를 시스템 전체에서 가로챈다. mac은 ⌘,(앱 환경설정 관례 · DR-2 "동작 관례는 각 OS 네이티브") |
| 메인창 | `main_win.rs` `Key::Character(",") if self.primary` → `MainAction::OpenSettings`(툴바 ⚙와 같은 길 · `primary` = mac ⌘ / 그 외 Ctrl) |
| 팝업 | `popup_win.rs` — 팝업은 ⌘를 추적하지 않았다(붙여넣기 모드가 ⇧/Ctrl/Alt) → `meta` 상태 추가 + `settings_primary()`(mac ⌘ · 그 외 Ctrl) · `PopupAction::OpenSettings` 신설 |
| 셸 | `tray_cmd.rs` — `OpenSettings` = `close_popup()`(포커스 복원 · 붙여넣기 없음) → `open_settings(el)` |
| 미변경 | 설정 → 단축키 화면(전역 3종)에는 넣지 않음 · 트레이 메뉴 · 위키 단축키 페이지(⏳ 반영 대상) |

**실측**: 코드 3파일(+29/-0) · 3-OS clippy ✓ · ⏳ **실기**: 메인창 `Ctrl+,` · 팝업 `Ctrl+,`(팝업 닫힘 + 설정 창 앞) · mac `⌘,` 양쪽.
