# 2026-09-01

> 시간 역순 — 최신이 위.

## 3차 — ★ S2 메인창 1단(T-18b0) + 실기 후속(트레이 최근·목록 휠/버튼)

사용자 "메인화면을 먼저 만들어줘" — [docs/28](../28-main-window.md) 설계(04 §2-2 계승 · 1단 범위 명시) 후
[`main_win.rs`](../../crates/nexa-clip/src/main_win.rs) 구현:

- 검색 1줄 + **좌측 세로 툴바 40px**(핀·삭제·복사·평문 · ⚙ 바닥 고정 · 벡터 아이콘) +
  목록(★ **핀 구획 상단** + accent 구분선) + 상태 1줄.
- Enter/더블클릭 = 원본 재적재(⇧ = 평문 · **주입 없음** — 관리 화면) · Delete = 삭제 ·
  Ctrl+P = 핀 · Esc/X = 숨김.
- 영속: 삭제 = REMOVE · ★ **핀 = 같은 id Add 재기록**(재생 교체 — docs/28 §4).
- ★ **트레이 좌클릭/"열기" = 메인창**(08-30 확정 이행 — 설정 승격 해제 · 설정은 ⚙).
- 이력 API: `get_by_id`·`remove(id)`.

실기 후속 셋(2차 뒤): 트레이 우클릭 최근 부재 = 복원 직후 refresh 누락 · 목록 휠 = last_mouse
추적으로 목록 우선 소비 · ＋/－ 무동작·색 안 돌아옴 = **MouseUp 미전달**(버튼은 놓음 확정) ·
버튼은 벡터 아이콘 75%로.

**게이트**: 374 테스트 · clippy 3타겟 · exe 11:07. 커밋 `71212ab`·`68fa262`.

## 2차 — 실기 후속 넷: 작업 영역 클램프 · 창 앞으로 · 목록 키 라우팅 · 빈 목록 i18n

사용자 보고 넷(ListBox 렌더링은 스크린샷 확인 — 목록·선택·버튼 동작):

| # | 보고 | 조치 |
|:--:|---|---|
| ① | 팝업이 여전히 작업표시줄에 가린다 + 우측 5px 여유 요청 | ★ `nclip-plat::screen` 신설 — `MonitorFromPoint`+`GetMonitorInfoW` **rcWork**(작업 영역) 기준 클램프 + 가장자리 5px(논리) 여유. 타 OS는 모니터 폴백 |
| ② | 트레이 클릭 시 창이 뒤로 숨음 | Windows 포그라운드 권한 규칙 — K-1의 AttachThreadInput 문법을 `paste::force_foreground`로 노출해 재사용(새 창·재열기 둘 다) |
| ③ | ListBox 선택 후 키가 사이드바 메뉴로 샘 | 목록이 포커스/편집 중이면 ↑↓·Delete·Enter·Esc·문자를 그 목록으로(검색 폴백 차단). Esc 3단: 편집 취소 → 포커스 해제 → 창 닫기 |
| ④ | 빈 목록 "—"를 i18n으로 | `Msg::ListEmpty`("내용 없음"/"No items"/4언어) — 컨트롤은 언어를 모르므로 호스트가 주입 |

곁다리 — 스크린샷이 잡은 결함: **− 버튼이 두부(□)** — U+2212가 맑은 고딕에 없다 → ASCII "+"/"-".
`RowCtl::List`는 Box(변형 크기 린트).

★ 사용자 작업 규약 확정: **재실기 요청 전 항상 디버그 빌드** · **미확인 항목은 묶어서 한꺼번에 요청**(메모리 저장).

**게이트**: 374 테스트 · clippy 3타겟 클린 · exe 10:42. 커밋 `9276e58`.

## 1차 — 실기 후속 셋: "기록 사라짐"의 정체 · 팝업 클램프 · ★ 차단 목록 ListBox

전날 실기 잔여 통과(휠·트레이 최근·자동 시작·hover/스플리터 · Word 원본 서식 유지 설정 = 팝업도 정상).
보고 셋 처리:

### ① "Clear history on quit 꺼져 있는데 종료 시 기록이 사라짐" → **소실 아님 · 시작 블록**

진단 덤프 신설([`store_dump`](../../crates/nclip-store/examples/store_dump.rs) + `diag`) — 이벤트 로그 전수:
**REMOVE 0 · wipe 0 · 14항목 + blob 5 전부 생존** · 재적재 실측 "14개 복원". 실체는 **debug 빌드에서
ChaCha20이 opt-level 0(~10MB/s)** 이라 이미지 blob(17MB) 복원이 시작을 수 초 블록 — 그 사이 트레이
툴팁·팝업이 비어 "사라진 것으로" 보였다. 조치: dev 프로필에서 **암호 원어 크레이트만 opt-level 3**
(RustCrypto 관행 · release 무관). 후속 여지: blob 지연 적재(시작은 인덱스만) — T-16 본편.

### ② 팝업 화면 경계 클램프 (사용자 요청)

커서 좌상단 기준으로 그리면 우측 하단에서 잘렸다 → [`clamp_to_monitor`](../../crates/nexa-clip/src/popup_win.rs)
— 커서가 든 모니터를 찾아 **전체가 들어가게 좌상단을 되민다**(모니터 배율로 물리 크기 환산 ·
미검출 = 그대로 fail-soft).

### ③ ★ 차단 페이지·제외 앱 = ListBox + 추가/삭제 + 인라인 편집 (사용자 요청)

`;` 한 줄 자유 입력(RadioInput)이던 것을 **`ListEditor` 컨트롤 신설**(nclip-ctl 18번째)로 교체:

| 조작 | 동작 |
|---|---|
| 행 클릭 / 선택 행 재클릭 | 선택 / **인라인 편집**(더블클릭 사건이 없는 이벤트 모델) |
| ＋ / − / `Delete` | 빈 행 추가+즉시 편집 / 선택 삭제 |
| `Enter` / `Esc` | 확정(빈 값 = 행 삭제) / 취소(유령 줄 정리) |
| ↑↓ · 휠 | 선택 이동 · 5행 창 스크롤 |

값 모델은 `;` 구분 한 줄 그대로 — **저장 형식·게이트 판정 코드 무수정**(화면만 목록). 편집은
`TextBox` 재사용이라 IME·클립보드·우클릭 메뉴 라우팅이 콤보와 같은 문법(`editing_input`).
설정 위젯에 `SettingKind::ListEdit` 통합(높이·포커스 재계산·틱·편집 라우팅 5블록).

**게이트**: **374 테스트**(listedit 6 신규) · clippy 3타깃 클린 · settings 창 무패닉 스모크. 커밋 1(`682e065`).

## 4차 — 메인창 다듬기: 머티리얼 툴바·툴팁 · 창 위치 기억 · ★ D1 = 복원 시점 차이

실기 통과: 메인창 표시 · ListBox 추가/삭제/편집 · C8 · D2~D4 · E1. 조치 셋:

| 보고 | 조치 |
|---|---|
| 툴바 머티리얼 + 툴팁 | hover 원형 상태 레이어(10% · Material 3 아이콘 버튼 문법) + 즉시 툴팁(오른쪽 · 단축키 병기 · 자체 렌더 — OS 툴팁 없음) |
| 닫힌 위치에 다시 열기 | 닫기/종료 때 `ui.win_x/y/w/h` 저장(예약 키 활용) → 다음 open이 `with_position`+`with_inner_size` |
| ★ D1 첫 우클릭에 최근 없음 | **시점 차이** — 트레이가 빈 메뉴로 먼저 뜨고 저장소 복원·refresh가 뒤였다 → **복원을 트레이 생성 앞으로**(초기 메뉴부터 채워짐 · 결정론적) |

**게이트**: 374 테스트 · clippy 3타깃 · exe 11:37.
