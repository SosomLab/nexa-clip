# 21 · 실기 점검표 — Windows · macOS · Linux 각각

> **역할**: ★ **OS별로 무엇이 검증됐고 무엇이 안 됐는지**를 한 장에서 관리한다.
> 사용자 방침(2026-08-26): *"맥 버전에 대한 테스트는 맥에서 점검 요청을 할 테니, 기능에 대한 검증을 Windows·macOS 각각 확인할 수 있도록 관리해줘."*
>
> ★ **자동 테스트가 못 잡는 것만 여기 있다.** 순수 로직은 `cargo test`가 잡고([docs/20 §7](20-implementation-spec.md)),
> **OS 권한·포커스·클립보드·트레이처럼 사람이 눈으로 봐야 하는 것**이 이 문서의 대상이다.
>
> **갱신 규칙**: 점검한 사람이 **날짜·버전·결과**를 채운다. 실패는 지우지 말고 **증상을 그대로** 남긴다.

---

## 0. 현황 요약

| 항목 | Windows | macOS | Linux(X11) | Linux(Wayland) |
|---|:--:|:--:|:--:|:--:|
| **T-14b** ★ **클립보드 감시**(`watch`) | ✅ **자동 확인**(08-27) | ☐ 미구현 | ☐ | ☐ |
| **T-14b'** 감시 — 앱별 훑기 1차(PPT·Excel·Edge·VS Code) | ✅ **완료**(08-27 · 18건) | ☐ | ☐ | ☐ |
| **T-14b''** 감시 — 2차(**Word** · 탐색기 **잘라내기**) | ✅ **완료**(08-27 · 결함 셋 → 수정) | ☐ | ☐ | ☐ |
| **T-14b'''** 감시 — 수정 재점검(**탐색기 복사·잘라내기**) | ✅ **통과**(08-27 · ⑫ 근본 수정 후 사용자 실기 — *"이제 정확하게 잡히고 있어"*) | ☐ | ☐ | ☐ |
| **T-21b** ★ **브라우저 암호 차단**(DR-36 · 기본 꺼짐) | ✅ **통과**(08-28 · **Chrome + Edge** 사용자 실기 — Edge는 실경로 `settings/autofill/passwords` 추가 후) | ☐ | ☐ | ☐ |
| **T-21c** ★ **차단 목록 편집**(08-28) | ⏳ **점검 요청** — `settings` → 보안·개인정보에서 ① "차단할 출처 페이지"에 항목 추가/삭제 후 동작 확인 ② "제외할 앱"에 앱 이름(예: `Code`) 넣고 그 앱 복사 → `(제외 앱 …)` 한 줄만(E2E는 통과 · 창 편집 실기 필요) | ☐ | ☐ | ☐ |
| **T-12c** 설정 **읽기**(저장본 반영) | ✅ **자동 확인**(08-26) | ✅ **자동 확인**(08-27) | ☐ | ☐ |
| **T-12c3** 설정 **쓰기**(창에서 값 변경 → 재시작) | ✅ **통과**(08-27 · 사용자 실기) | ⏳ **점검 요청**(창은 뜸 · 21개) | ☐ | ☐ |
| **T-12d5** 개수 설정이 **숫자로** 보인다 | ✅ **통과**(08-27 · 사용자 실기) | ⏳ 점검 요청 | ☐ | ☐ |
| **T-12d6** hover가 **서서히** 밝아진다(트리·버튼·콤보) | ⏳ 점검 요청 | ⏳ 점검 요청 | ☐ | ☐ |
| **T-9b** K-1 키 주입 | ✅ **실사용 통과**(08-26) | ✅ **실사용 통과**(08-27) | ☐ 미구현 | ✕ 구조적 불가 |
| **T-9b'** K-1 **포커스 복원** | ✅ **통과**(08-27 · 사용자 실기 — 탈취 경로 실행) | ✅ **통과**(08-27 · iTerm→Sublime) | ☐ 미구현 | ✕ 구조적 불가 |
| **T-12d7** ★ **고DPI 배율**(Retina) | ✅ (배율 1.0) | ⚠️ **수정함 · 재점검 요청**(08-27) | ☐ | ☐ |
| **T-14** 클립보드 감시 | ☐ 미구현 | ☐ 미구현 | ☐ 미구현 | ⚠️ 컴포지터 의존 |
| **T-15** 전역 단축키 | ☐ 미구현 | ☐ 미구현 | ☐ 미구현 | ⚠️ 표준 없음 |
| **T-12e** 트레이 상주 | ⏳ **점검 요청**(08-28 · `nexa-clip tray`) — ① 트레이에 청록 아이콘 ② 우클릭 = 이름/열기/종료 메뉴 ③ 복사할수록 **툴팁에 "N개 수집"** ④ ★ **좌클릭/열기 = 설정 창** ⑤ 메뉴 종료로 끝남 | ☐ **미이식**(objc2 결정 필요) | ☐ 미이식 | ☐ 미이식 |
| **T-12e2** ★ **자동 시작**(기본 켜짐) | ⏳ **점검 요청** — `settings`에서 토글 끔/켬 시 콘솔에 `자동 시작: …` · 재로그인 후 자동 실행(또는 `HKCU\…\Run\NexaClip` 확인). **등록/해제 자체는 자동 검증 ✅**(08-28 레지스트리 실측) | ☐ | ☐ | — |
| **T-12e3** ★ **닫을 때 트레이로**(기본 꺼짐) | ⏳ **점검 요청** — `tray` 상주 중 ① 창 X/Esc = **앱 종료**(기본) ② 설정 "창을 닫아도 트레이에 남기" 켬 → X/Esc = **트레이로 숨고 상주 유지** → 좌클릭으로 재열림(값 유지) | ☐ | ☐ | — |
| **T-18e** ★ **트레이 최근 메뉴 + 재적재**(1단) | ⏳ **점검 요청**(08-28) — `tray` 상주 중 몇 개 복사 → ① 우클릭 메뉴에 **최근 항목**(최신 위 · 최대 N=`ui.tray_recent_n`) ② 항목 클릭 → 다른 앱에 **Ctrl+V로 그대로 붙는다**(서식·파일 포함) ③ 같은 것 재복사 = 목록 안 늘고 **맨 위로** ④ 툴팁 "N개 보관" | ☐ | ☐ | ☐ |
| **T-12b2** 창·렌더 데모(+알파) | ✅ **통과**(08-27 · 두부 수정 확인 — "두부 사라짐") | ⏳ **점검 요청** | ☐ 미확인 | ☐ 미확인 |
| **T-12d2** ★ **설정 창**(+스플리터) | ⏳ **점검 요청**(실행됨·눈 확인 필요) | ⏳ **점검 요청** | ☐ 미확인 | ☐ 미확인 |

> 범례: ✅ 통과 · ❌ 실패(증상 기록) · ⏳ **점검 요청**(코드는 있으나 그 OS에서 미확인) · ☐ 미구현 · ✕ 구조적 불가 · ⚠️ 환경에 따라 갈림

---

## 1. 공통 준비

```bash
git pull
cargo run -p nexa-clip            # 환경 점검 — 이 PC에서 무엇이 되는지
```

`nexa-clip`을 인자 없이 실행하면 **되는 것과 안 되는 것**을 정직하게 출력한다.
여기서 `paste inject`가 `ok`가 아니면 [§2](#2-t-9b--k-1-포커스-복원--키-주입)의 원인 표를 먼저 본다.

---

## 1-1. ★ T-12d7 — 고DPI 배율 (Retina) · 08-27 수정

### 증상 (사용자 제보 · macOS 스크린샷)

설정 창에서 **레이아웃은 정상 크기인데 글자만 깨알**로 보였다 — 행 높이·사이드바 폭·여백은
2배로 커졌는데 텍스트만 1배로 남은 상태.

### 원인

`RasterCtx`는 배율을 **글자 크기에 곱해** 그린다. 그런데 호스트가 그 값을 넘기지 않았다.

```rust
// 잘못된 코드 — 배율이 기본값 1.0으로 남는다
let mut dc = RasterCtx::new(&mut gfx, &self.font);
```

위젯에는 `set_scale(2.0)` 을 줬으므로 **레이아웃만 2배**가 됐다.
⚠️ **Windows(배율 1.0)에서는 우연히 맞아 보인다** — 그래서 한쪽 OS에서만 조용히 틀렸다.
`nexa-beep`는 같은 자리에서 `.with_scale(entry.scale)` 을 호출하고 있었다(`app.rs:14055`).

### 조치 — ★ 테스트가 아니라 **구조**로 막았다

`RasterCtx::new(surface, font, **scale**)` — 배율을 **필수 인자**로 승격했다.
빌더 옵션이면 또 빠뜨릴 수 있지만 인자는 **컴파일러가 강제**한다.

### 재점검 (macOS)

| # | 확인 |
|:--:|---|
| 1 | `cargo run -p nexa-clip -- settings` — 글자 크기가 행 높이에 **맞아 보이는가** |
| 2 | 창을 **Retina ↔ 외부 모니터** 로 끌어 옮겨도 즉시 맞는가(`ScaleFactorChanged`) |
| 3 | `cargo run -p nexa-clip -- demo` — 같은지 |

---

## 1-2. T-14b — 클립보드 감시 (`nexa-clip watch`)

```bash
cargo run -p nexa-clip -- watch
```

복사할 때마다 **무엇이 잡혔는지** 한 덩어리씩 찍는다. `Ctrl+C` 로 끝낸다.

### Windows — ✅ 자동 확인 (2026-08-27)

| 복사한 것 | 종류 | 미리보기 |
|---|---|---|
| `한글 평문 확인` | `Text` | `한글 평문 확인` |
| `#1E90FF` | `Color` | `#1E90FF` |
| 파일 2개 | `Files` | `Cargo.toml 외 1개` |
| .NET 비트맵 320×240 | `Image` | `미리보기 준비 중`(T-14c 이전) |

### ⏳ 점검 요청 — **앱별로 훑어 주세요**

★ 이 명령의 목적은 *"우리가 아직 모르는 곁다리 포맷"* 을 찾는 것이다([27 §8-1](27-capture-cases.md)).
아래를 복사하고 **종류가 맞는지**만 봐 주시면 됩니다.

| # | 복사할 것 | 기대 종류 | 특히 볼 것 |
|:--:|---|---|---|
| 1 | **PPT 글상자 2개** | `RichText` | ★ `Art::GVML ClipFormat`이 목록에 있는가 · `Image`로 떨어지지 않는가 |
| 2 | PPT **도형만**(글 없음) | `Image` | 글이 없으면 그림이 맞다 |
| 3 | **Word 서식 문단** | `RichText` | `HTML Format` · `Rich Text Format` |
| 4 | **Excel 범위** | `RichText` | 벤더 포맷 이름 |
| 5 | **Chrome 웹 텍스트** | `RichText` | `msSourceUrl` 같은 곁다리가 새로 보이는가 |
| 6 | **Chrome 이미지** | `Image` | ⚠️ 리치로 떨어지면 알려 주세요 |
| 7 | **탐색기 파일 3개** | `Files` | 이름이 `외 2개`까지 맞는가 |
| 8 | **탐색기 잘라내기** | `Files` | `Preferred DropEffect` 표시 |
| 9 | **메모장 한 줄** | `Text` | 곁다리가 없어야 정상 |
| 10 | ★ **비밀번호 관리자에서 복사** | (아무 줄도 안 늘어남) | **민감 표식이 막는가**(FR-S-1) |

★ **종류가 틀린 것이 나오면 그 덩어리를 통째로 붙여 주세요** — 표현 이름 목록이 곧 원인입니다.

> ★ **1차 훑기(08-27) 결과** — 18건에서 결함 셋을 잡았다([27 §8-2](27-capture-cases.md)).
> 판정은 [`tests/real_clipboard_captures.rs`](../crates/nclip-core/tests/real_clipboard_captures.rs)에 박제했다.
>
> ★ **2차 훑기(08-27) 결과** — Word ✅(`RichText` 2건) · 탐색기에서 **결함 셋**을 더 잡았다
> ([27 §8-3](27-capture-cases.md)): ⑧ 잘라내기가 `Object`(→ `Files`로 수정) ·
> ⑨ **복사가 아예 안 잡힘**(→ 타이머 재시도) · ⑩ 민감 표식 무시(→ 마스킹).
> Outlook은 사용자 미사용으로 **보류**.
>
> **3차(수정 재점검)로 볼 것**:
>
> ★ **08-27 경과** — 첫 복사만 잡히고 그다음이 유실되는 증상을 **직접 재현**:
> `WM_CLIPBOARDUPDATE` **자체가 간헐적으로 안 온다**(⑪ · [27 §8-3](27-capture-cases.md)).
> **하트비트 안전망**(2초 일련번호 비교)을 추가하고 실제 탐색기 `^C` × 3 왕복으로 검증했다.
>
> | # | 할 것 | 기대 |
> |:--:|---|---|
> | 1 | `watch` **재시작** 후 탐색기 **복사**(Ctrl+C)를 **여러 번 반복** | ★ 매번 잡힌다 — 즉시(이벤트) 또는 **2초 안**(하트비트) · 종류 `Files` |
> | 2 | 탐색기에서 파일 **잘라내기**(Ctrl+X) 반복 | 종류가 **`Files`** (⑧ 수정 · `CF_HDROP`이 없으면 미리보기는 아직 "미리보기 없음"이 정상) |
> | 2b | 그래도 안 잡히는 게 있으면 `NEXA_CLIP_DIAG=1` 로 `watch` 실행 후 재현 | stderr의 `[diag]` 줄을 통째로 붙여 주세요 — "안 왔다"와 "와서 버려졌다"를 구분해 줍니다 |
> | 3 | ~~**Edge 비밀번호 복사**~~ | **완료(08-27)** — 아래 결과 참조 |
>
> ★ **3번 결과(08-27 실기)** — ⚠️ **Edge(Microsoft 암호 관리자)는 표식을 붙이지 않는다.**
> 암호 복사가 `CF_UNICODETEXT` + Chromium 내부 표식뿐인 평문 `Text`로 잡혔다
> (`ExcludeClipboardContentFromMonitorProcessing` 없음). 우리 결함이 아니라 **Edge의 동작**이다 —
> 같은 이유로 이 복사는 **Windows 클립보드 기록(Win+V)·클라우드 클립보드에도 그대로 남는다**.
>
> | 갈래 | 상태 |
> |---|---|
> | 표식이 **있으면** 막는가(⑩ 우리 코드) | ✅ 모의 클립보드 자가 검증(08-27) |
> | 표식을 **실제로 붙이는 앱** 실기 | ⏳ 전용 관리자(1Password·KeePass·Bitwarden) 보유 시에만 — 없으면 보류 |
> | 표식 **없는** 비밀번호(Edge류) 방어 | ☐ 표식만으로는 불가 → **제외 앱(FR-S-2 · T-21)** + Chromium `source URL` 휴리스틱(**D-79**) |

### `peek` — 한 번만 보고 싶을 때

```bash
cargo run -p nexa-clip -- peek
```

감시를 걸지 않으므로 **`watch` 세션과 함께 써도 된다**. *"방금 복사한 게 뭐로 잡혔나"* 만 볼 때.

---

## 2. T-9b · K-1 — 포커스 복원 + 키 주입

> ★ **제품의 존재 이유가 걸린 검증**이다. 이 왕복이 안 되면 "선택하면 붙여넣어진다"가 성립하지 않는다.
> 창을 만들기 전에 먼저 검증한다([docs/02 K-1](02-roadmap.md#7-리스크--일정을-실제로-흔들-것)).

### 2-1. 절차 (3-OS 공통)

```bash
cargo run -p nexa-clip -- spike-paste          # 원본 붙여넣기 경로
cargo run -p nexa-clip -- spike-paste --plain  # 평문 붙여넣기 경로
cargo run -p nexa-clip -- spike-paste --wait 8 # 대상 고를 시간 늘리기
```

1. **아무 텍스트나 복사**해 둔다(`Ctrl+C` / `⌘C`).
2. 붙여넣을 앱(메모장 · TextEdit · 편집기)을 열고 **커서를 둔다**.
3. 스파이크를 실행하고, 카운트다운 동안 **그 앱을 클릭**해 포그라운드로 만든다.
4. 카운트다운이 끝나면 프로그램이 **포커스를 뺏었다가**(팝업이 뜨는 순간을 흉내) **되돌리고 붙여넣기 키를 넣는다**.
5. ★ **대상 앱에 텍스트가 들어갔는지 눈으로 확인**한다.

### 2-2. 판정

| 관찰 | 판정 |
|---|---|
| 대상 앱에 텍스트가 들어갔다 | ✅ **통과** |
| `[5] 포커스 복원: ok` 인데 붙여넣기가 안 됐다 | ❌ 주입 실패 — 키 코드·수식 키 문제 |
| 창이 **깜빡이기만** 하고 포커스가 안 갔다 | ❌ 복원 실패 — Windows면 `AttachThreadInput` 경로 확인 |
| `권한 필요` 가 떴다 | ⏸ macOS 손쉬운 사용 허용 후 재시도([§2-4](#2-4-macos--점검-요청)) |

### 2-3. Windows — ✅ 통과 (2026-08-26)

| 항목 | 값 |
|---|---|
| 점검일 · 버전 | 2026-08-26 · `0.1.0` |
| 환경 | Windows 11 Pro 26200 |
| `capability` | `Full { backend: "win32-sendinput" }` |
| **키 주입** | ✅ **실사용 통과** — 사용자가 Sublime Text 대상으로 붙여넣기 성공 확인 |
| **포커스 복원** | ⚠️ ★ **미검증** — 아래 참조 |

**⚠️ 실행에서 `[3] 포커스 탈취: 실패` 가 떴다**(Windows Terminal에서 `GetConsoleWindow`가 안 잡힘).

```
[2] 대상 기억 : … - Sublime Text …
[3] 포커스 탈취: 실패
[5] 포커스 복원: ok
[6] 키 주입    : ok (Original)
→ 붙여넣기 성공
```

★ **탈취가 실패했다는 것은 대상이 계속 포그라운드로 남았다는 뜻**이다. 그러면 `SetForegroundWindow`는
사실상 할 일이 없었고, **`AttachThreadInput` 우회 경로는 실행되지 않았다.**
따라서 이 실행이 증명한 것은 **키 주입까지**이고, **복원은 아직 미검증**이다.

**조치(08-26)**: 콘솔 창을 못 잡으면 **임시 최상위 창을 잠깐 띄워** 진짜로 포커스를 가져오도록 스파이크를
고쳤다. 다시 돌려서 `[3] 포커스 탈취: ok` 가 뜬 상태로 붙여넣기가 되면 그때 복원까지 통과다.

**재검증(08-27)**: ✅ **통과** — 사용자 실기로 탈취 경로가 실행된 상태에서 붙여넣기 확인
(출력 미첨부 · 문제 재발 시 출력 첨부 요청).

**구현 메모** — `SetForegroundWindow`는 아무 프로세스나 부를 수 없다(포그라운드 강탈 방지).
우리 스레드를 대상 창의 입력 큐에 **붙였다 떼는**(`AttachThreadInput`) 우회가 필요하고,
**없으면 창이 깜빡이기만 한다**. `INPUT` 구조체 크기(x64에서 40B)가 틀리면 `SendInput`이
조용히 0을 돌려주므로 **컴파일 시점 `assert!`** 로 잠갔다.

> 🔴 **남은 실사용 확인**: 실제 붙여넣기가 대상 앱에 들어가는지 **사람이 눈으로** 봐야 한다
> (위 절차 5번). 자동 점검은 `capability`까지만 본다.

### 2-4. macOS — ✅ 통과 (2026-08-27)

| 항목 | 값 |
|---|---|
| 점검일 | 2026-08-27 |
| 명령 | `cargo run -p nexa-clip -- spike-paste --wait 8` |
| 대상 | Sublime Text |
| **선행 조건** | ★ **시스템 설정 → 개인정보 보호 및 보안 → 손쉬운 사용에 `iTerm` 추가** |
| 결과 | ✅ **붙여넣기 동작 확인**(사용자 실기) |

★ **Windows와 달리 복원 경로까지 실제로 돌았다.** mac은 스파이크를 띄운 터미널이
프런트모스트였다가 대상 앱으로 넘어가므로 `NSRunningApplication` 활성화가
**우회 없이 정상 경로로** 실행된다(Windows에서 `[3] 포커스 탈취: 실패` 로 건너뛴 구간).

⚠️ **권한은 터미널 앱에 붙는다.** `cargo run`으로 돌리면 권한 주체가 우리 바이너리가 아니라
**터미널(iTerm·Terminal.app)** 이다. 배포본(`.app` 번들)에서는 **번들 자체를 손쉬운 사용에 추가**해야
하고, 그때 권한이 다시 물어진다 — 개발 중 통과가 **배포본 통과를 뜻하지 않는다**(→ T-9d).

### 2-4-old. macOS — (구) 점검 요청 양식

| 항목 | 값 |
|---|---|
| 점검일 · 버전 | (미기입) |
| 환경 | (macOS 버전 기입) |
| `capability` | (`cargo run -p nexa-clip` 출력 붙여넣기) |
| 결과 | ⏳ |

**먼저 할 일 — 권한**

`capability`가 `needs permission`으로 나오면 **시스템 설정 → 개인정보 보호 및 보안 → 손쉬운 사용**에서
터미널(또는 실행한 앱)을 허용한다. ★ **권한을 켠 뒤에는 터미널을 재시작**해야 반영된다.

**구현 메모(미검증)** — `NSWorkspace.frontmostApplication`으로 PID를 기억하고,
`NSRunningApplication.activateWithOptions:`로 되돌린 뒤 `CGEventPost`로 `⌘V`를 넣는다.
objc2 의존 없이 **objc 런타임을 직접 호출**했다(DR-8) — 이 부분이 실기에서 처음 도는 코드다.

> ⚠️ **이 경로가 미검증 1순위다.** `objc_msgSend` 직접 호출은 시그니처가 틀리면 **조용히 죽거나
> 엉뚱한 값**을 준다. 실패하면 증상(크래시/무반응/잘못된 앱 활성화)을 아래에 그대로 적어 주세요 —
> objc2 크레이트 도입(beep 선례)으로 전환할지 판단하는 근거가 된다.

**증상 기록란**

```
(여기에 출력 전체와 관찰한 증상을 붙여넣기)
```

### 2-5. Linux — ☐ 미구현

X11은 `XTestFakeKeyEvent`로 가능하다(T-15b). **Wayland는 키 주입 표준이 없어**
[FR-P-1(클립보드 적재)로 정직하게 강등](20-implementation-spec.md#3-4-️-직전-포커스-창-복원--키-주입-fr-p-2--k-1-리스크)한다.

---

## 2-6. T-12b2 · 렌더 데모 — 창이 뜨고 우리 화면이 그려지는가

```bash
cargo run -p nexa-clip -- demo
```

**조작**: `1` 일반 · `2` 간략 · `3` 한 줄 · ★ `P` **반투명 미리보기 패널** · `T` 다크/라이트 · `Esc` 종료

### 확인할 것

| # | 항목 | 통과 기준 |
|:--:|---|---|
| **D-1** | 창이 뜨고 **S1 퀵 팝업 레이아웃**이 보인다 | 헤더(이름+검색+토글) · 목록 · 푸터 1줄 |
| **D-2** | ★ **한글이 깨지지 않는다** | 실행 시 콘솔에 잡힌 폰트 이름이 찍힌다(Win=맑은 고딕 · mac=Apple SD Gothic Neo) |
| **D-3** | ★ **보기 3모드가 실제로 다르다** | `1`↔`2`↔`3`에서 **행 높이와 보이는 항목 수**가 바뀐다 |
| **D-4** | ★ **일반 보기만 행 높이가 가변** | 이미지·서식 행이 더 크게 잡힌다(가변 높이 가상화가 필요한 이유) |
| **D-5** | ★ **세로 밀도**(DR-14) | 한 줄 보기에서 항목이 가장 많이 보인다 |
| **D-6** | 테마 전환 | `T`로 다크↔라이트가 **색 하드코딩 없이** 바뀐다 |
| **D-7** | 고DPI | 배율 150%/200% 모니터에서 글자·간격이 뭉개지지 않는다 |
| **D-8** | ★ **알파 합성**(`P`) | 미리보기 패널이 **반투명(0.92)** 으로 뜨고 **뒤 목록이 비친다** · 그림자 가장자리가 부드럽다 → [23](23-alpha-rendering.md) |

### Windows — ⚠️ 두부 발견 · 수정 (2026-08-27)

**08-27 사용자 실기**: 크기·모양·레이아웃 양호. ⚠️ **일부 글자가 두부(□)로** 보였다.

**원인** — 의도가 아니라 **폰트에 없는 글자**다. 데모 데이터가 이모지(`📄`·`📁`·`🎨`·`🔒`)와
`↵`(U+21B5)를 썼는데, 래스터라이저는 시스템 폰트 **한 벌**만 쓰고 폴백이 없어서
맑은 고딕에 없는 글리프는 .notdef(□)로 그려진다. `▣`만 맑은 고딕에 있어 정상으로 보였다.

**조치** — KS X 1001 도형 문자로 교체(`▤`·`▦`·`◆`·`¶`) · 푸터의 `🔒` 제거.
제품 아이콘은 글리프가 아니라 **직접 그린다**(T-18b). **재점검**: 두부가 사라졌는지만 보면 된다.

```
폰트: 맑은 고딕
창을 엽니다 — 1/2/3 보기 전환 · T 테마 · Esc 종료
```

### macOS — ⏳ 점검 요청

(미기입 — 위 D-1~D-7 결과와 콘솔 출력을 적어 주세요)

```
(출력 붙여넣기)
```

> ⚠️ macOS는 **폰트 후보 경로**(`nclip-plat/src/font.rs`)가 실기에서 처음 돈다.
> `시스템 UI 폰트를 찾지 못했습니다`가 뜨면 그 메시지를 그대로 적어 주세요.

---

## 2-7. T-12d2 · 설정 창 — 이식한 프레임워크가 실제로 도는가

```bash
cargo run -p nexa-clip -- settings
```

실행하면 콘솔에 **`설정 항목 N개`** 가 찍힌다(우리 레지스트리 크기). 창에서 확인할 것:

| # | 항목 | 통과 기준 |
|:--:|---|---|
| **S-1** | 창이 뜨고 **좌측 카테고리 + 우측 폼**이 보인다 | 현재 구현분 카테고리 8개 |
| **S-2** | ★ **설정 검색** — 상단에 타이핑 | 레지스트리에서 **라벨·설명이 함께** 걸러진다 |
| **S-3** | ★ **검색이 카테고리도 좁힌다** | 좌측에 매치 개수 `(N)` 가 붙는다 |
| **S-4** | 값 변경(체크박스·콤보) | ★ **콘솔에 `설정 변경: key = value`** 가 찍힌다(즉시 적용 계약) |
| **S-5** | ★ **팝업 위치 기본값** | `모양` → `팝업 위치` 가 **마우스 위치**로 선택돼 있다([DR-24](10-decision-record.md) 필수 기능) |
| **S-6** | 키보드 | ↑↓ 이동 · Enter 확정 · Backspace 검색어 축소 · Esc 종료 |
| **S-7** | 마우스 | 클릭 선택 · 휠 스크롤(트랙패드 분수 노치 포함) |
| ★ **S-9** | **스플리터**(VS Code 방식) | 사이드바 경계에 커서를 두면 ① **경계선이 accent로 두꺼워지고 손잡이가 뜬다** ② **커서가 좌우 리사이즈로 바뀐다** ③ **드래그로 폭이 조절된다** |
| **S-8** | 고DPI | 배율 150%/200%에서 레이아웃이 무너지지 않는다 |

### Windows — ⏳ 점검 요청 (2026-08-26)

```
폰트: 맑은 고딕 · 설정 항목 21개
창을 엽니다 — 상단에서 설정 검색 · 값 변경은 콘솔에 찍힙니다 · Esc 종료
```

실행은 확인했다. **화면과 조작은 눈으로 봐야 한다.**

### macOS — ⏳ 점검 요청

(미기입 — S-1~S-9 결과와 콘솔 출력)

```
(출력 붙여넣기)
```

> ⚠️ macOS는 **주 수식키가 ⌘** 로 매핑돼 있다 — `⌘A`(전체 선택)가 되는지 함께 봐 주세요.

---

## 3. T-14 · 클립보드 감시 (구현 후)

| OS | 확인할 것 |
|---|---|
| **Windows** | 텍스트·이미지·**파일 복사**가 각각 1건씩 잡히는가 · 같은 것 재복사 시 **새 항목이 아니라 승격**인가 · 다른 앱이 클립보드를 잠갔을 때 **재시도로 회복**하는가 |
| **macOS** | 폴링이 **유휴 시 느려지는가**(배터리) · 놓치는 복사가 없는가 · `changeCount` 변화 없을 때 **읽지 않는가** |
| **Linux/X11** | 복사한 **창을 닫아도** 내용이 남는가(즉시 읽기 확인) |
| **Linux/Wayland** | 지원 컴포지터(Sway/Hyprland)에서 동작 · ★ **GNOME에서 "수집 불가"가 명시**되는가(조용한 빈 목록 금지) |

**공통 — 민감 정보 차단(FR-S-1)**: 1Password·KeePass 등에서 복사한 것이 **기록에 남지 않아야** 한다.
★ 이건 OS별로 표식이 다르므로 **3-OS 각각** 확인한다.

---

## 4. T-15 · 전역 단축키 (구현 후)

| OS | 확인할 것 |
|---|---|
| **Windows** | 다른 앱이 같은 키를 쓰고 있을 때 **충돌이 표시**되는가 |
| **macOS** | 권한 없이도 등록되는가(`RegisterEventHotKey`를 고른 이유) |
| **Linux/X11** | NumLock·CapsLock이 켜져 있어도 동작하는가 |
| **Linux/Wayland** | 표준이 없다 — **안내가 뜨는가** |

---

## 5. 점검 결과 기록 규칙

| 규칙 | 내용 |
|---|---|
| **R-1** | ★ **실패를 지우지 않는다.** 증상을 그대로 남기고 원인·조치를 뒤에 덧붙인다 |
| **R-2** | 점검마다 **날짜 + 버전 + 환경**을 적는다. 버전이 없으면 재현이 불가능하다 |
| **R-3** | ⏳(점검 요청)는 **코드는 있으나 그 OS에서 확인 안 됨**이다 — ✅과 반드시 구분한다 |
| **R-4** | 상세 경위는 [journal](journal/)에, 여기는 **현황과 절차**만 |
