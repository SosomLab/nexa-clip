# 빌드 산출물 (Rust)
/target/
/build/
/dist/
/out/
# Cargo.lock은 바이너리 워크스페이스이므로 커밋한다(재현 빌드).
**/*.rs.bk

# OS
.DS_Store
Thumbs.db
desktop.ini

# 에디터
.vscode/
.idea/
*.swp

# 로컬 실행 데이터 (포터블 모드 영속물)
/data/
*.log

# 신원·키 잔재 (배포·커밋 금지)
*.key
